//! Generated operations for the `payment_methods` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(clippy::doc_markdown, clippy::too_many_arguments, clippy::new_without_default, rustdoc::bare_urls)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/payment_methods`
///
/// Returns a list of all PaymentMethods.
#[derive(Debug, Clone)]
pub struct GetPaymentMethods {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetPaymentMethods {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/payment_methods".to_owned(), params }
    }

    /// This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment meth…
    #[must_use]
    pub fn allow_redisplay(mut self, value: impl Into<String>) -> Self {
        self.params.insert("allow_redisplay".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The ID of the customer whose PaymentMethods will be retrieved.
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The ID of the Account whose PaymentMethods will be retrieved.
    #[must_use]
    pub fn customer_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Filters the list by the object `type` field. Unfiltered, the list returns all payment method types except `custom`. If your integration expects only one type of payment method in the response, specify that type value in the request to redu…
    #[must_use]
    pub fn type_(mut self, value: impl Into<String>) -> Self {
        self.params.insert("type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::PaymentMethod> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::PaymentMethod>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/payment_methods`
///
/// Creates a PaymentMethod object. Read the Stripe.js reference to learn how to create PaymentMethods via Stripe.js. Instead of creating a PaymentMethod directly, we recommend using the PaymentIntents API to accept a payment immediately or the SetupIntent API to collect payment method details ahead of a future payment.
#[derive(Debug, Clone)]
pub struct PostPaymentMethods {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPaymentMethods {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/payment_methods".to_owned(), params }
    }

    /// If this is an `acss_debit` PaymentMethod, this hash contains details about the ACSS Debit payment method.
    #[must_use]
    pub fn acss_debit(mut self, value: serde_json::Value) -> Self {
        self.params.insert("acss_debit".to_owned(), value);
        self
    }

    /// If this is an `affirm` PaymentMethod, this hash contains details about the Affirm payment method.
    #[must_use]
    pub fn affirm(mut self, value: serde_json::Value) -> Self {
        self.params.insert("affirm".to_owned(), value);
        self
    }

    /// If this is an `AfterpayClearpay` PaymentMethod, this hash contains details about the AfterpayClearpay payment method.
    #[must_use]
    pub fn afterpay_clearpay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("afterpay_clearpay".to_owned(), value);
        self
    }

    /// If this is an `Alipay` PaymentMethod, this hash contains details about the Alipay payment method.
    #[must_use]
    pub fn alipay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("alipay".to_owned(), value);
        self
    }

    /// This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment meth…
    #[must_use]
    pub fn allow_redisplay(mut self, value: impl Into<String>) -> Self {
        self.params.insert("allow_redisplay".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// If this is a Alma PaymentMethod, this hash contains details about the Alma payment method.
    #[must_use]
    pub fn alma(mut self, value: serde_json::Value) -> Self {
        self.params.insert("alma".to_owned(), value);
        self
    }

    /// If this is a AmazonPay PaymentMethod, this hash contains details about the AmazonPay payment method.
    #[must_use]
    pub fn amazon_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("amazon_pay".to_owned(), value);
        self
    }

    /// If this is an `au_becs_debit` PaymentMethod, this hash contains details about the bank account.
    #[must_use]
    pub fn au_becs_debit(mut self, value: serde_json::Value) -> Self {
        self.params.insert("au_becs_debit".to_owned(), value);
        self
    }

    /// If this is a `bacs_debit` PaymentMethod, this hash contains details about the Bacs Direct Debit bank account.
    #[must_use]
    pub fn bacs_debit(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bacs_debit".to_owned(), value);
        self
    }

    /// If this is a `bancontact` PaymentMethod, this hash contains details about the Bancontact payment method.
    #[must_use]
    pub fn bancontact(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bancontact".to_owned(), value);
        self
    }

    /// If this is a `billie` PaymentMethod, this hash contains details about the Billie payment method.
    #[must_use]
    pub fn billie(mut self, value: serde_json::Value) -> Self {
        self.params.insert("billie".to_owned(), value);
        self
    }

    /// Billing information associated with the PaymentMethod that may be used or required by particular types of payment methods.
    #[must_use]
    pub fn billing_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("billing_details".to_owned(), value);
        self
    }

    /// If this is a `bizum` PaymentMethod, this hash contains details about the Bizum payment method.
    #[must_use]
    pub fn bizum(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bizum".to_owned(), value);
        self
    }

    /// If this is a `blik` PaymentMethod, this hash contains details about the BLIK payment method.
    #[must_use]
    pub fn blik(mut self, value: serde_json::Value) -> Self {
        self.params.insert("blik".to_owned(), value);
        self
    }

    /// If this is a `boleto` PaymentMethod, this hash contains details about the Boleto payment method.
    #[must_use]
    pub fn boleto(mut self, value: serde_json::Value) -> Self {
        self.params.insert("boleto".to_owned(), value);
        self
    }

    /// If this is a `card` PaymentMethod, this hash contains the user's card details. For backwards compatibility, you can alternatively provide a Stripe token (e.g., for Apple Pay, Amex Express Checkout, or legacy Checkout) into the card hash wi…
    #[must_use]
    pub fn card(mut self, value: serde_json::Value) -> Self {
        self.params.insert("card".to_owned(), value);
        self
    }

    /// If this is a `cashapp` PaymentMethod, this hash contains details about the Cash App Pay payment method.
    #[must_use]
    pub fn cashapp(mut self, value: serde_json::Value) -> Self {
        self.params.insert("cashapp".to_owned(), value);
        self
    }

    /// If this is a Crypto PaymentMethod, this hash contains details about the Crypto payment method.
    #[must_use]
    pub fn crypto(mut self, value: serde_json::Value) -> Self {
        self.params.insert("crypto".to_owned(), value);
        self
    }

    /// If this is a `custom` PaymentMethod, this hash contains details about the Custom payment method.
    #[must_use]
    pub fn custom(mut self, value: serde_json::Value) -> Self {
        self.params.insert("custom".to_owned(), value);
        self
    }

    /// The `Customer` to whom the original PaymentMethod is attached.
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// If this is a `customer_balance` PaymentMethod, this hash contains details about the CustomerBalance payment method.
    #[must_use]
    pub fn customer_balance(mut self, value: serde_json::Value) -> Self {
        self.params.insert("customer_balance".to_owned(), value);
        self
    }

    /// If this is an `eps` PaymentMethod, this hash contains details about the EPS payment method.
    #[must_use]
    pub fn eps(mut self, value: serde_json::Value) -> Self {
        self.params.insert("eps".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// If this is an `fpx` PaymentMethod, this hash contains details about the FPX payment method.
    #[must_use]
    pub fn fpx(mut self, value: serde_json::Value) -> Self {
        self.params.insert("fpx".to_owned(), value);
        self
    }

    /// If this is a `giropay` PaymentMethod, this hash contains details about the Giropay payment method.
    #[must_use]
    pub fn giropay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("giropay".to_owned(), value);
        self
    }

    /// If this is a `grabpay` PaymentMethod, this hash contains details about the GrabPay payment method.
    #[must_use]
    pub fn grabpay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("grabpay".to_owned(), value);
        self
    }

    /// If this is an `ideal` PaymentMethod, this hash contains details about the iDEAL payment method.
    #[must_use]
    pub fn ideal(mut self, value: serde_json::Value) -> Self {
        self.params.insert("ideal".to_owned(), value);
        self
    }

    /// If this is an `interac_present` PaymentMethod, this hash contains details about the Interac Present payment method.
    #[must_use]
    pub fn interac_present(mut self, value: serde_json::Value) -> Self {
        self.params.insert("interac_present".to_owned(), value);
        self
    }

    /// If this is a `kakao_pay` PaymentMethod, this hash contains details about the Kakao Pay payment method.
    #[must_use]
    pub fn kakao_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("kakao_pay".to_owned(), value);
        self
    }

    /// If this is a `klarna` PaymentMethod, this hash contains details about the Klarna payment method.
    #[must_use]
    pub fn klarna(mut self, value: serde_json::Value) -> Self {
        self.params.insert("klarna".to_owned(), value);
        self
    }

    /// If this is a `konbini` PaymentMethod, this hash contains details about the Konbini payment method.
    #[must_use]
    pub fn konbini(mut self, value: serde_json::Value) -> Self {
        self.params.insert("konbini".to_owned(), value);
        self
    }

    /// If this is a `kr_card` PaymentMethod, this hash contains details about the Korean Card payment method.
    #[must_use]
    pub fn kr_card(mut self, value: serde_json::Value) -> Self {
        self.params.insert("kr_card".to_owned(), value);
        self
    }

    /// If this is an `Link` PaymentMethod, this hash contains details about the Link payment method (Link is also known as Onelink in the UK).
    #[must_use]
    pub fn link(mut self, value: serde_json::Value) -> Self {
        self.params.insert("link".to_owned(), value);
        self
    }

    /// If this is a MB WAY PaymentMethod, this hash contains details about the MB WAY payment method.
    #[must_use]
    pub fn mb_way(mut self, value: serde_json::Value) -> Self {
        self.params.insert("mb_way".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert("metadata".to_owned(), serde_json::Value::Object(value.into_iter().map(|(k, v)| (k, serde_json::Value::String(v))).collect()));
        self
    }

    /// If this is a `mobilepay` PaymentMethod, this hash contains details about the MobilePay payment method.
    #[must_use]
    pub fn mobilepay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("mobilepay".to_owned(), value);
        self
    }

    /// If this is a `multibanco` PaymentMethod, this hash contains details about the Multibanco payment method.
    #[must_use]
    pub fn multibanco(mut self, value: serde_json::Value) -> Self {
        self.params.insert("multibanco".to_owned(), value);
        self
    }

    /// If this is a `naver_pay` PaymentMethod, this hash contains details about the Naver Pay payment method.
    #[must_use]
    pub fn naver_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("naver_pay".to_owned(), value);
        self
    }

    /// If this is an nz_bank_account PaymentMethod, this hash contains details about the nz_bank_account payment method.
    #[must_use]
    pub fn nz_bank_account(mut self, value: serde_json::Value) -> Self {
        self.params.insert("nz_bank_account".to_owned(), value);
        self
    }

    /// If this is an `oxxo` PaymentMethod, this hash contains details about the OXXO payment method.
    #[must_use]
    pub fn oxxo(mut self, value: serde_json::Value) -> Self {
        self.params.insert("oxxo".to_owned(), value);
        self
    }

    /// If this is a `p24` PaymentMethod, this hash contains details about the P24 payment method.
    #[must_use]
    pub fn p24(mut self, value: serde_json::Value) -> Self {
        self.params.insert("p24".to_owned(), value);
        self
    }

    /// If this is a `pay_by_bank` PaymentMethod, this hash contains details about the PayByBank payment method.
    #[must_use]
    pub fn pay_by_bank(mut self, value: serde_json::Value) -> Self {
        self.params.insert("pay_by_bank".to_owned(), value);
        self
    }

    /// If this is a `payco` PaymentMethod, this hash contains details about the PAYCO payment method.
    #[must_use]
    pub fn payco(mut self, value: serde_json::Value) -> Self {
        self.params.insert("payco".to_owned(), value);
        self
    }

    /// The PaymentMethod to share.
    #[must_use]
    pub fn payment_method(mut self, value: impl Into<String>) -> Self {
        self.params.insert("payment_method".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// If this is a `paynow` PaymentMethod, this hash contains details about the PayNow payment method.
    #[must_use]
    pub fn paynow(mut self, value: serde_json::Value) -> Self {
        self.params.insert("paynow".to_owned(), value);
        self
    }

    /// If this is a `paypal` PaymentMethod, this hash contains details about the PayPal payment method.
    #[must_use]
    pub fn paypal(mut self, value: serde_json::Value) -> Self {
        self.params.insert("paypal".to_owned(), value);
        self
    }

    /// If this is a `payto` PaymentMethod, this hash contains details about the PayTo payment method.
    #[must_use]
    pub fn payto(mut self, value: serde_json::Value) -> Self {
        self.params.insert("payto".to_owned(), value);
        self
    }

    /// If this is a `pix` PaymentMethod, this hash contains details about the Pix payment method.
    #[must_use]
    pub fn pix(mut self, value: serde_json::Value) -> Self {
        self.params.insert("pix".to_owned(), value);
        self
    }

    /// If this is a `promptpay` PaymentMethod, this hash contains details about the PromptPay payment method.
    #[must_use]
    pub fn promptpay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("promptpay".to_owned(), value);
        self
    }

    /// Options to configure Radar. See \[Radar Session\](https://docs.stripe.com/radar/radar-session) for more information.
    #[must_use]
    pub fn radar_options(mut self, value: serde_json::Value) -> Self {
        self.params.insert("radar_options".to_owned(), value);
        self
    }

    /// If this is a `revolut_pay` PaymentMethod, this hash contains details about the Revolut Pay payment method.
    #[must_use]
    pub fn revolut_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("revolut_pay".to_owned(), value);
        self
    }

    /// If this is a `samsung_pay` PaymentMethod, this hash contains details about the SamsungPay payment method.
    #[must_use]
    pub fn samsung_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("samsung_pay".to_owned(), value);
        self
    }

    /// If this is a `satispay` PaymentMethod, this hash contains details about the Satispay payment method.
    #[must_use]
    pub fn satispay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("satispay".to_owned(), value);
        self
    }

    /// If this is a Scalapay PaymentMethod, this hash contains details about the Scalapay payment method.
    #[must_use]
    pub fn scalapay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("scalapay".to_owned(), value);
        self
    }

    /// If this is a `sepa_debit` PaymentMethod, this hash contains details about the SEPA debit bank account.
    #[must_use]
    pub fn sepa_debit(mut self, value: serde_json::Value) -> Self {
        self.params.insert("sepa_debit".to_owned(), value);
        self
    }

    /// If this is a `sofort` PaymentMethod, this hash contains details about the SOFORT payment method.
    #[must_use]
    pub fn sofort(mut self, value: serde_json::Value) -> Self {
        self.params.insert("sofort".to_owned(), value);
        self
    }

    /// If this is a `sunbit` PaymentMethod, this hash contains details about the Sunbit payment method.
    #[must_use]
    pub fn sunbit(mut self, value: serde_json::Value) -> Self {
        self.params.insert("sunbit".to_owned(), value);
        self
    }

    /// If this is a `swish` PaymentMethod, this hash contains details about the Swish payment method.
    #[must_use]
    pub fn swish(mut self, value: serde_json::Value) -> Self {
        self.params.insert("swish".to_owned(), value);
        self
    }

    /// If this is a TWINT PaymentMethod, this hash contains details about the TWINT payment method.
    #[must_use]
    pub fn twint(mut self, value: serde_json::Value) -> Self {
        self.params.insert("twint".to_owned(), value);
        self
    }

    /// The type of the PaymentMethod. An additional hash is included on the PaymentMethod with a name matching this value. It contains additional information specific to the PaymentMethod type.
    #[must_use]
    pub fn type_(mut self, value: impl Into<String>) -> Self {
        self.params.insert("type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// If this is a `upi` PaymentMethod, this hash contains details about the UPI payment method.
    #[must_use]
    pub fn upi(mut self, value: serde_json::Value) -> Self {
        self.params.insert("upi".to_owned(), value);
        self
    }

    /// If this is an `us_bank_account` PaymentMethod, this hash contains details about the US bank account payment method.
    #[must_use]
    pub fn us_bank_account(mut self, value: serde_json::Value) -> Self {
        self.params.insert("us_bank_account".to_owned(), value);
        self
    }

    /// If this is an `wechat_pay` PaymentMethod, this hash contains details about the wechat_pay payment method.
    #[must_use]
    pub fn wechat_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("wechat_pay".to_owned(), value);
        self
    }

    /// If this is a `zip` PaymentMethod, this hash contains details about the Zip payment method.
    #[must_use]
    pub fn zip(mut self, value: serde_json::Value) -> Self {
        self.params.insert("zip".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::PaymentMethod, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/payment_methods/{payment_method}`
///
/// Retrieves a PaymentMethod object attached to the StripeAccount. To retrieve a payment method attached to a Customer, you should use Retrieve a Customer’s PaymentMethods
#[derive(Debug, Clone)]
pub struct GetPaymentMethodsPaymentMethod {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetPaymentMethodsPaymentMethod {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(payment_method: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let payment_method = payment_method.into();
        Self { path: format!("/v1/payment_methods/{payment_method}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::PaymentMethod, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/payment_methods/{payment_method}`
///
/// Updates a PaymentMethod object. A PaymentMethod must be attached to a customer to be updated.
#[derive(Debug, Clone)]
pub struct PostPaymentMethodsPaymentMethod {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPaymentMethodsPaymentMethod {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(payment_method: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let payment_method = payment_method.into();
        Self { path: format!("/v1/payment_methods/{payment_method}"), params }
    }

    /// This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment meth…
    #[must_use]
    pub fn allow_redisplay(mut self, value: impl Into<String>) -> Self {
        self.params.insert("allow_redisplay".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Billing information associated with the PaymentMethod that may be used or required by particular types of payment methods.
    #[must_use]
    pub fn billing_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("billing_details".to_owned(), value);
        self
    }

    /// If this is a `card` PaymentMethod, this hash contains the user's card details.
    #[must_use]
    pub fn card(mut self, value: serde_json::Value) -> Self {
        self.params.insert("card".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// If this is a `payto` PaymentMethod, this hash contains details about the PayTo payment method.
    #[must_use]
    pub fn payto(mut self, value: serde_json::Value) -> Self {
        self.params.insert("payto".to_owned(), value);
        self
    }

    /// If this is an `us_bank_account` PaymentMethod, this hash contains details about the US bank account payment method.
    #[must_use]
    pub fn us_bank_account(mut self, value: serde_json::Value) -> Self {
        self.params.insert("us_bank_account".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::PaymentMethod, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/payment_methods/{payment_method}/attach`
///
/// Attaches a PaymentMethod object to a Customer. To attach a new PaymentMethod to a customer for future payments, we recommend you use a SetupIntent or a PaymentIntent with setup_future_usage. These approaches will perform any necessary steps to set up the PaymentMethod for future payments. Using the /v1/payment_methods/:id/attach endpoint without first using a SetupIntent or PaymentIntent with setup_future_usage does not optimize the PaymentMethod for future use, which makes later declines and p…
#[derive(Debug, Clone)]
pub struct PostPaymentMethodsPaymentMethodAttach {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPaymentMethodsPaymentMethodAttach {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(payment_method: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let payment_method = payment_method.into();
        Self { path: format!("/v1/payment_methods/{payment_method}/attach"), params }
    }

    /// The ID of the customer to which to attach the PaymentMethod.
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The ID of the Account representing the customer to which to attach the PaymentMethod.
    #[must_use]
    pub fn customer_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::PaymentMethod, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/payment_methods/{payment_method}/detach`
///
/// Detaches a PaymentMethod object from a Customer. After a PaymentMethod is detached, it can no longer be used for a payment or re-attached to a Customer.
#[derive(Debug, Clone)]
pub struct PostPaymentMethodsPaymentMethodDetach {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPaymentMethodsPaymentMethodDetach {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(payment_method: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let payment_method = payment_method.into();
        Self { path: format!("/v1/payment_methods/{payment_method}/detach"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::PaymentMethod, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
