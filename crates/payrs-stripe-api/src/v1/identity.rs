//! Generated operations for the `identity` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(clippy::doc_markdown, clippy::too_many_arguments, clippy::new_without_default, rustdoc::bare_urls)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/identity/verification_reports`
///
/// List all verification reports.
#[derive(Debug, Clone)]
pub struct GetIdentityVerificationReports {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIdentityVerificationReports {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/identity/verification_reports".to_owned(), params }
    }

    /// A string to reference this user. This can be a customer ID, a session ID, or similar, and can be used to reconcile this verification with your internal systems.
    #[must_use]
    pub fn client_reference_id(mut self, value: impl Into<String>) -> Self {
        self.params.insert("client_reference_id".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Only return VerificationReports that were created during the given date interval.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Only return VerificationReports of this type
    #[must_use]
    pub fn type_(mut self, value: impl Into<String>) -> Self {
        self.params.insert("type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Only return VerificationReports created by this VerificationSession ID. It is allowed to provide a VerificationIntent ID.
    #[must_use]
    pub fn verification_session(mut self, value: impl Into<String>) -> Self {
        self.params.insert("verification_session".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::IdentityVerificationReport> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::IdentityVerificationReport>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/identity/verification_reports/{report}`
///
/// Retrieves an existing VerificationReport
#[derive(Debug, Clone)]
pub struct GetIdentityVerificationReportsReport {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIdentityVerificationReportsReport {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(report: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let report = report.into();
        Self { path: format!("/v1/identity/verification_reports/{report}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IdentityVerificationReport, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/identity/verification_sessions`
///
/// Returns a list of VerificationSessions
#[derive(Debug, Clone)]
pub struct GetIdentityVerificationSessions {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIdentityVerificationSessions {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/identity/verification_sessions".to_owned(), params }
    }

    /// A string to reference this user. This can be a customer ID, a session ID, or similar, and can be used to reconcile this verification with your internal systems.
    #[must_use]
    pub fn client_reference_id(mut self, value: impl Into<String>) -> Self {
        self.params.insert("client_reference_id".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Only return VerificationSessions that were created during the given date interval.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Customer ID
    #[must_use]
    pub fn related_customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert("related_customer".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The ID of the Account representing a customer.
    #[must_use]
    pub fn related_customer_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("related_customer_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Only return VerificationSessions with this status. \[Learn more about the lifecycle of sessions\](https://docs.stripe.com/identity/how-sessions-work).
    #[must_use]
    pub fn status(mut self, value: impl Into<String>) -> Self {
        self.params.insert("status".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::IdentityVerificationSession> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::IdentityVerificationSession>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/identity/verification_sessions`
///
/// Creates a VerificationSession object. After the VerificationSession is created, display a verification modal using the session client_secret or send your users to the session’s url. If your API key is in test mode, verification checks won’t actually process, though everything else will occur as if in live mode. Related guide: Verify your users’ identity documents
#[derive(Debug, Clone)]
pub struct PostIdentityVerificationSessions {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostIdentityVerificationSessions {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/identity/verification_sessions".to_owned(), params }
    }

    /// A string to reference this user. This can be a customer ID, a session ID, or similar, and can be used to reconcile this verification with your internal systems.
    #[must_use]
    pub fn client_reference_id(mut self, value: impl Into<String>) -> Self {
        self.params.insert("client_reference_id".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert("metadata".to_owned(), serde_json::Value::Object(value.into_iter().map(|(k, v)| (k, serde_json::Value::String(v))).collect()));
        self
    }

    /// A set of options for the session’s verification checks.
    #[must_use]
    pub fn options(mut self, value: serde_json::Value) -> Self {
        self.params.insert("options".to_owned(), value);
        self
    }

    /// Details provided about the user being verified. These details might be shown to the user.
    #[must_use]
    pub fn provided_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("provided_details".to_owned(), value);
        self
    }

    /// Customer ID
    #[must_use]
    pub fn related_customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert("related_customer".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The ID of the Account representing a customer.
    #[must_use]
    pub fn related_customer_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("related_customer_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Tokens referencing a Person resource and its associated account.
    #[must_use]
    pub fn related_person(mut self, value: serde_json::Value) -> Self {
        self.params.insert("related_person".to_owned(), value);
        self
    }

    /// The URL that the user will be redirected to upon completing the verification flow.
    #[must_use]
    pub fn return_url(mut self, value: impl Into<String>) -> Self {
        self.params.insert("return_url".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The type of \[verification check\](https://docs.stripe.com/identity/verification-checks) to be performed. You must provide a `type` if not passing `verification_flow`.
    #[must_use]
    pub fn type_(mut self, value: impl Into<String>) -> Self {
        self.params.insert("type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The ID of a verification flow from the Dashboard. See https://docs.stripe.com/identity/verification-flows.
    #[must_use]
    pub fn verification_flow(mut self, value: impl Into<String>) -> Self {
        self.params.insert("verification_flow".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IdentityVerificationSession, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/identity/verification_sessions/{session}`
///
/// Retrieves the details of a VerificationSession that was previously created. When the session status is requires_input, you can use this method to retrieve a valid client_secret or url to allow re-submission.
#[derive(Debug, Clone)]
pub struct GetIdentityVerificationSessionsSession {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIdentityVerificationSessionsSession {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(session: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let session = session.into();
        Self { path: format!("/v1/identity/verification_sessions/{session}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IdentityVerificationSession, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/identity/verification_sessions/{session}`
///
/// Updates a VerificationSession object. When the session status is requires_input, you can use this method to update the verification check and options.
#[derive(Debug, Clone)]
pub struct PostIdentityVerificationSessionsSession {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostIdentityVerificationSessionsSession {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(session: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let session = session.into();
        Self { path: format!("/v1/identity/verification_sessions/{session}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert("metadata".to_owned(), serde_json::Value::Object(value.into_iter().map(|(k, v)| (k, serde_json::Value::String(v))).collect()));
        self
    }

    /// A set of options for the session’s verification checks.
    #[must_use]
    pub fn options(mut self, value: serde_json::Value) -> Self {
        self.params.insert("options".to_owned(), value);
        self
    }

    /// Details provided about the user being verified. These details may be shown to the user.
    #[must_use]
    pub fn provided_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("provided_details".to_owned(), value);
        self
    }

    /// The type of \[verification check\](https://docs.stripe.com/identity/verification-checks) to be performed.
    #[must_use]
    pub fn type_(mut self, value: impl Into<String>) -> Self {
        self.params.insert("type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IdentityVerificationSession, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/identity/verification_sessions/{session}/cancel`
///
/// A VerificationSession object can be canceled when it is in requires_input status. Once canceled, future submission attempts are disabled. This cannot be undone. Learn more.
#[derive(Debug, Clone)]
pub struct PostIdentityVerificationSessionsSessionCancel {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostIdentityVerificationSessionsSessionCancel {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(session: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let session = session.into();
        Self { path: format!("/v1/identity/verification_sessions/{session}/cancel"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IdentityVerificationSession, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/identity/verification_sessions/{session}/redact`
///
/// Redact a VerificationSession to remove all collected information from Stripe. This will redact the VerificationSession and all objects related to it, including VerificationReports, Events, request logs, etc. A VerificationSession object can be redacted when it is in requires_input or verified status. Redacting a VerificationSession in requires_action state will automatically cancel it. The redaction process may take up to four days. When the redaction process is in progress, the VerificationSes…
#[derive(Debug, Clone)]
pub struct PostIdentityVerificationSessionsSessionRedact {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostIdentityVerificationSessionsSessionRedact {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(session: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let session = session.into();
        Self { path: format!("/v1/identity/verification_sessions/{session}/redact"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IdentityVerificationSession, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
