//! Generated operations for the `terminal` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(
    clippy::doc_markdown,
    clippy::too_many_arguments,
    clippy::new_without_default,
    rustdoc::bare_urls
)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/terminal/configurations`
///
/// Returns a list of Configuration objects.
#[derive(Debug, Clone)]
pub struct GetTerminalConfigurations {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetTerminalConfigurations {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/terminal/configurations".to_owned(),
            params,
        }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// if present, only return the account default or non-default configurations.
    #[must_use]
    pub fn is_account_default(mut self, value: bool) -> Self {
        self.params.insert(
            "is_account_default".to_owned(),
            serde_json::Value::Bool(value),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::TerminalConfiguration> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::TerminalConfiguration>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/terminal/configurations`
///
/// Creates a new Configuration object.
#[derive(Debug, Clone)]
pub struct PostTerminalConfigurations {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTerminalConfigurations {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/terminal/configurations".to_owned(),
            params,
        }
    }

    /// An object containing device type specific settings for BBPOS WisePad 3 readers.
    #[must_use]
    pub fn bbpos_wisepad3(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bbpos_wisepad3".to_owned(), value);
        self
    }

    /// An object containing device type specific settings for BBPOS WisePOS E readers.
    #[must_use]
    pub fn bbpos_wisepos_e(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bbpos_wisepos_e".to_owned(), value);
        self
    }

    /// Configuration for cellular connectivity.
    #[must_use]
    pub fn cellular(mut self, value: serde_json::Value) -> Self {
        self.params.insert("cellular".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Name of the configuration
    #[must_use]
    pub fn name(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Configurations for collecting transactions offline.
    #[must_use]
    pub fn offline(mut self, value: serde_json::Value) -> Self {
        self.params.insert("offline".to_owned(), value);
        self
    }

    /// Reboot time settings for readers. that support customized reboot time configuration.
    #[must_use]
    pub fn reboot_window(mut self, value: serde_json::Value) -> Self {
        self.params.insert("reboot_window".to_owned(), value);
        self
    }

    /// An object containing device type specific settings for Stripe S700 readers.
    #[must_use]
    pub fn stripe_s700(mut self, value: serde_json::Value) -> Self {
        self.params.insert("stripe_s700".to_owned(), value);
        self
    }

    /// An object containing device type specific settings for Stripe S710 readers.
    #[must_use]
    pub fn stripe_s710(mut self, value: serde_json::Value) -> Self {
        self.params.insert("stripe_s710".to_owned(), value);
        self
    }

    /// Tipping configurations for readers that support on-reader tips.
    #[must_use]
    pub fn tipping(mut self, value: serde_json::Value) -> Self {
        self.params.insert("tipping".to_owned(), value);
        self
    }

    /// An object containing device type specific settings for Verifone M425 readers.
    #[must_use]
    pub fn verifone_m425(mut self, value: serde_json::Value) -> Self {
        self.params.insert("verifone_m425".to_owned(), value);
        self
    }

    /// An object containing device type specific settings for Verifone P400 readers.
    #[must_use]
    pub fn verifone_p400(mut self, value: serde_json::Value) -> Self {
        self.params.insert("verifone_p400".to_owned(), value);
        self
    }

    /// An object containing device type specific settings for Verifone P630 readers.
    #[must_use]
    pub fn verifone_p630(mut self, value: serde_json::Value) -> Self {
        self.params.insert("verifone_p630".to_owned(), value);
        self
    }

    /// An object containing device type specific settings for Verifone UX700 readers.
    #[must_use]
    pub fn verifone_ux700(mut self, value: serde_json::Value) -> Self {
        self.params.insert("verifone_ux700".to_owned(), value);
        self
    }

    /// An object containing device type specific settings for Verifone V660p readers.
    #[must_use]
    pub fn verifone_v660p(mut self, value: serde_json::Value) -> Self {
        self.params.insert("verifone_v660p".to_owned(), value);
        self
    }

    /// Configurations for connecting to a WiFi network.
    #[must_use]
    pub fn wifi(mut self, value: serde_json::Value) -> Self {
        self.params.insert("wifi".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<crate::models::TerminalConfiguration, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/terminal/configurations/{configuration}`
///
/// Retrieves a Configuration object.
#[derive(Debug, Clone)]
pub struct GetTerminalConfigurationsConfiguration {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetTerminalConfigurationsConfiguration {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(configuration: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let configuration = configuration.into();
        Self {
            path: format!("/v1/terminal/configurations/{configuration}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/terminal/configurations/{configuration}`
///
/// Updates a new Configuration object.
#[derive(Debug, Clone)]
pub struct PostTerminalConfigurationsConfiguration {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTerminalConfigurationsConfiguration {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(configuration: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let configuration = configuration.into();
        Self {
            path: format!("/v1/terminal/configurations/{configuration}"),
            params,
        }
    }

    /// An object containing device type specific settings for BBPOS WisePad 3 readers.
    #[must_use]
    pub fn bbpos_wisepad3(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bbpos_wisepad3".to_owned(), value);
        self
    }

    /// An object containing device type specific settings for BBPOS WisePOS E readers.
    #[must_use]
    pub fn bbpos_wisepos_e(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bbpos_wisepos_e".to_owned(), value);
        self
    }

    /// Configuration for cellular connectivity.
    #[must_use]
    pub fn cellular(mut self, value: serde_json::Value) -> Self {
        self.params.insert("cellular".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Name of the configuration
    #[must_use]
    pub fn name(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Configurations for collecting transactions offline.
    #[must_use]
    pub fn offline(mut self, value: serde_json::Value) -> Self {
        self.params.insert("offline".to_owned(), value);
        self
    }

    /// Reboot time settings for readers. that support customized reboot time configuration.
    #[must_use]
    pub fn reboot_window(mut self, value: serde_json::Value) -> Self {
        self.params.insert("reboot_window".to_owned(), value);
        self
    }

    /// An object containing device type specific settings for Stripe S700 readers.
    #[must_use]
    pub fn stripe_s700(mut self, value: serde_json::Value) -> Self {
        self.params.insert("stripe_s700".to_owned(), value);
        self
    }

    /// An object containing device type specific settings for Stripe S710 readers.
    #[must_use]
    pub fn stripe_s710(mut self, value: serde_json::Value) -> Self {
        self.params.insert("stripe_s710".to_owned(), value);
        self
    }

    /// Tipping configurations for readers that support on-reader tips.
    #[must_use]
    pub fn tipping(mut self, value: serde_json::Value) -> Self {
        self.params.insert("tipping".to_owned(), value);
        self
    }

    /// An object containing device type specific settings for Verifone M425 readers.
    #[must_use]
    pub fn verifone_m425(mut self, value: serde_json::Value) -> Self {
        self.params.insert("verifone_m425".to_owned(), value);
        self
    }

    /// An object containing device type specific settings for Verifone P400 readers.
    #[must_use]
    pub fn verifone_p400(mut self, value: serde_json::Value) -> Self {
        self.params.insert("verifone_p400".to_owned(), value);
        self
    }

    /// An object containing device type specific settings for Verifone P630 readers.
    #[must_use]
    pub fn verifone_p630(mut self, value: serde_json::Value) -> Self {
        self.params.insert("verifone_p630".to_owned(), value);
        self
    }

    /// An object containing device type specific settings for Verifone UX700 readers.
    #[must_use]
    pub fn verifone_ux700(mut self, value: serde_json::Value) -> Self {
        self.params.insert("verifone_ux700".to_owned(), value);
        self
    }

    /// An object containing device type specific settings for Verifone V660p readers.
    #[must_use]
    pub fn verifone_v660p(mut self, value: serde_json::Value) -> Self {
        self.params.insert("verifone_v660p".to_owned(), value);
        self
    }

    /// Configurations for connecting to a WiFi network.
    #[must_use]
    pub fn wifi(mut self, value: serde_json::Value) -> Self {
        self.params.insert("wifi".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `DELETE /v1/terminal/configurations/{configuration}`
///
/// Deletes a Configuration object.
#[derive(Debug, Clone)]
pub struct DeleteTerminalConfigurationsConfiguration {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteTerminalConfigurationsConfiguration {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(configuration: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let configuration = configuration.into();
        Self {
            path: format!("/v1/terminal/configurations/{configuration}"),
            params,
        }
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<crate::models::DeletedTerminalConfiguration, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/terminal/connection_tokens`
///
/// To connect to a reader the Stripe Terminal SDK needs to retrieve a short-lived connection token from Stripe, proxied through your server. On your backend, add an endpoint that creates and returns a connection token.
#[derive(Debug, Clone)]
pub struct PostTerminalConnectionTokens {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTerminalConnectionTokens {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/terminal/connection_tokens".to_owned(),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// The id of the location that this connection token is scoped to. If specified the connection token will only be usable with readers assigned to that location, otherwise the connection token will be usable with all readers. Note that locatio…
    #[must_use]
    pub fn location(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "location".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<crate::models::TerminalConnectionToken, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/terminal/locations`
///
/// Returns a list of Location objects.
#[derive(Debug, Clone)]
pub struct GetTerminalLocations {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetTerminalLocations {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/terminal/locations".to_owned(),
            params,
        }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::TerminalLocation> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::TerminalLocation>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/terminal/locations`
///
/// Creates a new Location object. For further details, including which address fields are required in each country, see the Manage locations guide.
#[derive(Debug, Clone)]
pub struct PostTerminalLocations {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTerminalLocations {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/terminal/locations".to_owned(),
            params,
        }
    }

    /// The full address of the location.
    #[must_use]
    pub fn address(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address".to_owned(), value);
        self
    }

    /// The Kana variation of the full address of the location (Japan only).
    #[must_use]
    pub fn address_kana(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address_kana".to_owned(), value);
        self
    }

    /// The Kanji variation of the full address of the location (Japan only).
    #[must_use]
    pub fn address_kanji(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address_kanji".to_owned(), value);
        self
    }

    /// The ID of a configuration that will be used to customize all readers in this location.
    #[must_use]
    pub fn configuration_overrides(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "configuration_overrides".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// A name for the location. Maximum length is 1000 characters.
    #[must_use]
    pub fn display_name(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "display_name".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// The Kana variation of the name for the location (Japan only). Maximum length is 1000 characters.
    #[must_use]
    pub fn display_name_kana(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "display_name_kana".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// The Kanji variation of the name for the location (Japan only). Maximum length is 1000 characters.
    #[must_use]
    pub fn display_name_kanji(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "display_name_kanji".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// The phone number for the location.
    #[must_use]
    pub fn phone(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("phone".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TerminalLocation, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/terminal/locations/{location}`
///
/// Retrieves a Location object.
#[derive(Debug, Clone)]
pub struct GetTerminalLocationsLocation {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetTerminalLocationsLocation {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(location: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let location = location.into();
        Self {
            path: format!("/v1/terminal/locations/{location}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/terminal/locations/{location}`
///
/// Updates a Location object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.
#[derive(Debug, Clone)]
pub struct PostTerminalLocationsLocation {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTerminalLocationsLocation {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(location: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let location = location.into();
        Self {
            path: format!("/v1/terminal/locations/{location}"),
            params,
        }
    }

    /// The full address of the location. You can't change the location's `country`. If you need to modify the `country` field, create a new `Location` object and re-register any existing readers to that location.
    #[must_use]
    pub fn address(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address".to_owned(), value);
        self
    }

    /// The Kana variation of the full address of the location (Japan only).
    #[must_use]
    pub fn address_kana(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address_kana".to_owned(), value);
        self
    }

    /// The Kanji variation of the full address of the location (Japan only).
    #[must_use]
    pub fn address_kanji(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address_kanji".to_owned(), value);
        self
    }

    /// The ID of a configuration that will be used to customize all readers in this location.
    #[must_use]
    pub fn configuration_overrides(mut self, value: serde_json::Value) -> Self {
        self.params
            .insert("configuration_overrides".to_owned(), value);
        self
    }

    /// A name for the location.
    #[must_use]
    pub fn display_name(mut self, value: serde_json::Value) -> Self {
        self.params.insert("display_name".to_owned(), value);
        self
    }

    /// The Kana variation of the name for the location (Japan only).
    #[must_use]
    pub fn display_name_kana(mut self, value: serde_json::Value) -> Self {
        self.params.insert("display_name_kana".to_owned(), value);
        self
    }

    /// The Kanji variation of the name for the location (Japan only).
    #[must_use]
    pub fn display_name_kanji(mut self, value: serde_json::Value) -> Self {
        self.params.insert("display_name_kanji".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// The phone number for the location.
    #[must_use]
    pub fn phone(mut self, value: serde_json::Value) -> Self {
        self.params.insert("phone".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `DELETE /v1/terminal/locations/{location}`
///
/// Deletes a Location object.
#[derive(Debug, Clone)]
pub struct DeleteTerminalLocationsLocation {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteTerminalLocationsLocation {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(location: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let location = location.into();
        Self {
            path: format!("/v1/terminal/locations/{location}"),
            params,
        }
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<crate::models::DeletedTerminalLocation, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/terminal/onboarding_links`
///
/// Creates a new OnboardingLink object that contains a redirect_url used for onboarding onto Tap to Pay on iPhone.
#[derive(Debug, Clone)]
pub struct PostTerminalOnboardingLinks {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTerminalOnboardingLinks {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(link_options: serde_json::Value, link_type: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("link_options".to_owned(), link_options);
        params.insert(
            "link_type".to_owned(),
            serde_json::Value::String(link_type.into()),
        );
        Self {
            path: "/v1/terminal/onboarding_links".to_owned(),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Stripe account ID to generate the link for.
    #[must_use]
    pub fn on_behalf_of(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "on_behalf_of".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<crate::models::TerminalOnboardingLink, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/terminal/readers`
///
/// Returns a list of Reader objects.
#[derive(Debug, Clone)]
pub struct GetTerminalReaders {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetTerminalReaders {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/terminal/readers".to_owned(),
            params,
        }
    }

    /// Filters readers by device type
    #[must_use]
    pub fn device_type(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "device_type".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A location ID to filter the response list to only readers at the specific location
    #[must_use]
    pub fn location(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "location".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Filters readers by serial number
    #[must_use]
    pub fn serial_number(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "serial_number".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// A status filter to filter readers to only offline or online readers
    #[must_use]
    pub fn status(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("status".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::TerminalReader> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::TerminalReader>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/terminal/readers`
///
/// Creates a new Reader object.
#[derive(Debug, Clone)]
pub struct PostTerminalReaders {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTerminalReaders {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(registration_code: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert(
            "registration_code".to_owned(),
            serde_json::Value::String(registration_code.into()),
        );
        Self {
            path: "/v1/terminal/readers".to_owned(),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Custom label given to the reader for easier identification. If no label is specified, the registration code will be used.
    #[must_use]
    pub fn label(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("label".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The location to assign the reader to.
    #[must_use]
    pub fn location(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "location".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TerminalReader, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/terminal/readers/{reader}`
///
/// Retrieves a Reader object.
#[derive(Debug, Clone)]
pub struct GetTerminalReadersReader {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetTerminalReadersReader {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(reader: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let reader = reader.into();
        Self {
            path: format!("/v1/terminal/readers/{reader}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/terminal/readers/{reader}`
///
/// Updates a Reader object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.
#[derive(Debug, Clone)]
pub struct PostTerminalReadersReader {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTerminalReadersReader {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(reader: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let reader = reader.into();
        Self {
            path: format!("/v1/terminal/readers/{reader}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// The new label of the reader.
    #[must_use]
    pub fn label(mut self, value: serde_json::Value) -> Self {
        self.params.insert("label".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `DELETE /v1/terminal/readers/{reader}`
///
/// Deletes a Reader object.
#[derive(Debug, Clone)]
pub struct DeleteTerminalReadersReader {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteTerminalReadersReader {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(reader: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let reader = reader.into();
        Self {
            path: format!("/v1/terminal/readers/{reader}"),
            params,
        }
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<crate::models::DeletedTerminalReader, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/terminal/readers/{reader}/cancel_action`
///
/// Cancels the current reader action. See Programmatic Cancellation for more details.
#[derive(Debug, Clone)]
pub struct PostTerminalReadersReaderCancelAction {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTerminalReadersReaderCancelAction {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(reader: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let reader = reader.into();
        Self {
            path: format!("/v1/terminal/readers/{reader}/cancel_action"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TerminalReader, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/terminal/readers/{reader}/collect_inputs`
///
/// Initiates an input collection flow on a Reader to display input forms and collect information from your customers.
#[derive(Debug, Clone)]
pub struct PostTerminalReadersReaderCollectInputs {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTerminalReadersReaderCollectInputs {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(reader: impl Into<String>, inputs: serde_json::Value) -> Self {
        let mut params = serde_json::Map::new();
        let reader = reader.into();
        params.insert("inputs".to_owned(), inputs);
        Self {
            path: format!("/v1/terminal/readers/{reader}/collect_inputs"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert(
            "metadata".to_owned(),
            serde_json::Value::Object(
                value
                    .into_iter()
                    .map(|(k, v)| (k, serde_json::Value::String(v)))
                    .collect(),
            ),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TerminalReader, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/terminal/readers/{reader}/collect_payment_method`
///
/// Initiates a payment flow on a Reader and updates the PaymentIntent with card details before manual confirmation. See Collecting a Payment method for more details.
#[derive(Debug, Clone)]
pub struct PostTerminalReadersReaderCollectPaymentMethod {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTerminalReadersReaderCollectPaymentMethod {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(reader: impl Into<String>, payment_intent: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        let reader = reader.into();
        params.insert(
            "payment_intent".to_owned(),
            serde_json::Value::String(payment_intent.into()),
        );
        Self {
            path: format!("/v1/terminal/readers/{reader}/collect_payment_method"),
            params,
        }
    }

    /// Configuration overrides for this collection, such as tipping, surcharging, and customer cancellation settings.
    #[must_use]
    pub fn collect_config(mut self, value: serde_json::Value) -> Self {
        self.params.insert("collect_config".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TerminalReader, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/terminal/readers/{reader}/confirm_payment_intent`
///
/// Finalizes a payment on a Reader. See Confirming a Payment for more details.
#[derive(Debug, Clone)]
pub struct PostTerminalReadersReaderConfirmPaymentIntent {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTerminalReadersReaderConfirmPaymentIntent {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(reader: impl Into<String>, payment_intent: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        let reader = reader.into();
        params.insert(
            "payment_intent".to_owned(),
            serde_json::Value::String(payment_intent.into()),
        );
        Self {
            path: format!("/v1/terminal/readers/{reader}/confirm_payment_intent"),
            params,
        }
    }

    /// Configuration overrides for this confirmation, such as surcharge settings and return URL.
    #[must_use]
    pub fn confirm_config(mut self, value: serde_json::Value) -> Self {
        self.params.insert("confirm_config".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TerminalReader, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/terminal/readers/{reader}/process_payment_intent`
///
/// Initiates a payment flow on a Reader. See process the payment for more details.
#[derive(Debug, Clone)]
pub struct PostTerminalReadersReaderProcessPaymentIntent {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTerminalReadersReaderProcessPaymentIntent {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(reader: impl Into<String>, payment_intent: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        let reader = reader.into();
        params.insert(
            "payment_intent".to_owned(),
            serde_json::Value::String(payment_intent.into()),
        );
        Self {
            path: format!("/v1/terminal/readers/{reader}/process_payment_intent"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Configuration overrides for this transaction, such as tipping and customer cancellation settings.
    #[must_use]
    pub fn process_config(mut self, value: serde_json::Value) -> Self {
        self.params.insert("process_config".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TerminalReader, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/terminal/readers/{reader}/process_setup_intent`
///
/// Initiates a SetupIntent flow on a Reader. See Save directly without charging for more details.
#[derive(Debug, Clone)]
pub struct PostTerminalReadersReaderProcessSetupIntent {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTerminalReadersReaderProcessSetupIntent {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(
        reader: impl Into<String>,
        allow_redisplay: impl Into<String>,
        setup_intent: impl Into<String>,
    ) -> Self {
        let mut params = serde_json::Map::new();
        let reader = reader.into();
        params.insert(
            "allow_redisplay".to_owned(),
            serde_json::Value::String(allow_redisplay.into()),
        );
        params.insert(
            "setup_intent".to_owned(),
            serde_json::Value::String(setup_intent.into()),
        );
        Self {
            path: format!("/v1/terminal/readers/{reader}/process_setup_intent"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Configuration overrides for this setup, such as MOTO and customer cancellation settings.
    #[must_use]
    pub fn process_config(mut self, value: serde_json::Value) -> Self {
        self.params.insert("process_config".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TerminalReader, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/terminal/readers/{reader}/refund_payment`
///
/// Initiates an in-person refund on a Reader. See Refund an Interac Payment for more details.
#[derive(Debug, Clone)]
pub struct PostTerminalReadersReaderRefundPayment {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTerminalReadersReaderRefundPayment {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(reader: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let reader = reader.into();
        Self {
            path: format!("/v1/terminal/readers/{reader}/refund_payment"),
            params,
        }
    }

    /// A positive integer in __cents__ representing how much of this charge to refund.
    #[must_use]
    pub fn amount(mut self, value: i64) -> Self {
        self.params
            .insert("amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// ID of the Charge to refund.
    #[must_use]
    pub fn charge(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("charge".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert(
            "metadata".to_owned(),
            serde_json::Value::Object(
                value
                    .into_iter()
                    .map(|(k, v)| (k, serde_json::Value::String(v)))
                    .collect(),
            ),
        );
        self
    }

    /// ID of the PaymentIntent to refund.
    #[must_use]
    pub fn payment_intent(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "payment_intent".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Boolean indicating whether the application fee should be refunded when refunding this charge. If a full charge refund is given, the full application fee will be refunded. Otherwise, the application fee will be refunded in an amount proport…
    #[must_use]
    pub fn refund_application_fee(mut self, value: bool) -> Self {
        self.params.insert(
            "refund_application_fee".to_owned(),
            serde_json::Value::Bool(value),
        );
        self
    }

    /// Configuration overrides for this refund, such as customer cancellation settings.
    #[must_use]
    pub fn refund_payment_config(mut self, value: serde_json::Value) -> Self {
        self.params
            .insert("refund_payment_config".to_owned(), value);
        self
    }

    /// Boolean indicating whether the transfer should be reversed when refunding this charge. The transfer will be reversed proportionally to the amount being refunded (either the entire or partial amount). A transfer can be reversed only by the…
    #[must_use]
    pub fn reverse_transfer(mut self, value: bool) -> Self {
        self.params.insert(
            "reverse_transfer".to_owned(),
            serde_json::Value::Bool(value),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TerminalReader, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/terminal/readers/{reader}/set_reader_display`
///
/// Sets the reader display to show cart details.
#[derive(Debug, Clone)]
pub struct PostTerminalReadersReaderSetReaderDisplay {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTerminalReadersReaderSetReaderDisplay {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(reader: impl Into<String>, type_: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        let reader = reader.into();
        params.insert("type".to_owned(), serde_json::Value::String(type_.into()));
        Self {
            path: format!("/v1/terminal/readers/{reader}/set_reader_display"),
            params,
        }
    }

    /// Cart details to display on the reader screen, including line items, amounts, and currency.
    #[must_use]
    pub fn cart(mut self, value: serde_json::Value) -> Self {
        self.params.insert("cart".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TerminalReader, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/terminal/refunds`
///
/// Internal endpoint for terminal use to create a refund for a card_present or card charge. You can optionally refund only part of a charge.
#[derive(Debug, Clone)]
pub struct PostTerminalRefunds {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTerminalRefunds {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/terminal/refunds".to_owned(),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TerminalRefund, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
