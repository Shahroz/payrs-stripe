//! Generated operations for the `payment_links` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(
    clippy::doc_markdown,
    clippy::too_many_arguments,
    clippy::new_without_default,
    rustdoc::bare_urls
)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/payment_links`
///
/// Returns a list of your payment links.
#[derive(Debug, Clone)]
pub struct GetPaymentLinks {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetPaymentLinks {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/payment_links".to_owned(),
            params,
        }
    }

    /// Only return payment links that are active or inactive (e.g., pass `false` to list all inactive payment links).
    #[must_use]
    pub fn active(mut self, value: bool) -> Self {
        self.params
            .insert("active".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::PaymentLink> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::PaymentLink>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/payment_links`
///
/// Creates a payment link.
#[derive(Debug, Clone)]
pub struct PostPaymentLinks {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPaymentLinks {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(line_items: serde_json::Value) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("line_items".to_owned(), line_items);
        Self {
            path: "/v1/payment_links".to_owned(),
            params,
        }
    }

    /// Behavior after the purchase is complete.
    #[must_use]
    pub fn after_completion(mut self, value: serde_json::Value) -> Self {
        self.params.insert("after_completion".to_owned(), value);
        self
    }

    /// Enables user redeemable promotion codes.
    #[must_use]
    pub fn allow_promotion_codes(mut self, value: bool) -> Self {
        self.params.insert(
            "allow_promotion_codes".to_owned(),
            serde_json::Value::Bool(value),
        );
        self
    }

    /// The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. Can only be applied when there are no line items with recurring prices.
    #[must_use]
    pub fn application_fee_amount(mut self, value: i64) -> Self {
        self.params.insert(
            "application_fee_amount".to_owned(),
            serde_json::Value::from(value),
        );
        self
    }

    /// A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the application owner's Stripe account. There must be at least 1 line i…
    #[must_use]
    pub fn application_fee_percent(mut self, value: f64) -> Self {
        self.params.insert(
            "application_fee_percent".to_owned(),
            serde_json::Value::from(value),
        );
        self
    }

    /// Configuration for automatic tax collection.
    #[must_use]
    pub fn automatic_tax(mut self, value: serde_json::Value) -> Self {
        self.params.insert("automatic_tax".to_owned(), value);
        self
    }

    /// Configuration for collecting the customer's billing address. Defaults to `auto`.
    #[must_use]
    pub fn billing_address_collection(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "billing_address_collection".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Configure fields to gather active consent from customers.
    #[must_use]
    pub fn consent_collection(mut self, value: serde_json::Value) -> Self {
        self.params.insert("consent_collection".to_owned(), value);
        self
    }

    /// Three-letter \[ISO currency code\](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a \[supported currency\](https://stripe.com/docs/currencies) and supported by each line item's price.
    #[must_use]
    pub fn currency(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "currency".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Collect additional information from your customer using custom fields. Up to 3 fields are supported. You can't set this parameter if `ui_mode` is `custom`.
    #[must_use]
    pub fn custom_fields(mut self, value: serde_json::Value) -> Self {
        self.params.insert("custom_fields".to_owned(), value);
        self
    }

    /// Display additional text for your customers using custom text. You can't set this parameter if `ui_mode` is `custom`.
    #[must_use]
    pub fn custom_text(mut self, value: serde_json::Value) -> Self {
        self.params.insert("custom_text".to_owned(), value);
        self
    }

    /// Configures whether \[checkout sessions\](https://docs.stripe.com/api/checkout/sessions) created by this payment link create a \[Customer\](https://docs.stripe.com/api/customers).
    #[must_use]
    pub fn customer_creation(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "customer_creation".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// The custom message to be displayed to a customer when a payment link is no longer active.
    #[must_use]
    pub fn inactive_message(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "inactive_message".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Generate a post-purchase Invoice for one-time payments.
    #[must_use]
    pub fn invoice_creation(mut self, value: serde_json::Value) -> Self {
        self.params.insert("invoice_creation".to_owned(), value);
        self
    }

    /// Settings for Managed Payments for this Payment Link and resulting \[CheckoutSessions\](/api/checkout/sessions/object), \[PaymentIntents\](/api/payment_intents/object), \[Invoices\](/api/invoices/object), and \[Subscriptions\](/api/subscrip…
    #[must_use]
    pub fn managed_payments(mut self, value: serde_json::Value) -> Self {
        self.params.insert("managed_payments".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert(
            "metadata".to_owned(),
            serde_json::Value::Object(
                value
                    .into_iter()
                    .map(|(k, v)| (k, serde_json::Value::String(v)))
                    .collect(),
            ),
        );
        self
    }

    /// Controls settings applied for collecting the customer's name.
    #[must_use]
    pub fn name_collection(mut self, value: serde_json::Value) -> Self {
        self.params.insert("name_collection".to_owned(), value);
        self
    }

    /// The account on behalf of which to charge.
    #[must_use]
    pub fn on_behalf_of(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "on_behalf_of".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// A list of optional items the customer can add to their order at checkout. Use this parameter to pass one-time or recurring \[Prices\](https://docs.stripe.com/api/prices). There is a maximum of 10 optional items allowed on a payment link, a…
    #[must_use]
    pub fn optional_items(mut self, value: serde_json::Value) -> Self {
        self.params.insert("optional_items".to_owned(), value);
        self
    }

    /// A subset of parameters to be passed to PaymentIntent creation for Checkout Sessions in `payment` mode.
    #[must_use]
    pub fn payment_intent_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("payment_intent_data".to_owned(), value);
        self
    }

    /// Specify whether Checkout should collect a payment method. When set to `if_required`, Checkout will not collect a payment method when the total due for the session is 0.This may occur if the Checkout Session includes a free trial or a disco…
    #[must_use]
    pub fn payment_method_collection(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "payment_method_collection".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Payment-method-specific configuration.
    #[must_use]
    pub fn payment_method_options(mut self, value: serde_json::Value) -> Self {
        self.params
            .insert("payment_method_options".to_owned(), value);
        self
    }

    /// The list of payment method types that customers can use. If no value is passed, Stripe will dynamically show relevant payment methods from your \[payment method settings\](https://dashboard.stripe.com/settings/payment_methods) (20+ payment…
    #[must_use]
    pub fn payment_method_types(mut self, value: serde_json::Value) -> Self {
        self.params.insert("payment_method_types".to_owned(), value);
        self
    }

    /// Controls phone number collection settings during checkout. We recommend that you review your privacy policy and check with your legal contacts.
    #[must_use]
    pub fn phone_number_collection(mut self, value: serde_json::Value) -> Self {
        self.params
            .insert("phone_number_collection".to_owned(), value);
        self
    }

    /// Settings that restrict the usage of a payment link.
    #[must_use]
    pub fn restrictions(mut self, value: serde_json::Value) -> Self {
        self.params.insert("restrictions".to_owned(), value);
        self
    }

    /// Configuration for collecting the customer's shipping address.
    #[must_use]
    pub fn shipping_address_collection(mut self, value: serde_json::Value) -> Self {
        self.params
            .insert("shipping_address_collection".to_owned(), value);
        self
    }

    /// The shipping rate options to apply to \[checkout sessions\](https://docs.stripe.com/api/checkout/sessions) created by this payment link.
    #[must_use]
    pub fn shipping_options(mut self, value: serde_json::Value) -> Self {
        self.params.insert("shipping_options".to_owned(), value);
        self
    }

    /// Describes the type of transaction being performed in order to customize relevant text on the page, such as the submit button. Changing this value will also affect the hostname in the \[url\](https://docs.stripe.com/api/payment_links/paymen…
    #[must_use]
    pub fn submit_type(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "submit_type".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// When creating a subscription, the specified configuration data will be used. There must be at least one line item with a recurring price to use `subscription_data`.
    #[must_use]
    pub fn subscription_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("subscription_data".to_owned(), value);
        self
    }

    /// Controls tax ID collection during checkout.
    #[must_use]
    pub fn tax_id_collection(mut self, value: serde_json::Value) -> Self {
        self.params.insert("tax_id_collection".to_owned(), value);
        self
    }

    /// The account (if any) the payments will be attributed to for tax reporting, and where funds from each payment will be transferred to.
    #[must_use]
    pub fn transfer_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("transfer_data".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::PaymentLink, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/payment_links/{payment_link}`
///
/// Retrieve a payment link.
#[derive(Debug, Clone)]
pub struct GetPaymentLinksPaymentLink {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetPaymentLinksPaymentLink {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(payment_link: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let payment_link = payment_link.into();
        Self {
            path: format!("/v1/payment_links/{payment_link}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::PaymentLink, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/payment_links/{payment_link}`
///
/// Updates a payment link.
#[derive(Debug, Clone)]
pub struct PostPaymentLinksPaymentLink {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPaymentLinksPaymentLink {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(payment_link: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let payment_link = payment_link.into();
        Self {
            path: format!("/v1/payment_links/{payment_link}"),
            params,
        }
    }

    /// Whether the payment link's `url` is active. If `false`, customers visiting the URL will be shown a page saying that the link has been deactivated.
    #[must_use]
    pub fn active(mut self, value: bool) -> Self {
        self.params
            .insert("active".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Behavior after the purchase is complete.
    #[must_use]
    pub fn after_completion(mut self, value: serde_json::Value) -> Self {
        self.params.insert("after_completion".to_owned(), value);
        self
    }

    /// Enables user redeemable promotion codes.
    #[must_use]
    pub fn allow_promotion_codes(mut self, value: bool) -> Self {
        self.params.insert(
            "allow_promotion_codes".to_owned(),
            serde_json::Value::Bool(value),
        );
        self
    }

    /// Configuration for automatic tax collection.
    #[must_use]
    pub fn automatic_tax(mut self, value: serde_json::Value) -> Self {
        self.params.insert("automatic_tax".to_owned(), value);
        self
    }

    /// Configuration for collecting the customer's billing address. Defaults to `auto`.
    #[must_use]
    pub fn billing_address_collection(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "billing_address_collection".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Collect additional information from your customer using custom fields. Up to 3 fields are supported. You can't set this parameter if `ui_mode` is `custom`.
    #[must_use]
    pub fn custom_fields(mut self, value: serde_json::Value) -> Self {
        self.params.insert("custom_fields".to_owned(), value);
        self
    }

    /// Display additional text for your customers using custom text. You can't set this parameter if `ui_mode` is `custom`.
    #[must_use]
    pub fn custom_text(mut self, value: serde_json::Value) -> Self {
        self.params.insert("custom_text".to_owned(), value);
        self
    }

    /// Configures whether \[checkout sessions\](https://docs.stripe.com/api/checkout/sessions) created by this payment link create a \[Customer\](https://docs.stripe.com/api/customers).
    #[must_use]
    pub fn customer_creation(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "customer_creation".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// The custom message to be displayed to a customer when a payment link is no longer active.
    #[must_use]
    pub fn inactive_message(mut self, value: serde_json::Value) -> Self {
        self.params.insert("inactive_message".to_owned(), value);
        self
    }

    /// Generate a post-purchase Invoice for one-time payments.
    #[must_use]
    pub fn invoice_creation(mut self, value: serde_json::Value) -> Self {
        self.params.insert("invoice_creation".to_owned(), value);
        self
    }

    /// The line items representing what is being sold. Each line item represents an item being sold. Up to 20 line items are supported.
    #[must_use]
    pub fn line_items(mut self, value: serde_json::Value) -> Self {
        self.params.insert("line_items".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert(
            "metadata".to_owned(),
            serde_json::Value::Object(
                value
                    .into_iter()
                    .map(|(k, v)| (k, serde_json::Value::String(v)))
                    .collect(),
            ),
        );
        self
    }

    /// Controls settings applied for collecting the customer's name.
    #[must_use]
    pub fn name_collection(mut self, value: serde_json::Value) -> Self {
        self.params.insert("name_collection".to_owned(), value);
        self
    }

    /// A list of optional items the customer can add to their order at checkout. Use this parameter to pass one-time or recurring \[Prices\](https://docs.stripe.com/api/prices). There is a maximum of 10 optional items allowed on a payment link, a…
    #[must_use]
    pub fn optional_items(mut self, value: serde_json::Value) -> Self {
        self.params.insert("optional_items".to_owned(), value);
        self
    }

    /// A subset of parameters to be passed to PaymentIntent creation for Checkout Sessions in `payment` mode.
    #[must_use]
    pub fn payment_intent_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("payment_intent_data".to_owned(), value);
        self
    }

    /// Specify whether Checkout should collect a payment method. When set to `if_required`, Checkout will not collect a payment method when the total due for the session is 0.This may occur if the Checkout Session includes a free trial or a disco…
    #[must_use]
    pub fn payment_method_collection(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "payment_method_collection".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Payment-method-specific configuration.
    #[must_use]
    pub fn payment_method_options(mut self, value: serde_json::Value) -> Self {
        self.params
            .insert("payment_method_options".to_owned(), value);
        self
    }

    /// The list of payment method types that customers can use. Pass an empty string to enable dynamic payment methods that use your \[payment method settings\](https://dashboard.stripe.com/settings/payment_methods).
    #[must_use]
    pub fn payment_method_types(mut self, value: serde_json::Value) -> Self {
        self.params.insert("payment_method_types".to_owned(), value);
        self
    }

    /// Controls phone number collection settings during checkout. We recommend that you review your privacy policy and check with your legal contacts.
    #[must_use]
    pub fn phone_number_collection(mut self, value: serde_json::Value) -> Self {
        self.params
            .insert("phone_number_collection".to_owned(), value);
        self
    }

    /// Settings that restrict the usage of a payment link.
    #[must_use]
    pub fn restrictions(mut self, value: serde_json::Value) -> Self {
        self.params.insert("restrictions".to_owned(), value);
        self
    }

    /// Configuration for collecting the customer's shipping address.
    #[must_use]
    pub fn shipping_address_collection(mut self, value: serde_json::Value) -> Self {
        self.params
            .insert("shipping_address_collection".to_owned(), value);
        self
    }

    /// Describes the type of transaction being performed in order to customize relevant text on the page, such as the submit button. Changing this value will also affect the hostname in the \[url\](https://docs.stripe.com/api/payment_links/paymen…
    #[must_use]
    pub fn submit_type(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "submit_type".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// When creating a subscription, the specified configuration data will be used. There must be at least one line item with a recurring price to use `subscription_data`.
    #[must_use]
    pub fn subscription_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("subscription_data".to_owned(), value);
        self
    }

    /// Controls tax ID collection during checkout.
    #[must_use]
    pub fn tax_id_collection(mut self, value: serde_json::Value) -> Self {
        self.params.insert("tax_id_collection".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::PaymentLink, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/payment_links/{payment_link}/line_items`
///
/// When retrieving a payment link, there is an includable line_items property containing the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of line items.
#[derive(Debug, Clone)]
pub struct GetPaymentLinksPaymentLinkLineItems {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetPaymentLinksPaymentLinkLineItems {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(payment_link: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let payment_link = payment_link.into();
        Self {
            path: format!("/v1/payment_links/{payment_link}/line_items"),
            params,
        }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Item> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::Item>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}
