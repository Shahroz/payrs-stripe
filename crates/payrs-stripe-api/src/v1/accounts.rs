//! Generated operations for the `accounts` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(clippy::doc_markdown, clippy::too_many_arguments, clippy::new_without_default, rustdoc::bare_urls)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/accounts`
///
/// Returns a list of accounts connected to your platform via Connect. If you’re not a platform, the list is empty.
#[derive(Debug, Clone)]
pub struct GetAccounts {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetAccounts {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/accounts".to_owned(), params }
    }

    /// Only return connected accounts that were created during the given date interval.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Account> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::Account>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/accounts`
///
/// With Connect, you can create Stripe accounts for your users. To do this, you’ll first need to register your platform. If you’ve already collected information for your connected accounts, you can prefill that information when creating the account. Connect Onboarding won’t ask for the prefilled information during account onboarding. You can prefill any information on the account.
#[derive(Debug, Clone)]
pub struct PostAccounts {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostAccounts {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/accounts".to_owned(), params }
    }

    /// An \[account token\](https://api.stripe.com#create_account_token), used to securely provide details to the account.
    #[must_use]
    pub fn account_token(mut self, value: impl Into<String>) -> Self {
        self.params.insert("account_token".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Either a token, like the ones returned by \[Stripe.js\](https://stripe.com/docs/js), or a dictionary containing a user's bank account details.
    #[must_use]
    pub fn bank_account(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bank_account".to_owned(), value);
        self
    }

    /// Business information about the account.
    #[must_use]
    pub fn business_profile(mut self, value: serde_json::Value) -> Self {
        self.params.insert("business_profile".to_owned(), value);
        self
    }

    /// The business type. Once you create an \[Account Link\](/api/account_links) or \[Account Session\](/api/account_sessions), this property can only be updated for accounts where \[controller.requirement_collection\](/api/accounts/object#accou…
    #[must_use]
    pub fn business_type(mut self, value: impl Into<String>) -> Self {
        self.params.insert("business_type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Each key of the dictionary represents a capability, and each capability maps to its settings (for example, whether it has been requested or not). Each capability is inactive until you have provided its specific requirements and Stripe has…
    #[must_use]
    pub fn capabilities(mut self, value: serde_json::Value) -> Self {
        self.params.insert("capabilities".to_owned(), value);
        self
    }

    /// Information about the company or business. This field is available for any `business_type`. Once you create an \[Account Link\](/api/account_links) or \[Account Session\](/api/account_sessions), this property can only be updated for accoun…
    #[must_use]
    pub fn company(mut self, value: serde_json::Value) -> Self {
        self.params.insert("company".to_owned(), value);
        self
    }

    /// A hash of configuration describing the account controller's attributes.
    #[must_use]
    pub fn controller(mut self, value: serde_json::Value) -> Self {
        self.params.insert("controller".to_owned(), value);
        self
    }

    /// The country in which the account holder resides, or in which the business is legally established. This should be an ISO 3166-1 alpha-2 country code. For example, if you are in the United States and the business for which you're creating an…
    #[must_use]
    pub fn country(mut self, value: impl Into<String>) -> Self {
        self.params.insert("country".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Three-letter ISO currency code representing the default currency for the account. This must be a currency that \[Stripe supports in the account's country\](https://docs.stripe.com/payouts).
    #[must_use]
    pub fn default_currency(mut self, value: impl Into<String>) -> Self {
        self.params.insert("default_currency".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Documents that may be submitted to satisfy various informational requests.
    #[must_use]
    pub fn documents(mut self, value: serde_json::Value) -> Self {
        self.params.insert("documents".to_owned(), value);
        self
    }

    /// The email address of the account holder. This is only to make the account easier to identify to you. If \[controller.requirement_collection\](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which in…
    #[must_use]
    pub fn email(mut self, value: impl Into<String>) -> Self {
        self.params.insert("email".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A card or bank account to attach to the account for receiving \[payouts\](/connect/bank-debit-card-payouts) (you won’t be able to use it for top-ups). You can provide either a token, like the ones returned by \[Stripe.js\](/js), or a dicti…
    #[must_use]
    pub fn external_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("external_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A hash of account group type to tokens. These are account groups this account should be added to.
    #[must_use]
    pub fn groups(mut self, value: serde_json::Value) -> Self {
        self.params.insert("groups".to_owned(), value);
        self
    }

    /// Information about the person represented by the account. This field is null unless `business_type` is set to `individual`. Once you create an \[Account Link\](/api/account_links) or \[Account Session\](/api/account_sessions), this property…
    #[must_use]
    pub fn individual(mut self, value: serde_json::Value) -> Self {
        self.params.insert("individual".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Options for customizing how the account functions within Stripe.
    #[must_use]
    pub fn settings(mut self, value: serde_json::Value) -> Self {
        self.params.insert("settings".to_owned(), value);
        self
    }

    /// Details on the account's acceptance of the \[Stripe Services Agreement\](/connect/updating-accounts#tos-acceptance). This property can only be updated for accounts where \[controller.requirement_collection\](/api/accounts/object#account_ob…
    #[must_use]
    pub fn tos_acceptance(mut self, value: serde_json::Value) -> Self {
        self.params.insert("tos_acceptance".to_owned(), value);
        self
    }

    /// The `type` parameter is deprecated. Use \[`controller`\](/api/accounts/create#create_account-controller) instead to configure dashboard access, fee payer, loss liability, and requirement collection.
    #[must_use]
    pub fn type_(mut self, value: impl Into<String>) -> Self {
        self.params.insert("type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Account, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/accounts/{account}`
///
/// Retrieves the details of an account.
#[derive(Debug, Clone)]
pub struct GetAccountsAccount {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetAccountsAccount {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        Self { path: format!("/v1/accounts/{account}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Account, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/accounts/{account}`
///
/// Updates a connected account by setting the values of the parameters passed. Any parameters not provided are left unchanged. For accounts where controller.requirement_collection is application, which includes Custom accounts, you can update any information on the account. For accounts where controller.requirement_collection is stripe, which includes Standard and Express accounts, you can update all information until you create an Account Link or Account Session to start Connect onboarding, after…
#[derive(Debug, Clone)]
pub struct PostAccountsAccount {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostAccountsAccount {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        Self { path: format!("/v1/accounts/{account}"), params }
    }

    /// An \[account token\](https://api.stripe.com#create_account_token), used to securely provide details to the account.
    #[must_use]
    pub fn account_token(mut self, value: impl Into<String>) -> Self {
        self.params.insert("account_token".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Business information about the account.
    #[must_use]
    pub fn business_profile(mut self, value: serde_json::Value) -> Self {
        self.params.insert("business_profile".to_owned(), value);
        self
    }

    /// The business type. Once you create an \[Account Link\](/api/account_links) or \[Account Session\](/api/account_sessions), this property can only be updated for accounts where \[controller.requirement_collection\](/api/accounts/object#accou…
    #[must_use]
    pub fn business_type(mut self, value: impl Into<String>) -> Self {
        self.params.insert("business_type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Each key of the dictionary represents a capability, and each capability maps to its settings (for example, whether it has been requested or not). Each capability is inactive until you have provided its specific requirements and Stripe has…
    #[must_use]
    pub fn capabilities(mut self, value: serde_json::Value) -> Self {
        self.params.insert("capabilities".to_owned(), value);
        self
    }

    /// Information about the company or business. This field is available for any `business_type`. Once you create an \[Account Link\](/api/account_links) or \[Account Session\](/api/account_sessions), this property can only be updated for accoun…
    #[must_use]
    pub fn company(mut self, value: serde_json::Value) -> Self {
        self.params.insert("company".to_owned(), value);
        self
    }

    /// Three-letter ISO currency code representing the default currency for the account. This must be a currency that \[Stripe supports in the account's country\](https://docs.stripe.com/payouts).
    #[must_use]
    pub fn default_currency(mut self, value: impl Into<String>) -> Self {
        self.params.insert("default_currency".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Documents that may be submitted to satisfy various informational requests.
    #[must_use]
    pub fn documents(mut self, value: serde_json::Value) -> Self {
        self.params.insert("documents".to_owned(), value);
        self
    }

    /// The email address of the account holder. This is only to make the account easier to identify to you. If \[controller.requirement_collection\](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which in…
    #[must_use]
    pub fn email(mut self, value: impl Into<String>) -> Self {
        self.params.insert("email".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A card or bank account to attach to the account for receiving \[payouts\](/connect/bank-debit-card-payouts) (you won’t be able to use it for top-ups). You can provide either a token, like the ones returned by \[Stripe.js\](/js), or a dicti…
    #[must_use]
    pub fn external_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("external_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A hash of account group type to tokens. These are account groups this account should be added to.
    #[must_use]
    pub fn groups(mut self, value: serde_json::Value) -> Self {
        self.params.insert("groups".to_owned(), value);
        self
    }

    /// Information about the person represented by the account. This field is null unless `business_type` is set to `individual`. Once you create an \[Account Link\](/api/account_links) or \[Account Session\](/api/account_sessions), this property…
    #[must_use]
    pub fn individual(mut self, value: serde_json::Value) -> Self {
        self.params.insert("individual".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Options for customizing how the account functions within Stripe.
    #[must_use]
    pub fn settings(mut self, value: serde_json::Value) -> Self {
        self.params.insert("settings".to_owned(), value);
        self
    }

    /// Details on the account's acceptance of the \[Stripe Services Agreement\](/connect/updating-accounts#tos-acceptance). This property can only be updated for accounts where \[controller.requirement_collection\](/api/accounts/object#account_ob…
    #[must_use]
    pub fn tos_acceptance(mut self, value: serde_json::Value) -> Self {
        self.params.insert("tos_acceptance".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Account, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `DELETE /v1/accounts/{account}`
///
/// With Connect, you can delete accounts you manage. Test-mode accounts can be deleted at any time. Live-mode accounts that have access to the standard dashboard and Stripe is responsible for negative account balances cannot be deleted, which includes Standard accounts. All other Live-mode accounts, can be deleted when all balances are zero. If you want to delete your own account, use the account information tab in your account settings instead.
#[derive(Debug, Clone)]
pub struct DeleteAccountsAccount {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteAccountsAccount {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        Self { path: format!("/v1/accounts/{account}"), params }
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::DeletedAccount, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/accounts/{account}/bank_accounts`
///
/// Create an external account for a given account.
#[derive(Debug, Clone)]
pub struct PostAccountsAccountBankAccounts {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostAccountsAccountBankAccounts {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        Self { path: format!("/v1/accounts/{account}/bank_accounts"), params }
    }

    /// Either a token, like the ones returned by \[Stripe.js\](https://stripe.com/docs/js), or a dictionary containing a user's bank account details.
    #[must_use]
    pub fn bank_account(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bank_account".to_owned(), value);
        self
    }

    /// When set to true, or if this is the first external account added in this currency, this account becomes the default external account for its currency.
    #[must_use]
    pub fn default_for_currency(mut self, value: bool) -> Self {
        self.params.insert("default_for_currency".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A token, like the ones returned by \[Stripe.js\](https://docs.stripe.com/js) or a dictionary containing a user's external account details (with the options shown below). Please refer to full \[documentation\](https://stripe.com/docs/api/ex…
    #[must_use]
    pub fn external_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("external_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert("metadata".to_owned(), serde_json::Value::Object(value.into_iter().map(|(k, v)| (k, serde_json::Value::String(v))).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/accounts/{account}/bank_accounts/{id}`
///
/// Retrieve a specified external account for a given account.
#[derive(Debug, Clone)]
pub struct GetAccountsAccountBankAccountsId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetAccountsAccountBankAccountsId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>, id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        let id = id.into();
        Self { path: format!("/v1/accounts/{account}/bank_accounts/{id}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/accounts/{account}/bank_accounts/{id}`
///
/// Updates the metadata, account holder name, account holder type of a bank account belonging to a connected account and optionally sets it as the default for its currency. Other bank account details are not editable by design. You can only update bank accounts when account.controller.requirement_collection is application, which includes Custom accounts. You can re-enable a disabled bank account by performing an update call without providing any arguments or changes.
#[derive(Debug, Clone)]
pub struct PostAccountsAccountBankAccountsId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostAccountsAccountBankAccountsId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>, id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        let id = id.into();
        Self { path: format!("/v1/accounts/{account}/bank_accounts/{id}"), params }
    }

    /// The name of the person or business that owns the bank account.
    #[must_use]
    pub fn account_holder_name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("account_holder_name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The type of entity that holds the account. This can be either `individual` or `company`.
    #[must_use]
    pub fn account_holder_type(mut self, value: impl Into<String>) -> Self {
        self.params.insert("account_holder_type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The bank account type. This can only be `checking` or `savings` in most countries. In Japan, this can only be `futsu` or `toza`.
    #[must_use]
    pub fn account_type(mut self, value: impl Into<String>) -> Self {
        self.params.insert("account_type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// City/District/Suburb/Town/Village.
    #[must_use]
    pub fn address_city(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_city".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Billing address country, if provided when creating card.
    #[must_use]
    pub fn address_country(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_country".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Address line 1 (Street address/PO Box/Company name).
    #[must_use]
    pub fn address_line1(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_line1".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Address line 2 (Apartment/Suite/Unit/Building).
    #[must_use]
    pub fn address_line2(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_line2".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// State/County/Province/Region.
    #[must_use]
    pub fn address_state(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_state".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// ZIP or postal code.
    #[must_use]
    pub fn address_zip(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_zip".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// When set to true, this becomes the default external account for its currency.
    #[must_use]
    pub fn default_for_currency(mut self, value: bool) -> Self {
        self.params.insert("default_for_currency".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Documents that may be submitted to satisfy various informational requests.
    #[must_use]
    pub fn documents(mut self, value: serde_json::Value) -> Self {
        self.params.insert("documents".to_owned(), value);
        self
    }

    /// Two digit number representing the card’s expiration month.
    #[must_use]
    pub fn exp_month(mut self, value: impl Into<String>) -> Self {
        self.params.insert("exp_month".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Four digit number representing the card’s expiration year.
    #[must_use]
    pub fn exp_year(mut self, value: impl Into<String>) -> Self {
        self.params.insert("exp_year".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Cardholder name.
    #[must_use]
    pub fn name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `DELETE /v1/accounts/{account}/bank_accounts/{id}`
///
/// Delete a specified external account for a given account.
#[derive(Debug, Clone)]
pub struct DeleteAccountsAccountBankAccountsId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteAccountsAccountBankAccountsId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>, id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        let id = id.into();
        Self { path: format!("/v1/accounts/{account}/bank_accounts/{id}"), params }
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/accounts/{account}/capabilities`
///
/// Returns a list of capabilities associated with the account. The capabilities are returned sorted by creation date, with the most recent capability appearing first.
#[derive(Debug, Clone)]
pub struct GetAccountsAccountCapabilities {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetAccountsAccountCapabilities {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        Self { path: format!("/v1/accounts/{account}/capabilities"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Capability> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::Capability>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/accounts/{account}/capabilities/{capability}`
///
/// Retrieves information about the specified Account Capability.
#[derive(Debug, Clone)]
pub struct GetAccountsAccountCapabilitiesCapability {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetAccountsAccountCapabilitiesCapability {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>, capability: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        let capability = capability.into();
        Self { path: format!("/v1/accounts/{account}/capabilities/{capability}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Capability, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/accounts/{account}/capabilities/{capability}`
///
/// Updates an existing Account Capability. Request or remove a capability by updating its requested parameter.
#[derive(Debug, Clone)]
pub struct PostAccountsAccountCapabilitiesCapability {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostAccountsAccountCapabilitiesCapability {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>, capability: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        let capability = capability.into();
        Self { path: format!("/v1/accounts/{account}/capabilities/{capability}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// To request a new capability for an account, pass true. There can be a delay before the requested capability becomes active. If the capability has any activation requirements, the response includes them in the `requirements` arrays. If a ca…
    #[must_use]
    pub fn requested(mut self, value: bool) -> Self {
        self.params.insert("requested".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Capability, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/accounts/{account}/external_accounts`
///
/// List external accounts for an account.
#[derive(Debug, Clone)]
pub struct GetAccountsAccountExternalAccounts {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetAccountsAccountExternalAccounts {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        Self { path: format!("/v1/accounts/{account}/external_accounts"), params }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Filter external accounts according to a particular object type.
    #[must_use]
    pub fn object(mut self, value: impl Into<String>) -> Self {
        self.params.insert("object".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/accounts/{account}/external_accounts`
///
/// Create an external account for a given account.
#[derive(Debug, Clone)]
pub struct PostAccountsAccountExternalAccounts {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostAccountsAccountExternalAccounts {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        Self { path: format!("/v1/accounts/{account}/external_accounts"), params }
    }

    /// Either a token, like the ones returned by \[Stripe.js\](https://stripe.com/docs/js), or a dictionary containing a user's bank account details.
    #[must_use]
    pub fn bank_account(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bank_account".to_owned(), value);
        self
    }

    /// When set to true, or if this is the first external account added in this currency, this account becomes the default external account for its currency.
    #[must_use]
    pub fn default_for_currency(mut self, value: bool) -> Self {
        self.params.insert("default_for_currency".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A token, like the ones returned by \[Stripe.js\](https://docs.stripe.com/js) or a dictionary containing a user's external account details (with the options shown below). Please refer to full \[documentation\](https://stripe.com/docs/api/ex…
    #[must_use]
    pub fn external_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("external_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert("metadata".to_owned(), serde_json::Value::Object(value.into_iter().map(|(k, v)| (k, serde_json::Value::String(v))).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/accounts/{account}/external_accounts/{id}`
///
/// Retrieve a specified external account for a given account.
#[derive(Debug, Clone)]
pub struct GetAccountsAccountExternalAccountsId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetAccountsAccountExternalAccountsId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>, id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        let id = id.into();
        Self { path: format!("/v1/accounts/{account}/external_accounts/{id}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/accounts/{account}/external_accounts/{id}`
///
/// Updates the metadata, account holder name, account holder type of a bank account belonging to a connected account and optionally sets it as the default for its currency. Other bank account details are not editable by design. You can only update bank accounts when account.controller.requirement_collection is application, which includes Custom accounts. You can re-enable a disabled bank account by performing an update call without providing any arguments or changes.
#[derive(Debug, Clone)]
pub struct PostAccountsAccountExternalAccountsId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostAccountsAccountExternalAccountsId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>, id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        let id = id.into();
        Self { path: format!("/v1/accounts/{account}/external_accounts/{id}"), params }
    }

    /// The name of the person or business that owns the bank account.
    #[must_use]
    pub fn account_holder_name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("account_holder_name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The type of entity that holds the account. This can be either `individual` or `company`.
    #[must_use]
    pub fn account_holder_type(mut self, value: impl Into<String>) -> Self {
        self.params.insert("account_holder_type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The bank account type. This can only be `checking` or `savings` in most countries. In Japan, this can only be `futsu` or `toza`.
    #[must_use]
    pub fn account_type(mut self, value: impl Into<String>) -> Self {
        self.params.insert("account_type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// City/District/Suburb/Town/Village.
    #[must_use]
    pub fn address_city(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_city".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Billing address country, if provided when creating card.
    #[must_use]
    pub fn address_country(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_country".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Address line 1 (Street address/PO Box/Company name).
    #[must_use]
    pub fn address_line1(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_line1".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Address line 2 (Apartment/Suite/Unit/Building).
    #[must_use]
    pub fn address_line2(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_line2".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// State/County/Province/Region.
    #[must_use]
    pub fn address_state(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_state".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// ZIP or postal code.
    #[must_use]
    pub fn address_zip(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_zip".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// When set to true, this becomes the default external account for its currency.
    #[must_use]
    pub fn default_for_currency(mut self, value: bool) -> Self {
        self.params.insert("default_for_currency".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Documents that may be submitted to satisfy various informational requests.
    #[must_use]
    pub fn documents(mut self, value: serde_json::Value) -> Self {
        self.params.insert("documents".to_owned(), value);
        self
    }

    /// Two digit number representing the card’s expiration month.
    #[must_use]
    pub fn exp_month(mut self, value: impl Into<String>) -> Self {
        self.params.insert("exp_month".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Four digit number representing the card’s expiration year.
    #[must_use]
    pub fn exp_year(mut self, value: impl Into<String>) -> Self {
        self.params.insert("exp_year".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Cardholder name.
    #[must_use]
    pub fn name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `DELETE /v1/accounts/{account}/external_accounts/{id}`
///
/// Delete a specified external account for a given account.
#[derive(Debug, Clone)]
pub struct DeleteAccountsAccountExternalAccountsId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteAccountsAccountExternalAccountsId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>, id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        let id = id.into();
        Self { path: format!("/v1/accounts/{account}/external_accounts/{id}"), params }
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/accounts/{account}/login_links`
///
/// Creates a login link for a connected account to access the Express Dashboard. You can only create login links for accounts that use the Express Dashboard and are connected to your platform.
#[derive(Debug, Clone)]
pub struct PostAccountsAccountLoginLinks {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostAccountsAccountLoginLinks {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        Self { path: format!("/v1/accounts/{account}/login_links"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::LoginLink, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/accounts/{account}/people`
///
/// Returns a list of people associated with the account’s legal entity. The people are returned sorted by creation date, with the most recent people appearing first.
#[derive(Debug, Clone)]
pub struct GetAccountsAccountPeople {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetAccountsAccountPeople {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        Self { path: format!("/v1/accounts/{account}/people"), params }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Filters on the list of people returned based on the person's relationship to the account's company.
    #[must_use]
    pub fn relationship(mut self, value: serde_json::Value) -> Self {
        self.params.insert("relationship".to_owned(), value);
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Person> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::Person>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/accounts/{account}/people`
///
/// Creates a new person.
#[derive(Debug, Clone)]
pub struct PostAccountsAccountPeople {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostAccountsAccountPeople {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        Self { path: format!("/v1/accounts/{account}/people"), params }
    }

    /// Details on the legal guardian's or authorizer's acceptance of the required Stripe agreements.
    #[must_use]
    pub fn additional_tos_acceptances(mut self, value: serde_json::Value) -> Self {
        self.params.insert("additional_tos_acceptances".to_owned(), value);
        self
    }

    /// The person's address.
    #[must_use]
    pub fn address(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address".to_owned(), value);
        self
    }

    /// The Kana variation of the person's address (Japan only).
    #[must_use]
    pub fn address_kana(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address_kana".to_owned(), value);
        self
    }

    /// The Kanji variation of the person's address (Japan only).
    #[must_use]
    pub fn address_kanji(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address_kanji".to_owned(), value);
        self
    }

    /// The person's date of birth.
    #[must_use]
    pub fn dob(mut self, value: serde_json::Value) -> Self {
        self.params.insert("dob".to_owned(), value);
        self
    }

    /// Documents that may be submitted to satisfy various informational requests.
    #[must_use]
    pub fn documents(mut self, value: serde_json::Value) -> Self {
        self.params.insert("documents".to_owned(), value);
        self
    }

    /// The person's email address.
    #[must_use]
    pub fn email(mut self, value: impl Into<String>) -> Self {
        self.params.insert("email".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// The person's first name.
    #[must_use]
    pub fn first_name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("first_name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The Kana variation of the person's first name (Japan only).
    #[must_use]
    pub fn first_name_kana(mut self, value: impl Into<String>) -> Self {
        self.params.insert("first_name_kana".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The Kanji variation of the person's first name (Japan only).
    #[must_use]
    pub fn first_name_kanji(mut self, value: impl Into<String>) -> Self {
        self.params.insert("first_name_kanji".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A list of alternate names or aliases that the person is known by.
    #[must_use]
    pub fn full_name_aliases(mut self, value: serde_json::Value) -> Self {
        self.params.insert("full_name_aliases".to_owned(), value);
        self
    }

    /// The person's gender (International regulations require either "male" or "female").
    #[must_use]
    pub fn gender(mut self, value: impl Into<String>) -> Self {
        self.params.insert("gender".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's ID number, as appropriate for their country. For example, a social security number in the U.S., social insurance number in Canada, etc. Instead of the number itself, you can also provide a \[PII token provided by Stripe.js\](h…
    #[must_use]
    pub fn id_number(mut self, value: impl Into<String>) -> Self {
        self.params.insert("id_number".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's secondary ID number, as appropriate for their country, will be used for enhanced verification checks. In Thailand, this would be the laser code found on the back of an ID card. Instead of the number itself, you can also provid…
    #[must_use]
    pub fn id_number_secondary(mut self, value: impl Into<String>) -> Self {
        self.params.insert("id_number_secondary".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's last name.
    #[must_use]
    pub fn last_name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("last_name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The Kana variation of the person's last name (Japan only).
    #[must_use]
    pub fn last_name_kana(mut self, value: impl Into<String>) -> Self {
        self.params.insert("last_name_kana".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The Kanji variation of the person's last name (Japan only).
    #[must_use]
    pub fn last_name_kanji(mut self, value: impl Into<String>) -> Self {
        self.params.insert("last_name_kanji".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's maiden name.
    #[must_use]
    pub fn maiden_name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("maiden_name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// The country where the person is a national. Two-letter country code (\[ISO 3166-1 alpha-2\](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)), or "XX" if unavailable.
    #[must_use]
    pub fn nationality(mut self, value: impl Into<String>) -> Self {
        self.params.insert("nationality".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A \[person token\](https://docs.stripe.com/connect/account-tokens), used to securely provide details to the person.
    #[must_use]
    pub fn person_token(mut self, value: impl Into<String>) -> Self {
        self.params.insert("person_token".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's phone number.
    #[must_use]
    pub fn phone(mut self, value: impl Into<String>) -> Self {
        self.params.insert("phone".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Indicates if the person or any of their representatives, family members, or other closely related persons, declares that they hold or have held an important public job or function, in any jurisdiction.
    #[must_use]
    pub fn political_exposure(mut self, value: impl Into<String>) -> Self {
        self.params.insert("political_exposure".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's registered address.
    #[must_use]
    pub fn registered_address(mut self, value: serde_json::Value) -> Self {
        self.params.insert("registered_address".to_owned(), value);
        self
    }

    /// The relationship that this person has with the account's legal entity.
    #[must_use]
    pub fn relationship(mut self, value: serde_json::Value) -> Self {
        self.params.insert("relationship".to_owned(), value);
        self
    }

    /// The last four digits of the person's Social Security number (U.S. only).
    #[must_use]
    pub fn ssn_last_4(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ssn_last_4".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Demographic data related to the person.
    #[must_use]
    pub fn us_cfpb_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("us_cfpb_data".to_owned(), value);
        self
    }

    /// The person's verification status.
    #[must_use]
    pub fn verification(mut self, value: serde_json::Value) -> Self {
        self.params.insert("verification".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Person, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/accounts/{account}/people/{person}`
///
/// Retrieves an existing person.
#[derive(Debug, Clone)]
pub struct GetAccountsAccountPeoplePerson {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetAccountsAccountPeoplePerson {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>, person: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        let person = person.into();
        Self { path: format!("/v1/accounts/{account}/people/{person}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Person, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/accounts/{account}/people/{person}`
///
/// Updates an existing person.
#[derive(Debug, Clone)]
pub struct PostAccountsAccountPeoplePerson {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostAccountsAccountPeoplePerson {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>, person: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        let person = person.into();
        Self { path: format!("/v1/accounts/{account}/people/{person}"), params }
    }

    /// Details on the legal guardian's or authorizer's acceptance of the required Stripe agreements.
    #[must_use]
    pub fn additional_tos_acceptances(mut self, value: serde_json::Value) -> Self {
        self.params.insert("additional_tos_acceptances".to_owned(), value);
        self
    }

    /// The person's address.
    #[must_use]
    pub fn address(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address".to_owned(), value);
        self
    }

    /// The Kana variation of the person's address (Japan only).
    #[must_use]
    pub fn address_kana(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address_kana".to_owned(), value);
        self
    }

    /// The Kanji variation of the person's address (Japan only).
    #[must_use]
    pub fn address_kanji(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address_kanji".to_owned(), value);
        self
    }

    /// The person's date of birth.
    #[must_use]
    pub fn dob(mut self, value: serde_json::Value) -> Self {
        self.params.insert("dob".to_owned(), value);
        self
    }

    /// Documents that may be submitted to satisfy various informational requests.
    #[must_use]
    pub fn documents(mut self, value: serde_json::Value) -> Self {
        self.params.insert("documents".to_owned(), value);
        self
    }

    /// The person's email address.
    #[must_use]
    pub fn email(mut self, value: impl Into<String>) -> Self {
        self.params.insert("email".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// The person's first name.
    #[must_use]
    pub fn first_name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("first_name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The Kana variation of the person's first name (Japan only).
    #[must_use]
    pub fn first_name_kana(mut self, value: impl Into<String>) -> Self {
        self.params.insert("first_name_kana".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The Kanji variation of the person's first name (Japan only).
    #[must_use]
    pub fn first_name_kanji(mut self, value: impl Into<String>) -> Self {
        self.params.insert("first_name_kanji".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A list of alternate names or aliases that the person is known by.
    #[must_use]
    pub fn full_name_aliases(mut self, value: serde_json::Value) -> Self {
        self.params.insert("full_name_aliases".to_owned(), value);
        self
    }

    /// The person's gender (International regulations require either "male" or "female").
    #[must_use]
    pub fn gender(mut self, value: impl Into<String>) -> Self {
        self.params.insert("gender".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's ID number, as appropriate for their country. For example, a social security number in the U.S., social insurance number in Canada, etc. Instead of the number itself, you can also provide a \[PII token provided by Stripe.js\](h…
    #[must_use]
    pub fn id_number(mut self, value: impl Into<String>) -> Self {
        self.params.insert("id_number".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's secondary ID number, as appropriate for their country, will be used for enhanced verification checks. In Thailand, this would be the laser code found on the back of an ID card. Instead of the number itself, you can also provid…
    #[must_use]
    pub fn id_number_secondary(mut self, value: impl Into<String>) -> Self {
        self.params.insert("id_number_secondary".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's last name.
    #[must_use]
    pub fn last_name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("last_name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The Kana variation of the person's last name (Japan only).
    #[must_use]
    pub fn last_name_kana(mut self, value: impl Into<String>) -> Self {
        self.params.insert("last_name_kana".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The Kanji variation of the person's last name (Japan only).
    #[must_use]
    pub fn last_name_kanji(mut self, value: impl Into<String>) -> Self {
        self.params.insert("last_name_kanji".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's maiden name.
    #[must_use]
    pub fn maiden_name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("maiden_name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// The country where the person is a national. Two-letter country code (\[ISO 3166-1 alpha-2\](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)), or "XX" if unavailable.
    #[must_use]
    pub fn nationality(mut self, value: impl Into<String>) -> Self {
        self.params.insert("nationality".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A \[person token\](https://docs.stripe.com/connect/account-tokens), used to securely provide details to the person.
    #[must_use]
    pub fn person_token(mut self, value: impl Into<String>) -> Self {
        self.params.insert("person_token".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's phone number.
    #[must_use]
    pub fn phone(mut self, value: impl Into<String>) -> Self {
        self.params.insert("phone".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Indicates if the person or any of their representatives, family members, or other closely related persons, declares that they hold or have held an important public job or function, in any jurisdiction.
    #[must_use]
    pub fn political_exposure(mut self, value: impl Into<String>) -> Self {
        self.params.insert("political_exposure".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's registered address.
    #[must_use]
    pub fn registered_address(mut self, value: serde_json::Value) -> Self {
        self.params.insert("registered_address".to_owned(), value);
        self
    }

    /// The relationship that this person has with the account's legal entity.
    #[must_use]
    pub fn relationship(mut self, value: serde_json::Value) -> Self {
        self.params.insert("relationship".to_owned(), value);
        self
    }

    /// The last four digits of the person's Social Security number (U.S. only).
    #[must_use]
    pub fn ssn_last_4(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ssn_last_4".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Demographic data related to the person.
    #[must_use]
    pub fn us_cfpb_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("us_cfpb_data".to_owned(), value);
        self
    }

    /// The person's verification status.
    #[must_use]
    pub fn verification(mut self, value: serde_json::Value) -> Self {
        self.params.insert("verification".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Person, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `DELETE /v1/accounts/{account}/people/{person}`
///
/// Deletes an existing person’s relationship to the account’s legal entity. Any person with a relationship for an account can be deleted through the API, except if the person is the account_opener. If your integration is using the executive parameter, you cannot delete the only verified executive on file.
#[derive(Debug, Clone)]
pub struct DeleteAccountsAccountPeoplePerson {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteAccountsAccountPeoplePerson {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>, person: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        let person = person.into();
        Self { path: format!("/v1/accounts/{account}/people/{person}"), params }
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::DeletedPerson, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/accounts/{account}/persons`
///
/// Returns a list of people associated with the account’s legal entity. The people are returned sorted by creation date, with the most recent people appearing first.
#[derive(Debug, Clone)]
pub struct GetAccountsAccountPersons {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetAccountsAccountPersons {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        Self { path: format!("/v1/accounts/{account}/persons"), params }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Filters on the list of people returned based on the person's relationship to the account's company.
    #[must_use]
    pub fn relationship(mut self, value: serde_json::Value) -> Self {
        self.params.insert("relationship".to_owned(), value);
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Person> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::Person>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/accounts/{account}/persons`
///
/// Creates a new person.
#[derive(Debug, Clone)]
pub struct PostAccountsAccountPersons {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostAccountsAccountPersons {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        Self { path: format!("/v1/accounts/{account}/persons"), params }
    }

    /// Details on the legal guardian's or authorizer's acceptance of the required Stripe agreements.
    #[must_use]
    pub fn additional_tos_acceptances(mut self, value: serde_json::Value) -> Self {
        self.params.insert("additional_tos_acceptances".to_owned(), value);
        self
    }

    /// The person's address.
    #[must_use]
    pub fn address(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address".to_owned(), value);
        self
    }

    /// The Kana variation of the person's address (Japan only).
    #[must_use]
    pub fn address_kana(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address_kana".to_owned(), value);
        self
    }

    /// The Kanji variation of the person's address (Japan only).
    #[must_use]
    pub fn address_kanji(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address_kanji".to_owned(), value);
        self
    }

    /// The person's date of birth.
    #[must_use]
    pub fn dob(mut self, value: serde_json::Value) -> Self {
        self.params.insert("dob".to_owned(), value);
        self
    }

    /// Documents that may be submitted to satisfy various informational requests.
    #[must_use]
    pub fn documents(mut self, value: serde_json::Value) -> Self {
        self.params.insert("documents".to_owned(), value);
        self
    }

    /// The person's email address.
    #[must_use]
    pub fn email(mut self, value: impl Into<String>) -> Self {
        self.params.insert("email".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// The person's first name.
    #[must_use]
    pub fn first_name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("first_name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The Kana variation of the person's first name (Japan only).
    #[must_use]
    pub fn first_name_kana(mut self, value: impl Into<String>) -> Self {
        self.params.insert("first_name_kana".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The Kanji variation of the person's first name (Japan only).
    #[must_use]
    pub fn first_name_kanji(mut self, value: impl Into<String>) -> Self {
        self.params.insert("first_name_kanji".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A list of alternate names or aliases that the person is known by.
    #[must_use]
    pub fn full_name_aliases(mut self, value: serde_json::Value) -> Self {
        self.params.insert("full_name_aliases".to_owned(), value);
        self
    }

    /// The person's gender (International regulations require either "male" or "female").
    #[must_use]
    pub fn gender(mut self, value: impl Into<String>) -> Self {
        self.params.insert("gender".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's ID number, as appropriate for their country. For example, a social security number in the U.S., social insurance number in Canada, etc. Instead of the number itself, you can also provide a \[PII token provided by Stripe.js\](h…
    #[must_use]
    pub fn id_number(mut self, value: impl Into<String>) -> Self {
        self.params.insert("id_number".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's secondary ID number, as appropriate for their country, will be used for enhanced verification checks. In Thailand, this would be the laser code found on the back of an ID card. Instead of the number itself, you can also provid…
    #[must_use]
    pub fn id_number_secondary(mut self, value: impl Into<String>) -> Self {
        self.params.insert("id_number_secondary".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's last name.
    #[must_use]
    pub fn last_name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("last_name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The Kana variation of the person's last name (Japan only).
    #[must_use]
    pub fn last_name_kana(mut self, value: impl Into<String>) -> Self {
        self.params.insert("last_name_kana".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The Kanji variation of the person's last name (Japan only).
    #[must_use]
    pub fn last_name_kanji(mut self, value: impl Into<String>) -> Self {
        self.params.insert("last_name_kanji".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's maiden name.
    #[must_use]
    pub fn maiden_name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("maiden_name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// The country where the person is a national. Two-letter country code (\[ISO 3166-1 alpha-2\](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)), or "XX" if unavailable.
    #[must_use]
    pub fn nationality(mut self, value: impl Into<String>) -> Self {
        self.params.insert("nationality".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A \[person token\](https://docs.stripe.com/connect/account-tokens), used to securely provide details to the person.
    #[must_use]
    pub fn person_token(mut self, value: impl Into<String>) -> Self {
        self.params.insert("person_token".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's phone number.
    #[must_use]
    pub fn phone(mut self, value: impl Into<String>) -> Self {
        self.params.insert("phone".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Indicates if the person or any of their representatives, family members, or other closely related persons, declares that they hold or have held an important public job or function, in any jurisdiction.
    #[must_use]
    pub fn political_exposure(mut self, value: impl Into<String>) -> Self {
        self.params.insert("political_exposure".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's registered address.
    #[must_use]
    pub fn registered_address(mut self, value: serde_json::Value) -> Self {
        self.params.insert("registered_address".to_owned(), value);
        self
    }

    /// The relationship that this person has with the account's legal entity.
    #[must_use]
    pub fn relationship(mut self, value: serde_json::Value) -> Self {
        self.params.insert("relationship".to_owned(), value);
        self
    }

    /// The last four digits of the person's Social Security number (U.S. only).
    #[must_use]
    pub fn ssn_last_4(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ssn_last_4".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Demographic data related to the person.
    #[must_use]
    pub fn us_cfpb_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("us_cfpb_data".to_owned(), value);
        self
    }

    /// The person's verification status.
    #[must_use]
    pub fn verification(mut self, value: serde_json::Value) -> Self {
        self.params.insert("verification".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Person, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/accounts/{account}/persons/{person}`
///
/// Retrieves an existing person.
#[derive(Debug, Clone)]
pub struct GetAccountsAccountPersonsPerson {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetAccountsAccountPersonsPerson {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>, person: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        let person = person.into();
        Self { path: format!("/v1/accounts/{account}/persons/{person}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Person, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/accounts/{account}/persons/{person}`
///
/// Updates an existing person.
#[derive(Debug, Clone)]
pub struct PostAccountsAccountPersonsPerson {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostAccountsAccountPersonsPerson {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>, person: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        let person = person.into();
        Self { path: format!("/v1/accounts/{account}/persons/{person}"), params }
    }

    /// Details on the legal guardian's or authorizer's acceptance of the required Stripe agreements.
    #[must_use]
    pub fn additional_tos_acceptances(mut self, value: serde_json::Value) -> Self {
        self.params.insert("additional_tos_acceptances".to_owned(), value);
        self
    }

    /// The person's address.
    #[must_use]
    pub fn address(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address".to_owned(), value);
        self
    }

    /// The Kana variation of the person's address (Japan only).
    #[must_use]
    pub fn address_kana(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address_kana".to_owned(), value);
        self
    }

    /// The Kanji variation of the person's address (Japan only).
    #[must_use]
    pub fn address_kanji(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address_kanji".to_owned(), value);
        self
    }

    /// The person's date of birth.
    #[must_use]
    pub fn dob(mut self, value: serde_json::Value) -> Self {
        self.params.insert("dob".to_owned(), value);
        self
    }

    /// Documents that may be submitted to satisfy various informational requests.
    #[must_use]
    pub fn documents(mut self, value: serde_json::Value) -> Self {
        self.params.insert("documents".to_owned(), value);
        self
    }

    /// The person's email address.
    #[must_use]
    pub fn email(mut self, value: impl Into<String>) -> Self {
        self.params.insert("email".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// The person's first name.
    #[must_use]
    pub fn first_name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("first_name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The Kana variation of the person's first name (Japan only).
    #[must_use]
    pub fn first_name_kana(mut self, value: impl Into<String>) -> Self {
        self.params.insert("first_name_kana".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The Kanji variation of the person's first name (Japan only).
    #[must_use]
    pub fn first_name_kanji(mut self, value: impl Into<String>) -> Self {
        self.params.insert("first_name_kanji".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A list of alternate names or aliases that the person is known by.
    #[must_use]
    pub fn full_name_aliases(mut self, value: serde_json::Value) -> Self {
        self.params.insert("full_name_aliases".to_owned(), value);
        self
    }

    /// The person's gender (International regulations require either "male" or "female").
    #[must_use]
    pub fn gender(mut self, value: impl Into<String>) -> Self {
        self.params.insert("gender".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's ID number, as appropriate for their country. For example, a social security number in the U.S., social insurance number in Canada, etc. Instead of the number itself, you can also provide a \[PII token provided by Stripe.js\](h…
    #[must_use]
    pub fn id_number(mut self, value: impl Into<String>) -> Self {
        self.params.insert("id_number".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's secondary ID number, as appropriate for their country, will be used for enhanced verification checks. In Thailand, this would be the laser code found on the back of an ID card. Instead of the number itself, you can also provid…
    #[must_use]
    pub fn id_number_secondary(mut self, value: impl Into<String>) -> Self {
        self.params.insert("id_number_secondary".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's last name.
    #[must_use]
    pub fn last_name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("last_name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The Kana variation of the person's last name (Japan only).
    #[must_use]
    pub fn last_name_kana(mut self, value: impl Into<String>) -> Self {
        self.params.insert("last_name_kana".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The Kanji variation of the person's last name (Japan only).
    #[must_use]
    pub fn last_name_kanji(mut self, value: impl Into<String>) -> Self {
        self.params.insert("last_name_kanji".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's maiden name.
    #[must_use]
    pub fn maiden_name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("maiden_name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// The country where the person is a national. Two-letter country code (\[ISO 3166-1 alpha-2\](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)), or "XX" if unavailable.
    #[must_use]
    pub fn nationality(mut self, value: impl Into<String>) -> Self {
        self.params.insert("nationality".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A \[person token\](https://docs.stripe.com/connect/account-tokens), used to securely provide details to the person.
    #[must_use]
    pub fn person_token(mut self, value: impl Into<String>) -> Self {
        self.params.insert("person_token".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's phone number.
    #[must_use]
    pub fn phone(mut self, value: impl Into<String>) -> Self {
        self.params.insert("phone".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Indicates if the person or any of their representatives, family members, or other closely related persons, declares that they hold or have held an important public job or function, in any jurisdiction.
    #[must_use]
    pub fn political_exposure(mut self, value: impl Into<String>) -> Self {
        self.params.insert("political_exposure".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The person's registered address.
    #[must_use]
    pub fn registered_address(mut self, value: serde_json::Value) -> Self {
        self.params.insert("registered_address".to_owned(), value);
        self
    }

    /// The relationship that this person has with the account's legal entity.
    #[must_use]
    pub fn relationship(mut self, value: serde_json::Value) -> Self {
        self.params.insert("relationship".to_owned(), value);
        self
    }

    /// The last four digits of the person's Social Security number (U.S. only).
    #[must_use]
    pub fn ssn_last_4(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ssn_last_4".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Demographic data related to the person.
    #[must_use]
    pub fn us_cfpb_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("us_cfpb_data".to_owned(), value);
        self
    }

    /// The person's verification status.
    #[must_use]
    pub fn verification(mut self, value: serde_json::Value) -> Self {
        self.params.insert("verification".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Person, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `DELETE /v1/accounts/{account}/persons/{person}`
///
/// Deletes an existing person’s relationship to the account’s legal entity. Any person with a relationship for an account can be deleted through the API, except if the person is the account_opener. If your integration is using the executive parameter, you cannot delete the only verified executive on file.
#[derive(Debug, Clone)]
pub struct DeleteAccountsAccountPersonsPerson {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteAccountsAccountPersonsPerson {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>, person: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let account = account.into();
        let person = person.into();
        Self { path: format!("/v1/accounts/{account}/persons/{person}"), params }
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::DeletedPerson, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/accounts/{account}/reject`
///
/// With Connect, you can reject accounts that you have flagged as suspicious. Only accounts where your platform is liable for negative account balances, which includes Custom and Express accounts, can be rejected. Test-mode accounts can be rejected at any time. Live-mode accounts can only be rejected after all balances are zero.
#[derive(Debug, Clone)]
pub struct PostAccountsAccountReject {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostAccountsAccountReject {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>, reason: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        let account = account.into();
        params.insert("reason".to_owned(), serde_json::Value::String(reason.into()));
        Self { path: format!("/v1/accounts/{account}/reject"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Account, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
