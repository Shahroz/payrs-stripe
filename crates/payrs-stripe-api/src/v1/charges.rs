//! Generated operations for the `charges` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(
    clippy::doc_markdown,
    clippy::too_many_arguments,
    clippy::new_without_default,
    rustdoc::bare_urls
)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/charges`
///
/// Returns a list of charges you’ve previously created. The charges are returned in sorted order, with the most recent charges appearing first.
#[derive(Debug, Clone)]
pub struct GetCharges {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCharges {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/charges".to_owned(),
            params,
        }
    }

    /// Only return charges that were created during the given date interval.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// Only return charges for the customer specified by this customer ID.
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "customer".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Only return charges that were created by the PaymentIntent specified by this PaymentIntent ID.
    #[must_use]
    pub fn payment_intent(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "payment_intent".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Only return charges for this transfer group, limited to 100.
    #[must_use]
    pub fn transfer_group(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "transfer_group".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Charge> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::Charge>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/charges`
///
/// This method is no longer recommended—use the Payment Intents API to initiate a new payment instead. Confirmation of the PaymentIntent creates the Charge object used to request payment.
#[derive(Debug, Clone)]
pub struct PostCharges {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCharges {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/charges".to_owned(),
            params,
        }
    }

    /// Amount intended to be collected by this payment. A positive integer representing how much to charge in the \[smallest currency unit\](https://docs.stripe.com/currencies#zero-decimal) (e.g., 100 cents to charge $1.00 or 100 to charge ¥100,…
    #[must_use]
    pub fn amount(mut self, value: i64) -> Self {
        self.params
            .insert("amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Set the `application_fee` parameter.
    #[must_use]
    pub fn application_fee(mut self, value: i64) -> Self {
        self.params
            .insert("application_fee".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A fee in cents (or local equivalent) that will be applied to the charge and transferred to the application owner's Stripe account. The request must be made with an OAuth key or the `Stripe-Account` header in order to take an application fe…
    #[must_use]
    pub fn application_fee_amount(mut self, value: i64) -> Self {
        self.params.insert(
            "application_fee_amount".to_owned(),
            serde_json::Value::from(value),
        );
        self
    }

    /// Whether to immediately capture the charge. Defaults to `true`. When `false`, the charge issues an authorization (or pre-authorization), and will need to be \[captured\](https://api.stripe.com#capture_charge) later. Uncaptured charges expir…
    #[must_use]
    pub fn capture(mut self, value: bool) -> Self {
        self.params
            .insert("capture".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// A token, like the ones returned by \[Stripe.js\](https://stripe.com/docs/js).
    #[must_use]
    pub fn card(mut self, value: serde_json::Value) -> Self {
        self.params.insert("card".to_owned(), value);
        self
    }

    /// Three-letter \[ISO currency code\](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a \[supported currency\](https://stripe.com/docs/currencies).
    #[must_use]
    pub fn currency(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "currency".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// The ID of an existing customer that will be charged in this request.
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "customer".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// An arbitrary string which you can attach to a `Charge` object. It is displayed when in the web interface alongside the charge. Note that if you use Stripe to send automatic email receipts to your customers, your receipt emails will include…
    #[must_use]
    pub fn description(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "description".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Set the `destination` parameter.
    #[must_use]
    pub fn destination(mut self, value: serde_json::Value) -> Self {
        self.params.insert("destination".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// The Stripe account ID for which these funds are intended. You can specify the business of record as the connected account using the `on_behalf_of` attribute on the charge. For details, see \[Creating Separate Charges and Transfers\](https:…
    #[must_use]
    pub fn on_behalf_of(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "on_behalf_of".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Options to configure Radar. See \[Radar Session\](https://docs.stripe.com/radar/radar-session) for more information.
    #[must_use]
    pub fn radar_options(mut self, value: serde_json::Value) -> Self {
        self.params.insert("radar_options".to_owned(), value);
        self
    }

    /// The email address to which this charge's \[receipt\](https://docs.stripe.com/dashboard/receipts) will be sent. The receipt will not be sent until the charge is paid, and no receipts will be sent for test mode charges. If this charge is for…
    #[must_use]
    pub fn receipt_email(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "receipt_email".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Shipping information for the charge. Helps prevent fraud on charges for physical goods.
    #[must_use]
    pub fn shipping(mut self, value: serde_json::Value) -> Self {
        self.params.insert("shipping".to_owned(), value);
        self
    }

    /// A payment source to be charged. This can be the ID of a \[card\](https://docs.stripe.com/api#cards) (i.e., credit or debit card), a \[bank account\](https://docs.stripe.com/api#bank_accounts), a \[source\](https://docs.stripe.com/api#sourc…
    #[must_use]
    pub fn source(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("source".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// For a non-card charge, text that appears on the customer's statement as the statement descriptor. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see \[…
    #[must_use]
    pub fn statement_descriptor(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "statement_descriptor".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Provides information about a card charge. Concatenated to the account's \[statement descriptor prefix\](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the complete statement descriptor that appears on the…
    #[must_use]
    pub fn statement_descriptor_suffix(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "statement_descriptor_suffix".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// An optional dictionary including the account to automatically transfer to as part of a destination charge. \[See the Connect documentation\](https://docs.stripe.com/connect/destination-charges) for details.
    #[must_use]
    pub fn transfer_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("transfer_data".to_owned(), value);
        self
    }

    /// A string that identifies this transaction as part of a group. For details, see \[Grouping transactions\](https://docs.stripe.com/connect/separate-charges-and-transfers#transfer-options).
    #[must_use]
    pub fn transfer_group(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "transfer_group".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Charge, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/charges/search`
///
/// Search for charges you’ve previously created using Stripe’s Search Query Language. Don’t use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages. Search functionality is not available to merchants in India.
#[derive(Debug, Clone)]
pub struct GetChargesSearch {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetChargesSearch {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(query: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("query".to_owned(), serde_json::Value::String(query.into()));
        Self {
            path: "/v1/charges/search".to_owned(),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
    #[must_use]
    pub fn page(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("page".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Charge> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::Charge>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/charges/{charge}`
///
/// Retrieves the details of a charge that has previously been created. Supply the unique charge ID that was returned from your previous request, and Stripe will return the corresponding charge information. The same information is returned when creating or refunding the charge.
#[derive(Debug, Clone)]
pub struct GetChargesCharge {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetChargesCharge {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(charge: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let charge = charge.into();
        Self {
            path: format!("/v1/charges/{charge}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Charge, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/charges/{charge}`
///
/// Updates the specified charge by setting the values of the parameters passed. Any parameters not provided will be left unchanged.
#[derive(Debug, Clone)]
pub struct PostChargesCharge {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostChargesCharge {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(charge: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let charge = charge.into();
        Self {
            path: format!("/v1/charges/{charge}"),
            params,
        }
    }

    /// The ID of an existing customer that will be associated with this request. This field may only be updated if there is no existing associated customer with this charge.
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "customer".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// An arbitrary string which you can attach to a charge object. It is displayed when in the web interface alongside the charge. Note that if you use Stripe to send automatic email receipts to your customers, your receipt emails will include t…
    #[must_use]
    pub fn description(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "description".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A set of key-value pairs you can attach to a charge giving information about its riskiness. If you believe a charge is fraudulent, include a `user_report` key with a value of `fraudulent`. If you believe a charge is safe, include a `user_r…
    #[must_use]
    pub fn fraud_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("fraud_details".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// This is the email address that the receipt for this charge will be sent to. If this field is updated, then a new email receipt will be sent to the updated address.
    #[must_use]
    pub fn receipt_email(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "receipt_email".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Shipping information for the charge. Helps prevent fraud on charges for physical goods.
    #[must_use]
    pub fn shipping(mut self, value: serde_json::Value) -> Self {
        self.params.insert("shipping".to_owned(), value);
        self
    }

    /// A string that identifies this transaction as part of a group. `transfer_group` may only be provided if it has not been set. See the \[Connect documentation\](https://docs.stripe.com/connect/separate-charges-and-transfers#transfer-options)…
    #[must_use]
    pub fn transfer_group(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "transfer_group".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Charge, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/charges/{charge}/capture`
///
/// Capture the payment of an existing, uncaptured charge that was created with the capture option set to false. Uncaptured payments expire a set number of days after they are created (7 by default), after which they are marked as refunded and capture attempts will fail. Don’t use this method to capture a PaymentIntent-initiated charge. Use Capture a PaymentIntent.
#[derive(Debug, Clone)]
pub struct PostChargesChargeCapture {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostChargesChargeCapture {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(charge: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let charge = charge.into();
        Self {
            path: format!("/v1/charges/{charge}/capture"),
            params,
        }
    }

    /// The amount to capture, which must be less than or equal to the original amount.
    #[must_use]
    pub fn amount(mut self, value: i64) -> Self {
        self.params
            .insert("amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// An application fee to add on to this charge.
    #[must_use]
    pub fn application_fee(mut self, value: i64) -> Self {
        self.params
            .insert("application_fee".to_owned(), serde_json::Value::from(value));
        self
    }

    /// An application fee amount to add on to this charge, which must be less than or equal to the original amount.
    #[must_use]
    pub fn application_fee_amount(mut self, value: i64) -> Self {
        self.params.insert(
            "application_fee_amount".to_owned(),
            serde_json::Value::from(value),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// The email address to send this charge's receipt to. This will override the previously-specified email address for this charge, if one was set. Receipts will not be sent in test mode.
    #[must_use]
    pub fn receipt_email(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "receipt_email".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// For a non-card charge, text that appears on the customer's statement as the statement descriptor. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see \[…
    #[must_use]
    pub fn statement_descriptor(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "statement_descriptor".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Provides information about a card charge. Concatenated to the account's \[statement descriptor prefix\](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the complete statement descriptor that appears on the…
    #[must_use]
    pub fn statement_descriptor_suffix(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "statement_descriptor_suffix".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// An optional dictionary including the account to automatically transfer to as part of a destination charge. \[See the Connect documentation\](https://docs.stripe.com/connect/destination-charges) for details.
    #[must_use]
    pub fn transfer_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("transfer_data".to_owned(), value);
        self
    }

    /// A string that identifies this transaction as part of a group. `transfer_group` may only be provided if it has not been set. See the \[Connect documentation\](https://docs.stripe.com/connect/separate-charges-and-transfers#transfer-options)…
    #[must_use]
    pub fn transfer_group(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "transfer_group".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Charge, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/charges/{charge}/dispute`
///
/// Retrieve a dispute for a specified charge.
#[derive(Debug, Clone)]
pub struct GetChargesChargeDispute {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetChargesChargeDispute {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(charge: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let charge = charge.into();
        Self {
            path: format!("/v1/charges/{charge}/dispute"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Dispute, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/charges/{charge}/dispute`
#[derive(Debug, Clone)]
pub struct PostChargesChargeDispute {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostChargesChargeDispute {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(charge: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let charge = charge.into();
        Self {
            path: format!("/v1/charges/{charge}/dispute"),
            params,
        }
    }

    /// Evidence to upload, to respond to a dispute. Updating any field in the hash will submit all fields in the hash for review. The combined character count of all fields is limited to 150,000.
    #[must_use]
    pub fn evidence(mut self, value: serde_json::Value) -> Self {
        self.params.insert("evidence".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Whether to immediately submit evidence to the bank. If `false`, evidence is staged on the dispute. Staged evidence is visible in the API and Dashboard, and can be submitted to the bank by making another request with this attribute set to `…
    #[must_use]
    pub fn submit(mut self, value: bool) -> Self {
        self.params
            .insert("submit".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Dispute, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/charges/{charge}/dispute/close`
#[derive(Debug, Clone)]
pub struct PostChargesChargeDisputeClose {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostChargesChargeDisputeClose {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(charge: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let charge = charge.into();
        Self {
            path: format!("/v1/charges/{charge}/dispute/close"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Dispute, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/charges/{charge}/refund`
///
/// When you create a new refund, you must specify either a Charge or a PaymentIntent object. This action refunds a previously created charge that’s not refunded yet. Funds are refunded to the credit or debit card that’s originally charged. You can optionally refund only part of a charge. You can repeat this until the entire charge is refunded. After you entirely refund a charge, you can’t refund it again. This method raises an error when it’s called on an already-refunded charge, or when you attem…
#[derive(Debug, Clone)]
pub struct PostChargesChargeRefund {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostChargesChargeRefund {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(charge: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let charge = charge.into();
        Self {
            path: format!("/v1/charges/{charge}/refund"),
            params,
        }
    }

    /// A positive integer in the \[smallest currency unit\](https://docs.stripe.com/currencies#zero-decimal) representing how much of this charge to refund. Can refund only up to the remaining, unrefunded amount of the charge.
    #[must_use]
    pub fn amount(mut self, value: i64) -> Self {
        self.params
            .insert("amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// For payment methods without native refund support (e.g., Konbini, PromptPay), use this email from the customer to receive refund instructions.
    #[must_use]
    pub fn instructions_email(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "instructions_email".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// The identifier of the PaymentIntent to refund.
    #[must_use]
    pub fn payment_intent(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "payment_intent".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// String indicating the reason for the refund. If set, possible values are `duplicate`, `fraudulent`, and `requested_by_customer`. If you believe the charge to be fraudulent, specifying `fraudulent` as the reason will add the associated card…
    #[must_use]
    pub fn reason(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("reason".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Boolean indicating whether the application fee should be refunded when refunding this charge. If a full charge refund is given, the full application fee will be refunded. Otherwise, the application fee will be refunded in an amount proport…
    #[must_use]
    pub fn refund_application_fee(mut self, value: bool) -> Self {
        self.params.insert(
            "refund_application_fee".to_owned(),
            serde_json::Value::Bool(value),
        );
        self
    }

    /// Boolean indicating whether the transfer should be reversed when refunding this charge. The transfer will be reversed proportionally to the amount being refunded (either the entire or partial amount).A transfer can be reversed only by the a…
    #[must_use]
    pub fn reverse_transfer(mut self, value: bool) -> Self {
        self.params.insert(
            "reverse_transfer".to_owned(),
            serde_json::Value::Bool(value),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Charge, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/charges/{charge}/refunds`
///
/// You can see a list of the refunds belonging to a specific charge. Note that the 10 most recent refunds are always available by default on the charge object. If you need more than those 10, you can use this API method and the limit and starting_after parameters to page through additional refunds.
#[derive(Debug, Clone)]
pub struct GetChargesChargeRefunds {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetChargesChargeRefunds {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(charge: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let charge = charge.into();
        Self {
            path: format!("/v1/charges/{charge}/refunds"),
            params,
        }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Refund> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::Refund>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/charges/{charge}/refunds`
///
/// When you create a new refund, you must specify a Charge or a PaymentIntent object on which to create it. Creating a new refund will refund a charge that has previously been created but not yet refunded. Funds will be refunded to the credit or debit card that was originally charged. You can optionally refund only part of a charge. You can do so multiple times, until the entire charge has been refunded. Once entirely refunded, a charge can’t be refunded again. This method will raise an error when…
#[derive(Debug, Clone)]
pub struct PostChargesChargeRefunds {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostChargesChargeRefunds {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(charge: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let charge = charge.into();
        Self {
            path: format!("/v1/charges/{charge}/refunds"),
            params,
        }
    }

    /// Set the `amount` parameter.
    #[must_use]
    pub fn amount(mut self, value: i64) -> Self {
        self.params
            .insert("amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Three-letter \[ISO currency code\](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a \[supported currency\](https://stripe.com/docs/currencies).
    #[must_use]
    pub fn currency(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "currency".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Customer whose customer balance to refund from.
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "customer".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// For payment methods without native refund support (e.g., Konbini, PromptPay), use this email from the customer to receive refund instructions.
    #[must_use]
    pub fn instructions_email(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "instructions_email".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Origin of the refund
    #[must_use]
    pub fn origin(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("origin".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The identifier of the PaymentIntent to refund.
    #[must_use]
    pub fn payment_intent(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "payment_intent".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// String indicating the reason for the refund. If set, possible values are `duplicate`, `fraudulent`, and `requested_by_customer`. If you believe the charge to be fraudulent, specifying `fraudulent` as the reason will add the associated card…
    #[must_use]
    pub fn reason(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("reason".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Boolean indicating whether the application fee should be refunded when refunding this charge. If a full charge refund is given, the full application fee will be refunded. Otherwise, the application fee will be refunded in an amount proport…
    #[must_use]
    pub fn refund_application_fee(mut self, value: bool) -> Self {
        self.params.insert(
            "refund_application_fee".to_owned(),
            serde_json::Value::Bool(value),
        );
        self
    }

    /// Boolean indicating whether the transfer should be reversed when refunding this charge. The transfer will be reversed proportionally to the amount being refunded (either the entire or partial amount).A transfer can be reversed only by the a…
    #[must_use]
    pub fn reverse_transfer(mut self, value: bool) -> Self {
        self.params.insert(
            "reverse_transfer".to_owned(),
            serde_json::Value::Bool(value),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Refund, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/charges/{charge}/refunds/{refund}`
///
/// Retrieves the details of an existing refund.
#[derive(Debug, Clone)]
pub struct GetChargesChargeRefundsRefund {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetChargesChargeRefundsRefund {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(charge: impl Into<String>, refund: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let charge = charge.into();
        let refund = refund.into();
        Self {
            path: format!("/v1/charges/{charge}/refunds/{refund}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Refund, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/charges/{charge}/refunds/{refund}`
///
/// Update a specified refund.
#[derive(Debug, Clone)]
pub struct PostChargesChargeRefundsRefund {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostChargesChargeRefundsRefund {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(charge: impl Into<String>, refund: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let charge = charge.into();
        let refund = refund.into();
        Self {
            path: format!("/v1/charges/{charge}/refunds/{refund}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set the `metadata` parameter.
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Refund, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
