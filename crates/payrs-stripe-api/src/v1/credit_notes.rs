//! Generated operations for the `credit_notes` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(clippy::doc_markdown, clippy::too_many_arguments, clippy::new_without_default, rustdoc::bare_urls)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/credit_notes`
///
/// Returns a list of credit notes.
#[derive(Debug, Clone)]
pub struct GetCreditNotes {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCreditNotes {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/credit_notes".to_owned(), params }
    }

    /// Only return credit notes that were created during the given date interval.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// Only return credit notes for the customer specified by this customer ID.
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Only return credit notes for the account representing the customer specified by this account ID.
    #[must_use]
    pub fn customer_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Only return credit notes for the invoice specified by this invoice ID.
    #[must_use]
    pub fn invoice(mut self, value: impl Into<String>) -> Self {
        self.params.insert("invoice".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::CreditNote> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::CreditNote>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/credit_notes`
///
/// Issue a credit note to adjust the amount of a finalized invoice. A credit note will first reduce the invoice’s amount_remaining (and amount_due), but not below zero. This amount is indicated by the credit note’s pre_payment_amount. The excess amount is indicated by post_payment_amount, and it can result in any combination of the following: Refunds: create a new refund (using refund_amount) or link existing refunds (using refunds). Customer balance credit: credit the customer’s balance (using cr…
#[derive(Debug, Clone)]
pub struct PostCreditNotes {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCreditNotes {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(invoice: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("invoice".to_owned(), serde_json::Value::String(invoice.into()));
        Self { path: "/v1/credit_notes".to_owned(), params }
    }

    /// The integer amount in cents (or local equivalent) representing the total amount of the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
    #[must_use]
    pub fn amount(mut self, value: i64) -> Self {
        self.params.insert("amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// The integer amount in cents (or local equivalent) representing the amount to credit the customer's balance, which will be automatically applied to their next invoice.
    #[must_use]
    pub fn credit_amount(mut self, value: i64) -> Self {
        self.params.insert("credit_amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// The date when this credit note is in effect. Same as `created` unless overwritten. When defined, this value replaces the system-generated 'Date of issue' printed on the credit note PDF.
    #[must_use]
    pub fn effective_at(mut self, value: i64) -> Self {
        self.params.insert("effective_at".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Type of email to send to the customer, one of `credit_note` or `none` and the default is `credit_note`.
    #[must_use]
    pub fn email_type(mut self, value: impl Into<String>) -> Self {
        self.params.insert("email_type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Line items that make up the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
    #[must_use]
    pub fn lines(mut self, value: serde_json::Value) -> Self {
        self.params.insert("lines".to_owned(), value);
        self
    }

    /// The credit note's memo appears on the credit note PDF.
    #[must_use]
    pub fn memo(mut self, value: impl Into<String>) -> Self {
        self.params.insert("memo".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert("metadata".to_owned(), serde_json::Value::Object(value.into_iter().map(|(k, v)| (k, serde_json::Value::String(v))).collect()));
        self
    }

    /// The integer amount in cents (or local equivalent) representing the amount that is credited outside of Stripe.
    #[must_use]
    pub fn out_of_band_amount(mut self, value: i64) -> Self {
        self.params.insert("out_of_band_amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Reason for issuing this credit note, one of `duplicate`, `fraudulent`, `order_change`, or `product_unsatisfactory`
    #[must_use]
    pub fn reason(mut self, value: impl Into<String>) -> Self {
        self.params.insert("reason".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The integer amount in cents (or local equivalent) representing the amount to refund. If set, a refund will be created for the charge associated with the invoice.
    #[must_use]
    pub fn refund_amount(mut self, value: i64) -> Self {
        self.params.insert("refund_amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Refunds to link to this credit note.
    #[must_use]
    pub fn refunds(mut self, value: serde_json::Value) -> Self {
        self.params.insert("refunds".to_owned(), value);
        self
    }

    /// When shipping_cost contains the shipping_rate from the invoice, the shipping_cost is included in the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
    #[must_use]
    pub fn shipping_cost(mut self, value: serde_json::Value) -> Self {
        self.params.insert("shipping_cost".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::CreditNote, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/credit_notes/preview`
///
/// Get a preview of a credit note without creating it.
#[derive(Debug, Clone)]
pub struct GetCreditNotesPreview {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCreditNotesPreview {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(invoice: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("invoice".to_owned(), serde_json::Value::String(invoice.into()));
        Self { path: "/v1/credit_notes/preview".to_owned(), params }
    }

    /// The integer amount in cents (or local equivalent) representing the total amount of the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
    #[must_use]
    pub fn amount(mut self, value: i64) -> Self {
        self.params.insert("amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// The integer amount in cents (or local equivalent) representing the amount to credit the customer's balance, which will be automatically applied to their next invoice.
    #[must_use]
    pub fn credit_amount(mut self, value: i64) -> Self {
        self.params.insert("credit_amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// The date when this credit note is in effect. Same as `created` unless overwritten. When defined, this value replaces the system-generated 'Date of issue' printed on the credit note PDF.
    #[must_use]
    pub fn effective_at(mut self, value: i64) -> Self {
        self.params.insert("effective_at".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Type of email to send to the customer, one of `credit_note` or `none` and the default is `credit_note`.
    #[must_use]
    pub fn email_type(mut self, value: impl Into<String>) -> Self {
        self.params.insert("email_type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Line items that make up the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
    #[must_use]
    pub fn lines(mut self, value: serde_json::Value) -> Self {
        self.params.insert("lines".to_owned(), value);
        self
    }

    /// The credit note's memo appears on the credit note PDF.
    #[must_use]
    pub fn memo(mut self, value: impl Into<String>) -> Self {
        self.params.insert("memo".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert("metadata".to_owned(), serde_json::Value::Object(value.into_iter().map(|(k, v)| (k, serde_json::Value::String(v))).collect()));
        self
    }

    /// The integer amount in cents (or local equivalent) representing the amount that is credited outside of Stripe.
    #[must_use]
    pub fn out_of_band_amount(mut self, value: i64) -> Self {
        self.params.insert("out_of_band_amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Reason for issuing this credit note, one of `duplicate`, `fraudulent`, `order_change`, or `product_unsatisfactory`
    #[must_use]
    pub fn reason(mut self, value: impl Into<String>) -> Self {
        self.params.insert("reason".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The integer amount in cents (or local equivalent) representing the amount to refund. If set, a refund will be created for the charge associated with the invoice.
    #[must_use]
    pub fn refund_amount(mut self, value: i64) -> Self {
        self.params.insert("refund_amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Refunds to link to this credit note.
    #[must_use]
    pub fn refunds(mut self, value: serde_json::Value) -> Self {
        self.params.insert("refunds".to_owned(), value);
        self
    }

    /// When shipping_cost contains the shipping_rate from the invoice, the shipping_cost is included in the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
    #[must_use]
    pub fn shipping_cost(mut self, value: serde_json::Value) -> Self {
        self.params.insert("shipping_cost".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::CreditNote, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/credit_notes/preview/lines`
///
/// When retrieving a credit note preview, you’ll get a lines property containing the first handful of those items. This URL you can retrieve the full (paginated) list of line items.
#[derive(Debug, Clone)]
pub struct GetCreditNotesPreviewLines {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCreditNotesPreviewLines {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(invoice: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("invoice".to_owned(), serde_json::Value::String(invoice.into()));
        Self { path: "/v1/credit_notes/preview/lines".to_owned(), params }
    }

    /// The integer amount in cents (or local equivalent) representing the total amount of the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
    #[must_use]
    pub fn amount(mut self, value: i64) -> Self {
        self.params.insert("amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// The integer amount in cents (or local equivalent) representing the amount to credit the customer's balance, which will be automatically applied to their next invoice.
    #[must_use]
    pub fn credit_amount(mut self, value: i64) -> Self {
        self.params.insert("credit_amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// The date when this credit note is in effect. Same as `created` unless overwritten. When defined, this value replaces the system-generated 'Date of issue' printed on the credit note PDF.
    #[must_use]
    pub fn effective_at(mut self, value: i64) -> Self {
        self.params.insert("effective_at".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Type of email to send to the customer, one of `credit_note` or `none` and the default is `credit_note`.
    #[must_use]
    pub fn email_type(mut self, value: impl Into<String>) -> Self {
        self.params.insert("email_type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Line items that make up the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
    #[must_use]
    pub fn lines(mut self, value: serde_json::Value) -> Self {
        self.params.insert("lines".to_owned(), value);
        self
    }

    /// The credit note's memo appears on the credit note PDF.
    #[must_use]
    pub fn memo(mut self, value: impl Into<String>) -> Self {
        self.params.insert("memo".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert("metadata".to_owned(), serde_json::Value::Object(value.into_iter().map(|(k, v)| (k, serde_json::Value::String(v))).collect()));
        self
    }

    /// The integer amount in cents (or local equivalent) representing the amount that is credited outside of Stripe.
    #[must_use]
    pub fn out_of_band_amount(mut self, value: i64) -> Self {
        self.params.insert("out_of_band_amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Reason for issuing this credit note, one of `duplicate`, `fraudulent`, `order_change`, or `product_unsatisfactory`
    #[must_use]
    pub fn reason(mut self, value: impl Into<String>) -> Self {
        self.params.insert("reason".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The integer amount in cents (or local equivalent) representing the amount to refund. If set, a refund will be created for the charge associated with the invoice.
    #[must_use]
    pub fn refund_amount(mut self, value: i64) -> Self {
        self.params.insert("refund_amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Refunds to link to this credit note.
    #[must_use]
    pub fn refunds(mut self, value: serde_json::Value) -> Self {
        self.params.insert("refunds".to_owned(), value);
        self
    }

    /// When shipping_cost contains the shipping_rate from the invoice, the shipping_cost is included in the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
    #[must_use]
    pub fn shipping_cost(mut self, value: serde_json::Value) -> Self {
        self.params.insert("shipping_cost".to_owned(), value);
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::CreditNoteLineItem> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::CreditNoteLineItem>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/credit_notes/{credit_note}/lines`
///
/// When retrieving a credit note, you’ll get a lines property containing the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of line items.
#[derive(Debug, Clone)]
pub struct GetCreditNotesCreditNoteLines {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCreditNotesCreditNoteLines {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(credit_note: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let credit_note = credit_note.into();
        Self { path: format!("/v1/credit_notes/{credit_note}/lines"), params }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::CreditNoteLineItem> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::CreditNoteLineItem>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/credit_notes/{id}`
///
/// Retrieves the credit note object with the given identifier.
#[derive(Debug, Clone)]
pub struct GetCreditNotesId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCreditNotesId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let id = id.into();
        Self { path: format!("/v1/credit_notes/{id}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::CreditNote, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/credit_notes/{id}`
///
/// Updates an existing credit note.
#[derive(Debug, Clone)]
pub struct PostCreditNotesId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCreditNotesId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let id = id.into();
        Self { path: format!("/v1/credit_notes/{id}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Credit note memo.
    #[must_use]
    pub fn memo(mut self, value: impl Into<String>) -> Self {
        self.params.insert("memo".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert("metadata".to_owned(), serde_json::Value::Object(value.into_iter().map(|(k, v)| (k, serde_json::Value::String(v))).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::CreditNote, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/credit_notes/{id}/void`
///
/// Marks a credit note as void. Learn more about voiding credit notes.
#[derive(Debug, Clone)]
pub struct PostCreditNotesIdVoid {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCreditNotesIdVoid {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let id = id.into();
        Self { path: format!("/v1/credit_notes/{id}/void"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::CreditNote, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
