//! Generated operations for the `setup_intents` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(
    clippy::doc_markdown,
    clippy::too_many_arguments,
    clippy::new_without_default,
    rustdoc::bare_urls
)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/setup_intents`
///
/// Returns a list of SetupIntents.
#[derive(Debug, Clone)]
pub struct GetSetupIntents {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetSetupIntents {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/setup_intents".to_owned(),
            params,
        }
    }

    /// If present, the SetupIntent's payment method will be attached to the in-context Stripe Account. It can only be used for this Stripe Account’s own money movement flows like InboundTransfer and OutboundTransfers. It cannot be set to true whe…
    #[must_use]
    pub fn attach_to_self(mut self, value: bool) -> Self {
        self.params
            .insert("attach_to_self".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// Only return SetupIntents for the customer specified by this customer ID.
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "customer".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Only return SetupIntents for the account specified by this customer ID.
    #[must_use]
    pub fn customer_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "customer_account".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Only return SetupIntents that associate with the specified payment method.
    #[must_use]
    pub fn payment_method(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "payment_method".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::SetupIntent> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::SetupIntent>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/setup_intents`
///
/// Creates a SetupIntent object. After you create the SetupIntent, attach a payment method and confirm it to collect any required permissions to charge the payment method later.
#[derive(Debug, Clone)]
pub struct PostSetupIntents {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostSetupIntents {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/setup_intents".to_owned(),
            params,
        }
    }

    /// If present, the SetupIntent's payment method will be attached to the in-context Stripe Account. It can only be used for this Stripe Account’s own money movement flows like InboundTransfer and OutboundTransfers. It cannot be set to true whe…
    #[must_use]
    pub fn attach_to_self(mut self, value: bool) -> Self {
        self.params
            .insert("attach_to_self".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// When you enable this parameter, this SetupIntent accepts payment methods that you enable in the Dashboard and that are compatible with its other parameters.
    #[must_use]
    pub fn automatic_payment_methods(mut self, value: serde_json::Value) -> Self {
        self.params
            .insert("automatic_payment_methods".to_owned(), value);
        self
    }

    /// Set to `true` to attempt to confirm this SetupIntent immediately. This parameter defaults to `false`. If a card is the attached payment method, you can provide a `return_url` in case further authentication is necessary.
    #[must_use]
    pub fn confirm(mut self, value: bool) -> Self {
        self.params
            .insert("confirm".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// ID of the ConfirmationToken used to confirm this SetupIntent. If the provided ConfirmationToken contains properties that are also being provided in this request, such as `payment_method`, then the values in this request will take precedenc…
    #[must_use]
    pub fn confirmation_token(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "confirmation_token".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// ID of the Customer this SetupIntent belongs to, if one exists. If present, the SetupIntent's payment method will be attached to the Customer on successful setup. Payment methods attached to other Customers cannot be used with this SetupInt…
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "customer".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// ID of the Account this SetupIntent belongs to, if one exists. If present, the SetupIntent's payment method will be attached to the Account on successful setup. Payment methods attached to other Accounts cannot be used with this SetupIntent.
    #[must_use]
    pub fn customer_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "customer_account".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// An arbitrary string attached to the object. Often useful for displaying to users.
    #[must_use]
    pub fn description(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "description".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// The list of payment method types to exclude from use with this SetupIntent.
    #[must_use]
    pub fn excluded_payment_method_types(mut self, value: serde_json::Value) -> Self {
        self.params
            .insert("excluded_payment_method_types".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Indicates the directions of money movement for which this payment method is intended to be used. Include `inbound` if you intend to use the payment method as the origin to pull funds from. Include `outbound` if you intend to use the paymen…
    #[must_use]
    pub fn flow_directions(mut self, value: serde_json::Value) -> Self {
        self.params.insert("flow_directions".to_owned(), value);
        self
    }

    /// This hash contains details about the mandate to create. This parameter can only be used with \[`confirm=true`\](https://docs.stripe.com/api/setup_intents/create#create_setup_intent-confirm).
    #[must_use]
    pub fn mandate_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("mandate_data".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert(
            "metadata".to_owned(),
            serde_json::Value::Object(
                value
                    .into_iter()
                    .map(|(k, v)| (k, serde_json::Value::String(v)))
                    .collect(),
            ),
        );
        self
    }

    /// The Stripe account ID created for this SetupIntent.
    #[must_use]
    pub fn on_behalf_of(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "on_behalf_of".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// ID of the payment method (a PaymentMethod, Card, or saved Source object) to attach to this SetupIntent.
    #[must_use]
    pub fn payment_method(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "payment_method".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// The ID of the \[payment method configuration\](https://docs.stripe.com/api/payment_method_configurations) to use with this SetupIntent.
    #[must_use]
    pub fn payment_method_configuration(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "payment_method_configuration".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// When included, this hash creates a PaymentMethod that is set as the \[`payment_method`\](https://docs.stripe.com/api/setup_intents/object#setup_intent_object-payment_method) value in the SetupIntent.
    #[must_use]
    pub fn payment_method_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("payment_method_data".to_owned(), value);
        self
    }

    /// Payment method-specific configuration for this SetupIntent.
    #[must_use]
    pub fn payment_method_options(mut self, value: serde_json::Value) -> Self {
        self.params
            .insert("payment_method_options".to_owned(), value);
        self
    }

    /// The list of payment method types (for example, card) that this SetupIntent can use. If you don't provide this, Stripe will dynamically show relevant payment methods from your \[payment method settings\](https://dashboard.stripe.com/setting…
    #[must_use]
    pub fn payment_method_types(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "payment_method_types".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// The URL to redirect your customer back to after they authenticate or cancel their payment on the payment method's app or site. To redirect to a mobile application, you can alternatively supply an application URI scheme. This parameter can…
    #[must_use]
    pub fn return_url(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "return_url".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// If you populate this hash, this SetupIntent generates a `single_use` mandate after successful completion. Single-use mandates are only valid for the following payment methods: `acss_debit`, `alipay`, `au_becs_debit`, `bacs_debit`, `bancont…
    #[must_use]
    pub fn single_use(mut self, value: serde_json::Value) -> Self {
        self.params.insert("single_use".to_owned(), value);
        self
    }

    /// Indicates how the payment method is intended to be used in the future. If not provided, this value defaults to `off_session`.
    #[must_use]
    pub fn usage(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("usage".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set to `true` when confirming server-side and using Stripe.js, iOS, or Android client-side SDKs to handle the next actions.
    #[must_use]
    pub fn use_stripe_sdk(mut self, value: bool) -> Self {
        self.params
            .insert("use_stripe_sdk".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::SetupIntent, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/setup_intents/{intent}`
///
/// Retrieves the details of a SetupIntent that has previously been created. Client-side retrieval using a publishable key is allowed when the client_secret is provided in the query string. When retrieved with a publishable key, only a subset of properties will be returned. Please refer to the SetupIntent object reference for more details.
#[derive(Debug, Clone)]
pub struct GetSetupIntentsIntent {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetSetupIntentsIntent {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(intent: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let intent = intent.into();
        Self {
            path: format!("/v1/setup_intents/{intent}"),
            params,
        }
    }

    /// The client secret of the SetupIntent. We require this string if you use a publishable key to retrieve the SetupIntent.
    #[must_use]
    pub fn client_secret(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "client_secret".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::SetupIntent, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/setup_intents/{intent}`
///
/// Updates a SetupIntent object.
#[derive(Debug, Clone)]
pub struct PostSetupIntentsIntent {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostSetupIntentsIntent {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(intent: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let intent = intent.into();
        Self {
            path: format!("/v1/setup_intents/{intent}"),
            params,
        }
    }

    /// If present, the SetupIntent's payment method will be attached to the in-context Stripe Account. It can only be used for this Stripe Account’s own money movement flows like InboundTransfer and OutboundTransfers. It cannot be set to true whe…
    #[must_use]
    pub fn attach_to_self(mut self, value: bool) -> Self {
        self.params
            .insert("attach_to_self".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// ID of the Customer this SetupIntent belongs to, if one exists. If present, the SetupIntent's payment method will be attached to the Customer on successful setup. Payment methods attached to other Customers cannot be used with this SetupInt…
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "customer".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// ID of the Account this SetupIntent belongs to, if one exists. If present, the SetupIntent's payment method will be attached to the Account on successful setup. Payment methods attached to other Accounts cannot be used with this SetupIntent.
    #[must_use]
    pub fn customer_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "customer_account".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// An arbitrary string attached to the object. Often useful for displaying to users.
    #[must_use]
    pub fn description(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "description".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// The list of payment method types to exclude from use with this SetupIntent.
    #[must_use]
    pub fn excluded_payment_method_types(mut self, value: serde_json::Value) -> Self {
        self.params
            .insert("excluded_payment_method_types".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Indicates the directions of money movement for which this payment method is intended to be used. Include `inbound` if you intend to use the payment method as the origin to pull funds from. Include `outbound` if you intend to use the paymen…
    #[must_use]
    pub fn flow_directions(mut self, value: serde_json::Value) -> Self {
        self.params.insert("flow_directions".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// ID of the payment method (a PaymentMethod, Card, or saved Source object) to attach to this SetupIntent. To unset this field to null, pass in an empty string.
    #[must_use]
    pub fn payment_method(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "payment_method".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// The ID of the \[payment method configuration\](https://docs.stripe.com/api/payment_method_configurations) to use with this SetupIntent.
    #[must_use]
    pub fn payment_method_configuration(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "payment_method_configuration".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// When included, this hash creates a PaymentMethod that is set as the \[`payment_method`\](https://docs.stripe.com/api/setup_intents/object#setup_intent_object-payment_method) value in the SetupIntent.
    #[must_use]
    pub fn payment_method_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("payment_method_data".to_owned(), value);
        self
    }

    /// Payment method-specific configuration for this SetupIntent.
    #[must_use]
    pub fn payment_method_options(mut self, value: serde_json::Value) -> Self {
        self.params
            .insert("payment_method_options".to_owned(), value);
        self
    }

    /// The list of payment method types (for example, card) that this SetupIntent can set up. If you don't provide this, Stripe will dynamically show relevant payment methods from your \[payment method settings\](https://dashboard.stripe.com/sett…
    #[must_use]
    pub fn payment_method_types(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "payment_method_types".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::SetupIntent, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/setup_intents/{intent}/cancel`
///
/// You can cancel a SetupIntent object when it’s in one of these statuses: requires_payment_method, requires_confirmation, or requires_action. After you cancel it, setup is abandoned and any operations on the SetupIntent fail with an error. You can’t cancel the SetupIntent for a Checkout Session. Expire the Checkout Session instead.
#[derive(Debug, Clone)]
pub struct PostSetupIntentsIntentCancel {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostSetupIntentsIntentCancel {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(intent: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let intent = intent.into();
        Self {
            path: format!("/v1/setup_intents/{intent}/cancel"),
            params,
        }
    }

    /// Reason for canceling this SetupIntent. Possible values are: `abandoned`, `requested_by_customer`, or `duplicate`
    #[must_use]
    pub fn cancellation_reason(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "cancellation_reason".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::SetupIntent, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/setup_intents/{intent}/confirm`
///
/// Confirm that your customer intends to set up the current or provided payment method. For example, you would confirm a SetupIntent when a customer hits the “Save” button on a payment method management page on your website. If the selected payment method does not require any additional steps from the customer, the SetupIntent will transition to the succeeded status. Otherwise, it will transition to the requires_action status and suggest additional actions via next_action. If setup fails, the Setu…
#[derive(Debug, Clone)]
pub struct PostSetupIntentsIntentConfirm {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostSetupIntentsIntentConfirm {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(intent: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let intent = intent.into();
        Self {
            path: format!("/v1/setup_intents/{intent}/confirm"),
            params,
        }
    }

    /// The client secret of the SetupIntent.
    #[must_use]
    pub fn client_secret(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "client_secret".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// ID of the ConfirmationToken used to confirm this SetupIntent. If the provided ConfirmationToken contains properties that are also being provided in this request, such as `payment_method`, then the values in this request will take precedenc…
    #[must_use]
    pub fn confirmation_token(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "confirmation_token".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set the `mandate_data` parameter.
    #[must_use]
    pub fn mandate_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("mandate_data".to_owned(), value);
        self
    }

    /// ID of the payment method (a PaymentMethod, Card, or saved Source object) to attach to this SetupIntent.
    #[must_use]
    pub fn payment_method(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "payment_method".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// When included, this hash creates a PaymentMethod that is set as the \[`payment_method`\](https://docs.stripe.com/api/setup_intents/object#setup_intent_object-payment_method) value in the SetupIntent.
    #[must_use]
    pub fn payment_method_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("payment_method_data".to_owned(), value);
        self
    }

    /// Payment method-specific configuration for this SetupIntent.
    #[must_use]
    pub fn payment_method_options(mut self, value: serde_json::Value) -> Self {
        self.params
            .insert("payment_method_options".to_owned(), value);
        self
    }

    /// The URL to redirect your customer back to after they authenticate on the payment method's app or site. If you'd prefer to redirect to a mobile application, you can alternatively supply an application URI scheme. This parameter is only used…
    #[must_use]
    pub fn return_url(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "return_url".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Set to `true` when confirming server-side and using Stripe.js, iOS, or Android client-side SDKs to handle the next actions.
    #[must_use]
    pub fn use_stripe_sdk(mut self, value: bool) -> Self {
        self.params
            .insert("use_stripe_sdk".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::SetupIntent, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/setup_intents/{intent}/verify_microdeposits`
///
/// Verifies microdeposits on a SetupIntent object.
#[derive(Debug, Clone)]
pub struct PostSetupIntentsIntentVerifyMicrodeposits {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostSetupIntentsIntentVerifyMicrodeposits {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(intent: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let intent = intent.into();
        Self {
            path: format!("/v1/setup_intents/{intent}/verify_microdeposits"),
            params,
        }
    }

    /// Two positive integers, in *cents*, equal to the values of the microdeposits sent to the bank account.
    #[must_use]
    pub fn amounts(mut self, value: serde_json::Value) -> Self {
        self.params.insert("amounts".to_owned(), value);
        self
    }

    /// The client secret of the SetupIntent.
    #[must_use]
    pub fn client_secret(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "client_secret".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// A six-character code starting with SM present in the microdeposit sent to the bank account.
    #[must_use]
    pub fn descriptor_code(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "descriptor_code".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::SetupIntent, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
