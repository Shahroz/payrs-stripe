//! Generated operations for the `subscription_schedules` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(clippy::doc_markdown, clippy::too_many_arguments, clippy::new_without_default, rustdoc::bare_urls)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/subscription_schedules`
///
/// Retrieves the list of your subscription schedules.
#[derive(Debug, Clone)]
pub struct GetSubscriptionSchedules {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetSubscriptionSchedules {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/subscription_schedules".to_owned(), params }
    }

    /// Only return subscription schedules that were created canceled the given date interval.
    #[must_use]
    pub fn canceled_at(mut self, value: serde_json::Value) -> Self {
        self.params.insert("canceled_at".to_owned(), value);
        self
    }

    /// Only return subscription schedules that completed during the given date interval.
    #[must_use]
    pub fn completed_at(mut self, value: serde_json::Value) -> Self {
        self.params.insert("completed_at".to_owned(), value);
        self
    }

    /// Only return subscription schedules that were created during the given date interval.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// Only return subscription schedules for the given customer.
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Only return subscription schedules for the given account.
    #[must_use]
    pub fn customer_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Only return subscription schedules that were released during the given date interval.
    #[must_use]
    pub fn released_at(mut self, value: serde_json::Value) -> Self {
        self.params.insert("released_at".to_owned(), value);
        self
    }

    /// Only return subscription schedules that have not started yet.
    #[must_use]
    pub fn scheduled(mut self, value: bool) -> Self {
        self.params.insert("scheduled".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::SubscriptionSchedule> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::SubscriptionSchedule>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/subscription_schedules`
///
/// Creates a new subscription schedule object. Each customer can have up to 500 active or scheduled subscriptions.
#[derive(Debug, Clone)]
pub struct PostSubscriptionSchedules {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostSubscriptionSchedules {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/subscription_schedules".to_owned(), params }
    }

    /// Controls how prorations and invoices for subscriptions are calculated and orchestrated.
    #[must_use]
    pub fn billing_mode(mut self, value: serde_json::Value) -> Self {
        self.params.insert("billing_mode".to_owned(), value);
        self
    }

    /// The identifier of the customer to create the subscription schedule for.
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The identifier of the account to create the subscription schedule for.
    #[must_use]
    pub fn customer_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Object representing the subscription schedule's default settings.
    #[must_use]
    pub fn default_settings(mut self, value: serde_json::Value) -> Self {
        self.params.insert("default_settings".to_owned(), value);
        self
    }

    /// Behavior of the subscription schedule and underlying subscription when it ends. Possible values are `release` or `cancel` with the default being `release`. `release` will end the subscription schedule and keep the underlying subscription r…
    #[must_use]
    pub fn end_behavior(mut self, value: impl Into<String>) -> Self {
        self.params.insert("end_behavior".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Migrate an existing subscription to be managed by a subscription schedule. If this parameter is set, a subscription schedule will be created using the subscription's item(s), set to auto-renew using the subscription's interval. When using…
    #[must_use]
    pub fn from_subscription(mut self, value: impl Into<String>) -> Self {
        self.params.insert("from_subscription".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// List representing phases of the subscription schedule. Each phase can be customized to have different durations, plans, and coupons. If there are multiple phases, the `end_date` of one phase will always equal the `start_date` of the next p…
    #[must_use]
    pub fn phases(mut self, value: serde_json::Value) -> Self {
        self.params.insert("phases".to_owned(), value);
        self
    }

    /// When the subscription schedule starts. We recommend using `now` so that it starts the subscription immediately. You can also use a Unix timestamp to backdate the subscription so that it starts on a past date, or set a future date for the s…
    #[must_use]
    pub fn start_date(mut self, value: serde_json::Value) -> Self {
        self.params.insert("start_date".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::SubscriptionSchedule, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/subscription_schedules/{schedule}`
///
/// Retrieves the details of an existing subscription schedule. You only need to supply the unique subscription schedule identifier that was returned upon subscription schedule creation.
#[derive(Debug, Clone)]
pub struct GetSubscriptionSchedulesSchedule {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetSubscriptionSchedulesSchedule {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(schedule: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let schedule = schedule.into();
        Self { path: format!("/v1/subscription_schedules/{schedule}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::SubscriptionSchedule, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/subscription_schedules/{schedule}`
///
/// Updates an existing subscription schedule.
#[derive(Debug, Clone)]
pub struct PostSubscriptionSchedulesSchedule {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostSubscriptionSchedulesSchedule {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(schedule: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let schedule = schedule.into();
        Self { path: format!("/v1/subscription_schedules/{schedule}"), params }
    }

    /// Object representing the subscription schedule's default settings.
    #[must_use]
    pub fn default_settings(mut self, value: serde_json::Value) -> Self {
        self.params.insert("default_settings".to_owned(), value);
        self
    }

    /// Behavior of the subscription schedule and underlying subscription when it ends. Possible values are `release` or `cancel` with the default being `release`. `release` will end the subscription schedule and keep the underlying subscription r…
    #[must_use]
    pub fn end_behavior(mut self, value: impl Into<String>) -> Self {
        self.params.insert("end_behavior".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// List representing phases of the subscription schedule. Each phase can be customized to have different durations, plans, and coupons. If there are multiple phases, the `end_date` of one phase will always equal the `start_date` of the next p…
    #[must_use]
    pub fn phases(mut self, value: serde_json::Value) -> Self {
        self.params.insert("phases".to_owned(), value);
        self
    }

    /// If the update changes the billing configuration (item price, quantity, etc.) of the current phase, indicates how prorations from this change should be handled. The default value is `create_prorations`.
    #[must_use]
    pub fn proration_behavior(mut self, value: impl Into<String>) -> Self {
        self.params.insert("proration_behavior".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::SubscriptionSchedule, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/subscription_schedules/{schedule}/cancel`
///
/// Cancels a subscription schedule and its associated subscription immediately (if the subscription schedule has an active subscription). A subscription schedule can only be canceled if its status is not_started or active.
#[derive(Debug, Clone)]
pub struct PostSubscriptionSchedulesScheduleCancel {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostSubscriptionSchedulesScheduleCancel {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(schedule: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let schedule = schedule.into();
        Self { path: format!("/v1/subscription_schedules/{schedule}/cancel"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// If the subscription schedule is `active`, indicates if a final invoice will be generated that contains any un-invoiced metered usage and new/pending proration invoice items. Defaults to `true`.
    #[must_use]
    pub fn invoice_now(mut self, value: bool) -> Self {
        self.params.insert("invoice_now".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// If the subscription schedule is `active`, indicates if the cancellation should be prorated. Defaults to `true`.
    #[must_use]
    pub fn prorate(mut self, value: bool) -> Self {
        self.params.insert("prorate".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::SubscriptionSchedule, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/subscription_schedules/{schedule}/release`
///
/// Releases the subscription schedule immediately, which will stop scheduling of its phases, but leave any existing subscription in place. A schedule can only be released if its status is not_started or active. If the subscription schedule is currently associated with a subscription, releasing it will remove its subscription property and set the subscription’s ID to the released_subscription property.
#[derive(Debug, Clone)]
pub struct PostSubscriptionSchedulesScheduleRelease {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostSubscriptionSchedulesScheduleRelease {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(schedule: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let schedule = schedule.into();
        Self { path: format!("/v1/subscription_schedules/{schedule}/release"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Keep any cancellation on the subscription that the schedule has set
    #[must_use]
    pub fn preserve_cancel_date(mut self, value: bool) -> Self {
        self.params.insert("preserve_cancel_date".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::SubscriptionSchedule, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
