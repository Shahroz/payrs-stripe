//! Generated operations for the `prices` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(
    clippy::doc_markdown,
    clippy::too_many_arguments,
    clippy::new_without_default,
    rustdoc::bare_urls
)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/prices`
///
/// Returns a list of your active prices, excluding inline prices. For the list of inactive prices, set active to false.
#[derive(Debug, Clone)]
pub struct GetPrices {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetPrices {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/prices".to_owned(),
            params,
        }
    }

    /// Only return prices that are active or inactive (e.g., pass `false` to list all inactive prices).
    #[must_use]
    pub fn active(mut self, value: bool) -> Self {
        self.params
            .insert("active".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// Only return prices for the given currency.
    #[must_use]
    pub fn currency(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "currency".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Only return the price with these lookup_keys, if any exist. You can specify up to 10 lookup_keys.
    #[must_use]
    pub fn lookup_keys(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "lookup_keys".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Only return prices for the given product.
    #[must_use]
    pub fn product(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "product".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Only return prices with these recurring fields.
    #[must_use]
    pub fn recurring(mut self, value: serde_json::Value) -> Self {
        self.params.insert("recurring".to_owned(), value);
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Only return prices of type `recurring` or `one_time`.
    #[must_use]
    pub fn type_(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Price> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::Price>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/prices`
///
/// Creates a new Price for an existing Product. The Price can be recurring or one-time.
#[derive(Debug, Clone)]
pub struct PostPrices {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPrices {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(currency: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert(
            "currency".to_owned(),
            serde_json::Value::String(currency.into()),
        );
        Self {
            path: "/v1/prices".to_owned(),
            params,
        }
    }

    /// Whether the price can be used for new purchases. Defaults to `true`.
    #[must_use]
    pub fn active(mut self, value: bool) -> Self {
        self.params
            .insert("active".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Describes how to compute the price per period. Either `per_unit` or `tiered`. `per_unit` indicates that the fixed amount (specified in `unit_amount` or `unit_amount_decimal`) will be charged per unit in `quantity` (for prices with `usage_t…
    #[must_use]
    pub fn billing_scheme(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "billing_scheme".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Prices defined in each available currency option. Each key must be a three-letter \[ISO currency code\](https://www.iso.org/iso-4217-currency-codes.html) and a \[supported currency\](https://stripe.com/docs/currencies).
    #[must_use]
    pub fn currency_options(mut self, value: serde_json::Value) -> Self {
        self.params.insert("currency_options".to_owned(), value);
        self
    }

    /// When set, provides configuration for the amount to be adjusted by the customer during Checkout Sessions and Payment Links.
    #[must_use]
    pub fn custom_unit_amount(mut self, value: serde_json::Value) -> Self {
        self.params.insert("custom_unit_amount".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A lookup key used to retrieve prices dynamically from a static string. This may be up to 200 characters.
    #[must_use]
    pub fn lookup_key(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "lookup_key".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert(
            "metadata".to_owned(),
            serde_json::Value::Object(
                value
                    .into_iter()
                    .map(|(k, v)| (k, serde_json::Value::String(v)))
                    .collect(),
            ),
        );
        self
    }

    /// A brief description of the price, hidden from customers.
    #[must_use]
    pub fn nickname(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "nickname".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// The ID of the \[Product\](https://docs.stripe.com/api/products) that this \[Price\](https://docs.stripe.com/api/prices) will belong to.
    #[must_use]
    pub fn product(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "product".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// These fields can be used to create a new product that this price will belong to.
    #[must_use]
    pub fn product_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("product_data".to_owned(), value);
        self
    }

    /// The recurring components of a price such as `interval` and `usage_type`.
    #[must_use]
    pub fn recurring(mut self, value: serde_json::Value) -> Self {
        self.params.insert("recurring".to_owned(), value);
        self
    }

    /// Only required if a \[default tax behavior\](https://docs.stripe.com/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is c…
    #[must_use]
    pub fn tax_behavior(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "tax_behavior".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Each element represents a pricing tier. This parameter requires `billing_scheme` to be set to `tiered`. See also the documentation for `billing_scheme`.
    #[must_use]
    pub fn tiers(mut self, value: serde_json::Value) -> Self {
        self.params.insert("tiers".to_owned(), value);
        self
    }

    /// Defines if the tiering price should be `graduated` or `volume` based. In `volume`-based tiering, the maximum quantity within a period determines the per unit price, in `graduated` tiering pricing can successively change as the quantity gro…
    #[must_use]
    pub fn tiers_mode(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "tiers_mode".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// If set to true, will atomically remove the lookup key from the existing price, and assign it to this price.
    #[must_use]
    pub fn transfer_lookup_key(mut self, value: bool) -> Self {
        self.params.insert(
            "transfer_lookup_key".to_owned(),
            serde_json::Value::Bool(value),
        );
        self
    }

    /// Apply a transformation to the reported usage or set quantity before computing the billed price. Cannot be combined with `tiers`.
    #[must_use]
    pub fn transform_quantity(mut self, value: serde_json::Value) -> Self {
        self.params.insert("transform_quantity".to_owned(), value);
        self
    }

    /// A positive integer in cents (or local equivalent) (or 0 for a free price) representing how much to charge. One of `unit_amount`, `unit_amount_decimal`, or `custom_unit_amount` is required, unless `billing_scheme=tiered`.
    #[must_use]
    pub fn unit_amount(mut self, value: i64) -> Self {
        self.params
            .insert("unit_amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Same as `unit_amount`, but accepts a decimal value in cents (or local equivalent) with at most 12 decimal places. Only one of `unit_amount` and `unit_amount_decimal` can be set.
    #[must_use]
    pub fn unit_amount_decimal(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "unit_amount_decimal".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Price, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/prices/search`
///
/// Search for prices you’ve previously created using Stripe’s Search Query Language. Don’t use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages. Search functionality is not available to merchants in India.
#[derive(Debug, Clone)]
pub struct GetPricesSearch {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetPricesSearch {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(query: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("query".to_owned(), serde_json::Value::String(query.into()));
        Self {
            path: "/v1/prices/search".to_owned(),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
    #[must_use]
    pub fn page(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("page".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Price> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::Price>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/prices/{price}`
///
/// Retrieves the price with the given ID.
#[derive(Debug, Clone)]
pub struct GetPricesPrice {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetPricesPrice {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(price: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let price = price.into();
        Self {
            path: format!("/v1/prices/{price}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Price, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/prices/{price}`
///
/// Updates the specified price by setting the values of the parameters passed. Any parameters not provided are left unchanged.
#[derive(Debug, Clone)]
pub struct PostPricesPrice {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPricesPrice {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(price: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let price = price.into();
        Self {
            path: format!("/v1/prices/{price}"),
            params,
        }
    }

    /// Whether the price can be used for new purchases. Defaults to `true`.
    #[must_use]
    pub fn active(mut self, value: bool) -> Self {
        self.params
            .insert("active".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Prices defined in each available currency option. Each key must be a three-letter \[ISO currency code\](https://www.iso.org/iso-4217-currency-codes.html) and a \[supported currency\](https://stripe.com/docs/currencies).
    #[must_use]
    pub fn currency_options(mut self, value: serde_json::Value) -> Self {
        self.params.insert("currency_options".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A lookup key used to retrieve prices dynamically from a static string. This may be up to 200 characters.
    #[must_use]
    pub fn lookup_key(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "lookup_key".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// A brief description of the price, hidden from customers.
    #[must_use]
    pub fn nickname(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "nickname".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Only required if a \[default tax behavior\](https://docs.stripe.com/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is c…
    #[must_use]
    pub fn tax_behavior(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "tax_behavior".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// If set to true, will atomically remove the lookup key from the existing price, and assign it to this price.
    #[must_use]
    pub fn transfer_lookup_key(mut self, value: bool) -> Self {
        self.params.insert(
            "transfer_lookup_key".to_owned(),
            serde_json::Value::Bool(value),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Price, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
