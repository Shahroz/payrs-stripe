//! Generated operations for the `test_helpers` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(clippy::doc_markdown, clippy::too_many_arguments, clippy::new_without_default, rustdoc::bare_urls)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `POST /v1/test_helpers/confirmation_tokens`
///
/// Creates a test mode Confirmation Token server side for your integration tests.
#[derive(Debug, Clone)]
pub struct PostTestHelpersConfirmationTokens {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersConfirmationTokens {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/test_helpers/confirmation_tokens".to_owned(), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// ID of an existing PaymentMethod.
    #[must_use]
    pub fn payment_method(mut self, value: impl Into<String>) -> Self {
        self.params.insert("payment_method".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// If provided, this hash will be used to create a PaymentMethod.
    #[must_use]
    pub fn payment_method_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("payment_method_data".to_owned(), value);
        self
    }

    /// Payment-method-specific configuration for this ConfirmationToken.
    #[must_use]
    pub fn payment_method_options(mut self, value: serde_json::Value) -> Self {
        self.params.insert("payment_method_options".to_owned(), value);
        self
    }

    /// Return URL used to confirm the Intent.
    #[must_use]
    pub fn return_url(mut self, value: impl Into<String>) -> Self {
        self.params.insert("return_url".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Indicates that you intend to make future payments with this ConfirmationToken's payment method. The presence of this property will \[attach the payment method\](https://docs.stripe.com/payments/save-during-payment) to the PaymentIntent's C…
    #[must_use]
    pub fn setup_future_usage(mut self, value: impl Into<String>) -> Self {
        self.params.insert("setup_future_usage".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Shipping information for this ConfirmationToken.
    #[must_use]
    pub fn shipping(mut self, value: serde_json::Value) -> Self {
        self.params.insert("shipping".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::ConfirmationToken, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/customers/{customer}/fund_cash_balance`
///
/// Create an incoming testmode bank transfer
#[derive(Debug, Clone)]
pub struct PostTestHelpersCustomersCustomerFundCashBalance {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersCustomersCustomerFundCashBalance {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, amount: i64, currency: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        let customer = customer.into();
        params.insert("amount".to_owned(), serde_json::Value::from(amount));
        params.insert("currency".to_owned(), serde_json::Value::String(currency.into()));
        Self { path: format!("/v1/test_helpers/customers/{customer}/fund_cash_balance"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A description of the test funding. This simulates free-text references supplied by customers when making bank transfers to their cash balance. You can use this to test how Stripe's \[reconciliation algorithm\](https://docs.stripe.com/payme…
    #[must_use]
    pub fn reference(mut self, value: impl Into<String>) -> Self {
        self.params.insert("reference".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::CustomerCashBalanceTransaction, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/authorizations`
///
/// Create a test-mode authorization.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingAuthorizations {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingAuthorizations {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(card: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("card".to_owned(), serde_json::Value::String(card.into()));
        Self { path: "/v1/test_helpers/issuing/authorizations".to_owned(), params }
    }

    /// The total amount to attempt to authorize. This amount is in the provided currency, or defaults to the card's currency, and in the \[smallest currency unit\](https://docs.stripe.com/currencies#zero-decimal).
    #[must_use]
    pub fn amount(mut self, value: i64) -> Self {
        self.params.insert("amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Detailed breakdown of amount components. These amounts are denominated in `currency` and in the \[smallest currency unit\](https://docs.stripe.com/currencies#zero-decimal).
    #[must_use]
    pub fn amount_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("amount_details".to_owned(), value);
        self
    }

    /// How the card details were provided. Defaults to online.
    #[must_use]
    pub fn authorization_method(mut self, value: impl Into<String>) -> Self {
        self.params.insert("authorization_method".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The currency of the authorization. If not provided, defaults to the currency of the card. Three-letter \[ISO currency code\](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a \[supported currency\](https://stripe.c…
    #[must_use]
    pub fn currency(mut self, value: impl Into<String>) -> Self {
        self.params.insert("currency".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Fleet-specific information for authorizations using Fleet cards.
    #[must_use]
    pub fn fleet(mut self, value: serde_json::Value) -> Self {
        self.params.insert("fleet".to_owned(), value);
        self
    }

    /// Probability that this transaction can be disputed in the event of fraud. Assessed by comparing the characteristics of the authorization to card network rules.
    #[must_use]
    pub fn fraud_disputability_likelihood(mut self, value: impl Into<String>) -> Self {
        self.params.insert("fraud_disputability_likelihood".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Information about fuel that was purchased with this transaction.
    #[must_use]
    pub fn fuel(mut self, value: serde_json::Value) -> Self {
        self.params.insert("fuel".to_owned(), value);
        self
    }

    /// If set `true`, you may provide \[amount\](https://docs.stripe.com/api/issuing/authorizations/approve#approve_issuing_authorization-amount) to control how much to hold for the authorization.
    #[must_use]
    pub fn is_amount_controllable(mut self, value: bool) -> Self {
        self.params.insert("is_amount_controllable".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// The total amount to attempt to authorize. This amount is in the provided merchant currency, and in the \[smallest currency unit\](https://docs.stripe.com/currencies#zero-decimal).
    #[must_use]
    pub fn merchant_amount(mut self, value: i64) -> Self {
        self.params.insert("merchant_amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// The currency of the authorization. If not provided, defaults to the currency of the card. Three-letter \[ISO currency code\](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a \[supported currency\](https://stripe.c…
    #[must_use]
    pub fn merchant_currency(mut self, value: impl Into<String>) -> Self {
        self.params.insert("merchant_currency".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Details about the seller (grocery store, e-commerce website, etc.) where the card authorization happened.
    #[must_use]
    pub fn merchant_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("merchant_data".to_owned(), value);
        self
    }

    /// Details about the authorization, such as identifiers, set by the card network.
    #[must_use]
    pub fn network_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("network_data".to_owned(), value);
        self
    }

    /// Stripe’s assessment of the fraud risk for this authorization.
    #[must_use]
    pub fn risk_assessment(mut self, value: serde_json::Value) -> Self {
        self.params.insert("risk_assessment".to_owned(), value);
        self
    }

    /// Verifications that Stripe performed on information that the cardholder provided to the merchant.
    #[must_use]
    pub fn verification_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("verification_data".to_owned(), value);
        self
    }

    /// The digital wallet used for this transaction. One of `apple_pay`, `google_pay`, or `samsung_pay`. Will populate as `null` when no digital wallet was utilized.
    #[must_use]
    pub fn wallet(mut self, value: impl Into<String>) -> Self {
        self.params.insert("wallet".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingAuthorization, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/authorizations/{authorization}/capture`
///
/// Capture a test-mode authorization.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingAuthorizationsAuthorizationCapture {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingAuthorizationsAuthorizationCapture {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(authorization: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let authorization = authorization.into();
        Self { path: format!("/v1/test_helpers/issuing/authorizations/{authorization}/capture"), params }
    }

    /// The amount to capture from the authorization. If not provided, the full amount of the authorization will be captured. This amount is in the authorization currency and in the \[smallest currency unit\](https://docs.stripe.com/currencies#zer…
    #[must_use]
    pub fn capture_amount(mut self, value: i64) -> Self {
        self.params.insert("capture_amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Whether to close the authorization after capture. Defaults to true. Set to false to enable multi-capture flows.
    #[must_use]
    pub fn close_authorization(mut self, value: bool) -> Self {
        self.params.insert("close_authorization".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Additional purchase information that is optionally provided by the merchant.
    #[must_use]
    pub fn purchase_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("purchase_details".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingAuthorization, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/authorizations/{authorization}/expire`
///
/// Expire a test-mode Authorization.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingAuthorizationsAuthorizationExpire {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingAuthorizationsAuthorizationExpire {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(authorization: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let authorization = authorization.into();
        Self { path: format!("/v1/test_helpers/issuing/authorizations/{authorization}/expire"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingAuthorization, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/authorizations/{authorization}/finalize_amount`
///
/// Finalize the amount on an Authorization prior to capture, when the initial authorization was for an estimated amount.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingAuthorizationsAuthorizationFinalizeAmount {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingAuthorizationsAuthorizationFinalizeAmount {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(authorization: impl Into<String>, final_amount: i64) -> Self {
        let mut params = serde_json::Map::new();
        let authorization = authorization.into();
        params.insert("final_amount".to_owned(), serde_json::Value::from(final_amount));
        Self { path: format!("/v1/test_helpers/issuing/authorizations/{authorization}/finalize_amount"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Fleet-specific information for authorizations using Fleet cards.
    #[must_use]
    pub fn fleet(mut self, value: serde_json::Value) -> Self {
        self.params.insert("fleet".to_owned(), value);
        self
    }

    /// Information about fuel that was purchased with this transaction.
    #[must_use]
    pub fn fuel(mut self, value: serde_json::Value) -> Self {
        self.params.insert("fuel".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingAuthorization, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/authorizations/{authorization}/fraud_challenges/respond`
///
/// Respond to a fraud challenge on a testmode Issuing authorization, simulating either a confirmation of fraud or a correction of legitimacy.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingAuthorizationsAuthorizationFraudChallengesRespond {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingAuthorizationsAuthorizationFraudChallengesRespond {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(authorization: impl Into<String>, confirmed: bool) -> Self {
        let mut params = serde_json::Map::new();
        let authorization = authorization.into();
        params.insert("confirmed".to_owned(), serde_json::Value::Bool(confirmed));
        Self { path: format!("/v1/test_helpers/issuing/authorizations/{authorization}/fraud_challenges/respond"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingAuthorization, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/authorizations/{authorization}/increment`
///
/// Increment a test-mode Authorization.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingAuthorizationsAuthorizationIncrement {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingAuthorizationsAuthorizationIncrement {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(authorization: impl Into<String>, increment_amount: i64) -> Self {
        let mut params = serde_json::Map::new();
        let authorization = authorization.into();
        params.insert("increment_amount".to_owned(), serde_json::Value::from(increment_amount));
        Self { path: format!("/v1/test_helpers/issuing/authorizations/{authorization}/increment"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// If set `true`, you may provide \[amount\](https://docs.stripe.com/api/issuing/authorizations/approve#approve_issuing_authorization-amount) to control how much to hold for the authorization.
    #[must_use]
    pub fn is_amount_controllable(mut self, value: bool) -> Self {
        self.params.insert("is_amount_controllable".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingAuthorization, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/authorizations/{authorization}/reverse`
///
/// Reverse a test-mode Authorization.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingAuthorizationsAuthorizationReverse {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingAuthorizationsAuthorizationReverse {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(authorization: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let authorization = authorization.into();
        Self { path: format!("/v1/test_helpers/issuing/authorizations/{authorization}/reverse"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// The amount to reverse from the authorization. If not provided, the full amount of the authorization will be reversed. This amount is in the authorization currency and in the \[smallest currency unit\](https://docs.stripe.com/currencies#zer…
    #[must_use]
    pub fn reverse_amount(mut self, value: i64) -> Self {
        self.params.insert("reverse_amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingAuthorization, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/cards/{card}/shipping/deliver`
///
/// Updates the shipping status of the specified Issuing Card object to delivered.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingCardsCardShippingDeliver {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingCardsCardShippingDeliver {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(card: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let card = card.into();
        Self { path: format!("/v1/test_helpers/issuing/cards/{card}/shipping/deliver"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingCard, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/cards/{card}/shipping/fail`
///
/// Updates the shipping status of the specified Issuing Card object to failure.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingCardsCardShippingFail {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingCardsCardShippingFail {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(card: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let card = card.into();
        Self { path: format!("/v1/test_helpers/issuing/cards/{card}/shipping/fail"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingCard, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/cards/{card}/shipping/return`
///
/// Updates the shipping status of the specified Issuing Card object to returned.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingCardsCardShippingReturn {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingCardsCardShippingReturn {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(card: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let card = card.into();
        Self { path: format!("/v1/test_helpers/issuing/cards/{card}/shipping/return"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingCard, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/cards/{card}/shipping/ship`
///
/// Updates the shipping status of the specified Issuing Card object to shipped.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingCardsCardShippingShip {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingCardsCardShippingShip {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(card: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let card = card.into();
        Self { path: format!("/v1/test_helpers/issuing/cards/{card}/shipping/ship"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingCard, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/cards/{card}/shipping/submit`
///
/// Updates the shipping status of the specified Issuing Card object to submitted. This method requires Stripe Version ‘2024-09-30.acacia’ or later.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingCardsCardShippingSubmit {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingCardsCardShippingSubmit {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(card: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let card = card.into();
        Self { path: format!("/v1/test_helpers/issuing/cards/{card}/shipping/submit"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingCard, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/personalization_designs/{personalization_design}/activate`
///
/// Updates the status of the specified testmode personalization design object to active.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingPersonalizationDesignsPersonalizationDesignActivate {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingPersonalizationDesignsPersonalizationDesignActivate {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(personalization_design: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let personalization_design = personalization_design.into();
        Self { path: format!("/v1/test_helpers/issuing/personalization_designs/{personalization_design}/activate"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingPersonalizationDesign, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/personalization_designs/{personalization_design}/deactivate`
///
/// Updates the status of the specified testmode personalization design object to inactive.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingPersonalizationDesignsPersonalizationDesignDeactivate {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingPersonalizationDesignsPersonalizationDesignDeactivate {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(personalization_design: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let personalization_design = personalization_design.into();
        Self { path: format!("/v1/test_helpers/issuing/personalization_designs/{personalization_design}/deactivate"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingPersonalizationDesign, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/personalization_designs/{personalization_design}/reject`
///
/// Updates the status of the specified testmode personalization design object to rejected.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingPersonalizationDesignsPersonalizationDesignReject {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingPersonalizationDesignsPersonalizationDesignReject {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(personalization_design: impl Into<String>, rejection_reasons: serde_json::Value) -> Self {
        let mut params = serde_json::Map::new();
        let personalization_design = personalization_design.into();
        params.insert("rejection_reasons".to_owned(), rejection_reasons);
        Self { path: format!("/v1/test_helpers/issuing/personalization_designs/{personalization_design}/reject"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingPersonalizationDesign, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/settlements`
///
/// Allows the user to create an Issuing settlement.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingSettlements {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingSettlements {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(bin: impl Into<String>, clearing_date: i64, currency: impl Into<String>, net_total_amount: i64) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("bin".to_owned(), serde_json::Value::String(bin.into()));
        params.insert("clearing_date".to_owned(), serde_json::Value::from(clearing_date));
        params.insert("currency".to_owned(), serde_json::Value::String(currency.into()));
        params.insert("net_total_amount".to_owned(), serde_json::Value::from(net_total_amount));
        Self { path: "/v1/test_helpers/issuing/settlements".to_owned(), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// The total interchange received as reimbursement for the transactions.
    #[must_use]
    pub fn interchange_fees_amount(mut self, value: i64) -> Self {
        self.params.insert("interchange_fees_amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// The card network for this settlement. One of \["visa", "maestro", "mastercard"\]
    #[must_use]
    pub fn network(mut self, value: impl Into<String>) -> Self {
        self.params.insert("network".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The Settlement Identification Number assigned by the network.
    #[must_use]
    pub fn network_settlement_identifier(mut self, value: impl Into<String>) -> Self {
        self.params.insert("network_settlement_identifier".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The total transaction amount reflected in this settlement.
    #[must_use]
    pub fn transaction_amount(mut self, value: i64) -> Self {
        self.params.insert("transaction_amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// The total number of transactions reflected in this settlement.
    #[must_use]
    pub fn transaction_count(mut self, value: i64) -> Self {
        self.params.insert("transaction_count".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingSettlement, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/settlements/{settlement}/complete`
///
/// Allows the user to mark an Issuing settlement as complete.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingSettlementsSettlementComplete {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingSettlementsSettlementComplete {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(settlement: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let settlement = settlement.into();
        Self { path: format!("/v1/test_helpers/issuing/settlements/{settlement}/complete"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingSettlement, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/transactions/create_force_capture`
///
/// Allows the user to capture an arbitrary amount, also known as a forced capture.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingTransactionsCreateForceCapture {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingTransactionsCreateForceCapture {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(amount: i64, card: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("amount".to_owned(), serde_json::Value::from(amount));
        params.insert("card".to_owned(), serde_json::Value::String(card.into()));
        Self { path: "/v1/test_helpers/issuing/transactions/create_force_capture".to_owned(), params }
    }

    /// The currency of the capture. If not provided, defaults to the currency of the card. Three-letter \[ISO currency code\](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a \[supported currency\](https://stripe.com/doc…
    #[must_use]
    pub fn currency(mut self, value: impl Into<String>) -> Self {
        self.params.insert("currency".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Details about the seller (grocery store, e-commerce website, etc.) where the card authorization happened.
    #[must_use]
    pub fn merchant_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("merchant_data".to_owned(), value);
        self
    }

    /// Additional purchase information that is optionally provided by the merchant.
    #[must_use]
    pub fn purchase_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("purchase_details".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingTransaction, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/transactions/create_unlinked_refund`
///
/// Allows the user to refund an arbitrary amount, also known as a unlinked refund.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingTransactionsCreateUnlinkedRefund {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingTransactionsCreateUnlinkedRefund {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(amount: i64, card: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("amount".to_owned(), serde_json::Value::from(amount));
        params.insert("card".to_owned(), serde_json::Value::String(card.into()));
        Self { path: "/v1/test_helpers/issuing/transactions/create_unlinked_refund".to_owned(), params }
    }

    /// The currency of the unlinked refund. If not provided, defaults to the currency of the card. Three-letter \[ISO currency code\](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a \[supported currency\](https://stripe…
    #[must_use]
    pub fn currency(mut self, value: impl Into<String>) -> Self {
        self.params.insert("currency".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Details about the seller (grocery store, e-commerce website, etc.) where the card authorization happened.
    #[must_use]
    pub fn merchant_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("merchant_data".to_owned(), value);
        self
    }

    /// Additional purchase information that is optionally provided by the merchant.
    #[must_use]
    pub fn purchase_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("purchase_details".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingTransaction, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/issuing/transactions/{transaction}/refund`
///
/// Refund a test-mode Transaction.
#[derive(Debug, Clone)]
pub struct PostTestHelpersIssuingTransactionsTransactionRefund {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersIssuingTransactionsTransactionRefund {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(transaction: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let transaction = transaction.into();
        Self { path: format!("/v1/test_helpers/issuing/transactions/{transaction}/refund"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// The total amount to attempt to refund. This amount is in the provided currency, or defaults to the cards currency, and in the \[smallest currency unit\](https://docs.stripe.com/currencies#zero-decimal).
    #[must_use]
    pub fn refund_amount(mut self, value: i64) -> Self {
        self.params.insert("refund_amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingTransaction, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/refunds/{refund}/expire`
///
/// Expire a refund with a status of requires_action.
#[derive(Debug, Clone)]
pub struct PostTestHelpersRefundsRefundExpire {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersRefundsRefundExpire {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(refund: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let refund = refund.into();
        Self { path: format!("/v1/test_helpers/refunds/{refund}/expire"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Refund, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/terminal/readers/{reader}/present_payment_method`
///
/// Presents a payment method on a simulated reader. Can be used to simulate accepting a payment, saving a card or refunding a transaction.
#[derive(Debug, Clone)]
pub struct PostTestHelpersTerminalReadersReaderPresentPaymentMethod {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersTerminalReadersReaderPresentPaymentMethod {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(reader: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let reader = reader.into();
        Self { path: format!("/v1/test_helpers/terminal/readers/{reader}/present_payment_method"), params }
    }

    /// Simulated on-reader tip amount.
    #[must_use]
    pub fn amount_tip(mut self, value: i64) -> Self {
        self.params.insert("amount_tip".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Simulated data for the card payment method.
    #[must_use]
    pub fn card(mut self, value: serde_json::Value) -> Self {
        self.params.insert("card".to_owned(), value);
        self
    }

    /// Simulated data for the card_present payment method.
    #[must_use]
    pub fn card_present(mut self, value: serde_json::Value) -> Self {
        self.params.insert("card_present".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Simulated data for the interac_present payment method.
    #[must_use]
    pub fn interac_present(mut self, value: serde_json::Value) -> Self {
        self.params.insert("interac_present".to_owned(), value);
        self
    }

    /// Simulated payment type.
    #[must_use]
    pub fn type_(mut self, value: impl Into<String>) -> Self {
        self.params.insert("type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TerminalReader, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/terminal/readers/{reader}/succeed_input_collection`
///
/// Use this endpoint to trigger a successful input collection on a simulated reader.
#[derive(Debug, Clone)]
pub struct PostTestHelpersTerminalReadersReaderSucceedInputCollection {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersTerminalReadersReaderSucceedInputCollection {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(reader: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let reader = reader.into();
        Self { path: format!("/v1/test_helpers/terminal/readers/{reader}/succeed_input_collection"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// This parameter defines the skip behavior for input collection.
    #[must_use]
    pub fn skip_non_required_inputs(mut self, value: impl Into<String>) -> Self {
        self.params.insert("skip_non_required_inputs".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TerminalReader, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/terminal/readers/{reader}/timeout_input_collection`
///
/// Use this endpoint to complete an input collection with a timeout error on a simulated reader.
#[derive(Debug, Clone)]
pub struct PostTestHelpersTerminalReadersReaderTimeoutInputCollection {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersTerminalReadersReaderTimeoutInputCollection {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(reader: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let reader = reader.into();
        Self { path: format!("/v1/test_helpers/terminal/readers/{reader}/timeout_input_collection"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TerminalReader, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/test_helpers/test_clocks`
///
/// Returns a list of your test clocks.
#[derive(Debug, Clone)]
pub struct GetTestHelpersTestClocks {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetTestHelpersTestClocks {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/test_helpers/test_clocks".to_owned(), params }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::TestHelpersTestClock> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::TestHelpersTestClock>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/test_clocks`
///
/// Creates a new test clock that can be attached to new customers and quotes.
#[derive(Debug, Clone)]
pub struct PostTestHelpersTestClocks {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersTestClocks {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(frozen_time: i64) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("frozen_time".to_owned(), serde_json::Value::from(frozen_time));
        Self { path: "/v1/test_helpers/test_clocks".to_owned(), params }
    }

    /// Existing customer this test clock will be attached to. Once attached, customers can't be removed from a test clock.
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// The name for this test clock.
    #[must_use]
    pub fn name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TestHelpersTestClock, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/test_helpers/test_clocks/{test_clock}`
///
/// Retrieves a test clock.
#[derive(Debug, Clone)]
pub struct GetTestHelpersTestClocksTestClock {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetTestHelpersTestClocksTestClock {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(test_clock: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let test_clock = test_clock.into();
        Self { path: format!("/v1/test_helpers/test_clocks/{test_clock}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TestHelpersTestClock, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `DELETE /v1/test_helpers/test_clocks/{test_clock}`
///
/// Deletes a test clock.
#[derive(Debug, Clone)]
pub struct DeleteTestHelpersTestClocksTestClock {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteTestHelpersTestClocksTestClock {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(test_clock: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let test_clock = test_clock.into();
        Self { path: format!("/v1/test_helpers/test_clocks/{test_clock}"), params }
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::DeletedTestHelpersTestClock, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/test_clocks/{test_clock}/advance`
///
/// Starts advancing a test clock to a specified time in the future. Advancement is done when status changes to Ready.
#[derive(Debug, Clone)]
pub struct PostTestHelpersTestClocksTestClockAdvance {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersTestClocksTestClockAdvance {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(test_clock: impl Into<String>, frozen_time: i64) -> Self {
        let mut params = serde_json::Map::new();
        let test_clock = test_clock.into();
        params.insert("frozen_time".to_owned(), serde_json::Value::from(frozen_time));
        Self { path: format!("/v1/test_helpers/test_clocks/{test_clock}/advance"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TestHelpersTestClock, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/treasury/inbound_transfers/{id}/fail`
///
/// Transitions a test mode created InboundTransfer to the failed status. The InboundTransfer must already be in the processing state.
#[derive(Debug, Clone)]
pub struct PostTestHelpersTreasuryInboundTransfersIdFail {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersTreasuryInboundTransfersIdFail {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let id = id.into();
        Self { path: format!("/v1/test_helpers/treasury/inbound_transfers/{id}/fail"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Details about a failed InboundTransfer.
    #[must_use]
    pub fn failure_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("failure_details".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TreasuryInboundTransfer, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/treasury/inbound_transfers/{id}/return`
///
/// Marks the test mode InboundTransfer object as returned and links the InboundTransfer to a ReceivedDebit. The InboundTransfer must already be in the succeeded state.
#[derive(Debug, Clone)]
pub struct PostTestHelpersTreasuryInboundTransfersIdReturn {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersTreasuryInboundTransfersIdReturn {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let id = id.into();
        Self { path: format!("/v1/test_helpers/treasury/inbound_transfers/{id}/return"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TreasuryInboundTransfer, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/treasury/inbound_transfers/{id}/succeed`
///
/// Transitions a test mode created InboundTransfer to the succeeded status. The InboundTransfer must already be in the processing state.
#[derive(Debug, Clone)]
pub struct PostTestHelpersTreasuryInboundTransfersIdSucceed {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersTreasuryInboundTransfersIdSucceed {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let id = id.into();
        Self { path: format!("/v1/test_helpers/treasury/inbound_transfers/{id}/succeed"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TreasuryInboundTransfer, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/treasury/outbound_payments/{id}`
///
/// Updates a test mode created OutboundPayment with tracking details. The OutboundPayment must not be cancelable, and cannot be in the canceled or failed states.
#[derive(Debug, Clone)]
pub struct PostTestHelpersTreasuryOutboundPaymentsId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersTreasuryOutboundPaymentsId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>, tracking_details: serde_json::Value) -> Self {
        let mut params = serde_json::Map::new();
        let id = id.into();
        params.insert("tracking_details".to_owned(), tracking_details);
        Self { path: format!("/v1/test_helpers/treasury/outbound_payments/{id}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TreasuryOutboundPayment, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/treasury/outbound_payments/{id}/fail`
///
/// Transitions a test mode created OutboundPayment to the failed status. The OutboundPayment must already be in the processing state.
#[derive(Debug, Clone)]
pub struct PostTestHelpersTreasuryOutboundPaymentsIdFail {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersTreasuryOutboundPaymentsIdFail {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let id = id.into();
        Self { path: format!("/v1/test_helpers/treasury/outbound_payments/{id}/fail"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TreasuryOutboundPayment, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/treasury/outbound_payments/{id}/post`
///
/// Transitions a test mode created OutboundPayment to the posted status. The OutboundPayment must already be in the processing state.
#[derive(Debug, Clone)]
pub struct PostTestHelpersTreasuryOutboundPaymentsIdPost {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersTreasuryOutboundPaymentsIdPost {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let id = id.into();
        Self { path: format!("/v1/test_helpers/treasury/outbound_payments/{id}/post"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TreasuryOutboundPayment, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/treasury/outbound_payments/{id}/return`
///
/// Transitions a test mode created OutboundPayment to the returned status. The OutboundPayment must already be in the processing state.
#[derive(Debug, Clone)]
pub struct PostTestHelpersTreasuryOutboundPaymentsIdReturn {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersTreasuryOutboundPaymentsIdReturn {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let id = id.into();
        Self { path: format!("/v1/test_helpers/treasury/outbound_payments/{id}/return"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Optional hash to set the return code.
    #[must_use]
    pub fn returned_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("returned_details".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TreasuryOutboundPayment, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}`
///
/// Updates a test mode created OutboundTransfer with tracking details. The OutboundTransfer must not be cancelable, and cannot be in the canceled or failed states.
#[derive(Debug, Clone)]
pub struct PostTestHelpersTreasuryOutboundTransfersOutboundTransfer {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersTreasuryOutboundTransfersOutboundTransfer {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(outbound_transfer: impl Into<String>, tracking_details: serde_json::Value) -> Self {
        let mut params = serde_json::Map::new();
        let outbound_transfer = outbound_transfer.into();
        params.insert("tracking_details".to_owned(), tracking_details);
        Self { path: format!("/v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TreasuryOutboundTransfer, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/fail`
///
/// Transitions a test mode created OutboundTransfer to the failed status. The OutboundTransfer must already be in the processing state.
#[derive(Debug, Clone)]
pub struct PostTestHelpersTreasuryOutboundTransfersOutboundTransferFail {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersTreasuryOutboundTransfersOutboundTransferFail {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(outbound_transfer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let outbound_transfer = outbound_transfer.into();
        Self { path: format!("/v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/fail"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TreasuryOutboundTransfer, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/post`
///
/// Transitions a test mode created OutboundTransfer to the posted status. The OutboundTransfer must already be in the processing state.
#[derive(Debug, Clone)]
pub struct PostTestHelpersTreasuryOutboundTransfersOutboundTransferPost {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersTreasuryOutboundTransfersOutboundTransferPost {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(outbound_transfer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let outbound_transfer = outbound_transfer.into();
        Self { path: format!("/v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/post"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TreasuryOutboundTransfer, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/return`
///
/// Transitions a test mode created OutboundTransfer to the returned status. The OutboundTransfer must already be in the processing state.
#[derive(Debug, Clone)]
pub struct PostTestHelpersTreasuryOutboundTransfersOutboundTransferReturn {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersTreasuryOutboundTransfersOutboundTransferReturn {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(outbound_transfer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let outbound_transfer = outbound_transfer.into();
        Self { path: format!("/v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/return"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Details about a returned OutboundTransfer.
    #[must_use]
    pub fn returned_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("returned_details".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TreasuryOutboundTransfer, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/treasury/received_credits`
///
/// Use this endpoint to simulate a test mode ReceivedCredit initiated by a third party. In live mode, you can’t directly create ReceivedCredits initiated by third parties.
#[derive(Debug, Clone)]
pub struct PostTestHelpersTreasuryReceivedCredits {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersTreasuryReceivedCredits {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(amount: i64, currency: impl Into<String>, financial_account: impl Into<String>, network: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("amount".to_owned(), serde_json::Value::from(amount));
        params.insert("currency".to_owned(), serde_json::Value::String(currency.into()));
        params.insert("financial_account".to_owned(), serde_json::Value::String(financial_account.into()));
        params.insert("network".to_owned(), serde_json::Value::String(network.into()));
        Self { path: "/v1/test_helpers/treasury/received_credits".to_owned(), params }
    }

    /// An arbitrary string attached to the object. Often useful for displaying to users.
    #[must_use]
    pub fn description(mut self, value: impl Into<String>) -> Self {
        self.params.insert("description".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Initiating payment method details for the object.
    #[must_use]
    pub fn initiating_payment_method_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("initiating_payment_method_details".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TreasuryReceivedCredit, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/test_helpers/treasury/received_debits`
///
/// Use this endpoint to simulate a test mode ReceivedDebit initiated by a third party. In live mode, you can’t directly create ReceivedDebits initiated by third parties.
#[derive(Debug, Clone)]
pub struct PostTestHelpersTreasuryReceivedDebits {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostTestHelpersTreasuryReceivedDebits {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(amount: i64, currency: impl Into<String>, financial_account: impl Into<String>, network: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("amount".to_owned(), serde_json::Value::from(amount));
        params.insert("currency".to_owned(), serde_json::Value::String(currency.into()));
        params.insert("financial_account".to_owned(), serde_json::Value::String(financial_account.into()));
        params.insert("network".to_owned(), serde_json::Value::String(network.into()));
        Self { path: "/v1/test_helpers/treasury/received_debits".to_owned(), params }
    }

    /// An arbitrary string attached to the object. Often useful for displaying to users.
    #[must_use]
    pub fn description(mut self, value: impl Into<String>) -> Self {
        self.params.insert("description".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Initiating payment method details for the object.
    #[must_use]
    pub fn initiating_payment_method_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("initiating_payment_method_details".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TreasuryReceivedDebit, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
