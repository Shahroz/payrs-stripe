//! Generated operations for the `payment_records` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(
    clippy::doc_markdown,
    clippy::too_many_arguments,
    clippy::new_without_default,
    rustdoc::bare_urls
)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `POST /v1/payment_records/report_payment`
///
/// Report a new Payment Record. You may report a Payment Record as it is initialized and later report updates through the other report_* methods, or report Payment Records in a terminal state directly, through this method.
#[derive(Debug, Clone)]
pub struct PostPaymentRecordsReportPayment {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPaymentRecordsReportPayment {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(
        amount_requested: serde_json::Value,
        initiated_at: i64,
        payment_method_details: serde_json::Value,
    ) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("amount_requested".to_owned(), amount_requested);
        params.insert(
            "initiated_at".to_owned(),
            serde_json::Value::from(initiated_at),
        );
        params.insert("payment_method_details".to_owned(), payment_method_details);
        Self {
            path: "/v1/payment_records/report_payment".to_owned(),
            params,
        }
    }

    /// Customer information for this payment.
    #[must_use]
    pub fn customer_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("customer_details".to_owned(), value);
        self
    }

    /// Indicates whether the customer was present in your checkout flow during this payment.
    #[must_use]
    pub fn customer_presence(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "customer_presence".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// An arbitrary string attached to the object. Often useful for displaying to users.
    #[must_use]
    pub fn description(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "description".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Information about the payment attempt failure.
    #[must_use]
    pub fn failed(mut self, value: serde_json::Value) -> Self {
        self.params.insert("failed".to_owned(), value);
        self
    }

    /// Information about the payment attempt guarantee.
    #[must_use]
    pub fn guaranteed(mut self, value: serde_json::Value) -> Self {
        self.params.insert("guaranteed".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// The outcome of the reported payment.
    #[must_use]
    pub fn outcome(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "outcome".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Processor information for this payment.
    #[must_use]
    pub fn processor_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("processor_details".to_owned(), value);
        self
    }

    /// Shipping information for this payment.
    #[must_use]
    pub fn shipping_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("shipping_details".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::PaymentRecord, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/payment_records/{id}`
///
/// Retrieves a Payment Record with the given ID
#[derive(Debug, Clone)]
pub struct GetPaymentRecordsId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetPaymentRecordsId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let id = id.into();
        Self {
            path: format!("/v1/payment_records/{id}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::PaymentRecord, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/payment_records/{id}/report_payment_attempt`
///
/// Report a new payment attempt on the specified Payment Record. A new payment attempt can only be specified if all other payment attempts are canceled or failed.
#[derive(Debug, Clone)]
pub struct PostPaymentRecordsIdReportPaymentAttempt {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPaymentRecordsIdReportPaymentAttempt {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>, initiated_at: i64) -> Self {
        let mut params = serde_json::Map::new();
        let id = id.into();
        params.insert(
            "initiated_at".to_owned(),
            serde_json::Value::from(initiated_at),
        );
        Self {
            path: format!("/v1/payment_records/{id}/report_payment_attempt"),
            params,
        }
    }

    /// An arbitrary string attached to the object. Often useful for displaying to users.
    #[must_use]
    pub fn description(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "description".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Information about the payment attempt failure.
    #[must_use]
    pub fn failed(mut self, value: serde_json::Value) -> Self {
        self.params.insert("failed".to_owned(), value);
        self
    }

    /// Information about the payment attempt guarantee.
    #[must_use]
    pub fn guaranteed(mut self, value: serde_json::Value) -> Self {
        self.params.insert("guaranteed".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// The outcome of the reported payment.
    #[must_use]
    pub fn outcome(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "outcome".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Information about the Payment Method debited for this payment.
    #[must_use]
    pub fn payment_method_details(mut self, value: serde_json::Value) -> Self {
        self.params
            .insert("payment_method_details".to_owned(), value);
        self
    }

    /// Shipping information for this payment.
    #[must_use]
    pub fn shipping_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("shipping_details".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::PaymentRecord, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/payment_records/{id}/report_payment_attempt_canceled`
///
/// Report that the most recent payment attempt on the specified Payment Record was canceled.
#[derive(Debug, Clone)]
pub struct PostPaymentRecordsIdReportPaymentAttemptCanceled {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPaymentRecordsIdReportPaymentAttemptCanceled {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>, canceled_at: i64) -> Self {
        let mut params = serde_json::Map::new();
        let id = id.into();
        params.insert(
            "canceled_at".to_owned(),
            serde_json::Value::from(canceled_at),
        );
        Self {
            path: format!("/v1/payment_records/{id}/report_payment_attempt_canceled"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::PaymentRecord, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/payment_records/{id}/report_payment_attempt_failed`
///
/// Report that the most recent payment attempt on the specified Payment Record failed or errored.
#[derive(Debug, Clone)]
pub struct PostPaymentRecordsIdReportPaymentAttemptFailed {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPaymentRecordsIdReportPaymentAttemptFailed {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>, failed_at: i64) -> Self {
        let mut params = serde_json::Map::new();
        let id = id.into();
        params.insert("failed_at".to_owned(), serde_json::Value::from(failed_at));
        Self {
            path: format!("/v1/payment_records/{id}/report_payment_attempt_failed"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::PaymentRecord, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/payment_records/{id}/report_payment_attempt_guaranteed`
///
/// Report that the most recent payment attempt on the specified Payment Record was guaranteed.
#[derive(Debug, Clone)]
pub struct PostPaymentRecordsIdReportPaymentAttemptGuaranteed {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPaymentRecordsIdReportPaymentAttemptGuaranteed {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>, guaranteed_at: i64) -> Self {
        let mut params = serde_json::Map::new();
        let id = id.into();
        params.insert(
            "guaranteed_at".to_owned(),
            serde_json::Value::from(guaranteed_at),
        );
        Self {
            path: format!("/v1/payment_records/{id}/report_payment_attempt_guaranteed"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::PaymentRecord, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/payment_records/{id}/report_payment_attempt_informational`
///
/// Report informational updates on the specified Payment Record.
#[derive(Debug, Clone)]
pub struct PostPaymentRecordsIdReportPaymentAttemptInformational {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPaymentRecordsIdReportPaymentAttemptInformational {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let id = id.into();
        Self {
            path: format!("/v1/payment_records/{id}/report_payment_attempt_informational"),
            params,
        }
    }

    /// Customer information for this payment.
    #[must_use]
    pub fn customer_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("customer_details".to_owned(), value);
        self
    }

    /// An arbitrary string attached to the object. Often useful for displaying to users.
    #[must_use]
    pub fn description(mut self, value: serde_json::Value) -> Self {
        self.params.insert("description".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Shipping information for this payment.
    #[must_use]
    pub fn shipping_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("shipping_details".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::PaymentRecord, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/payment_records/{id}/report_refund`
///
/// Report that the most recent payment attempt on the specified Payment Record was refunded.
#[derive(Debug, Clone)]
pub struct PostPaymentRecordsIdReportRefund {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPaymentRecordsIdReportRefund {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(
        id: impl Into<String>,
        outcome: impl Into<String>,
        processor_details: serde_json::Value,
    ) -> Self {
        let mut params = serde_json::Map::new();
        let id = id.into();
        params.insert(
            "outcome".to_owned(),
            serde_json::Value::String(outcome.into()),
        );
        params.insert("processor_details".to_owned(), processor_details);
        Self {
            path: format!("/v1/payment_records/{id}/report_refund"),
            params,
        }
    }

    /// A positive integer in the \[smallest currency unit\](https://docs.stripe.com/currencies#zero-decimal) representing how much of this payment to refund. Can refund only up to the remaining, unrefunded amount of the payment.
    #[must_use]
    pub fn amount(mut self, value: serde_json::Value) -> Self {
        self.params.insert("amount".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// When the reported refund was initiated. Measured in seconds since the Unix epoch.
    #[must_use]
    pub fn initiated_at(mut self, value: i64) -> Self {
        self.params
            .insert("initiated_at".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Information about the payment attempt refund.
    #[must_use]
    pub fn refunded(mut self, value: serde_json::Value) -> Self {
        self.params.insert("refunded".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::PaymentRecord, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
