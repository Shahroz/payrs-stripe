//! Generated operations for the `quotes` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(clippy::doc_markdown, clippy::too_many_arguments, clippy::new_without_default, rustdoc::bare_urls)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/quotes`
///
/// Returns a list of your quotes.
#[derive(Debug, Clone)]
pub struct GetQuotes {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetQuotes {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/quotes".to_owned(), params }
    }

    /// The ID of the customer whose quotes you're retrieving.
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The ID of the account representing the customer whose quotes you're retrieving.
    #[must_use]
    pub fn customer_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The status of the quote.
    #[must_use]
    pub fn status(mut self, value: impl Into<String>) -> Self {
        self.params.insert("status".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Provides a list of quotes that are associated with the specified test clock. The response will not include quotes with test clocks if this and the customer parameter is not set.
    #[must_use]
    pub fn test_clock(mut self, value: impl Into<String>) -> Self {
        self.params.insert("test_clock".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Quote> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::Quote>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/quotes`
///
/// A quote models prices and services for a customer. Default options for header, description, footer, and expires_at can be set in the dashboard via the quote template.
#[derive(Debug, Clone)]
pub struct PostQuotes {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostQuotes {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/quotes".to_owned(), params }
    }

    /// The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. There cannot be any line items with recurring prices when using this field.
    #[must_use]
    pub fn application_fee_amount(mut self, value: serde_json::Value) -> Self {
        self.params.insert("application_fee_amount".to_owned(), value);
        self
    }

    /// A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the application owner's Stripe account. There must be at least 1 line i…
    #[must_use]
    pub fn application_fee_percent(mut self, value: serde_json::Value) -> Self {
        self.params.insert("application_fee_percent".to_owned(), value);
        self
    }

    /// Settings for automatic tax lookup for this quote and resulting invoices and subscriptions.
    #[must_use]
    pub fn automatic_tax(mut self, value: serde_json::Value) -> Self {
        self.params.insert("automatic_tax".to_owned(), value);
        self
    }

    /// Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay invoices at the end of the subscription cycle or at invoice finalization using the default payment method attached to the subscriptio…
    #[must_use]
    pub fn collection_method(mut self, value: impl Into<String>) -> Self {
        self.params.insert("collection_method".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The customer for which this quote belongs to. A customer is required before finalizing the quote. Once specified, it cannot be changed.
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The account for which this quote belongs to. A customer or account is required before finalizing the quote. Once specified, it cannot be changed.
    #[must_use]
    pub fn customer_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The tax rates that will apply to any line item that does not have `tax_rates` set.
    #[must_use]
    pub fn default_tax_rates(mut self, value: serde_json::Value) -> Self {
        self.params.insert("default_tax_rates".to_owned(), value);
        self
    }

    /// A description that will be displayed on the quote PDF. If no value is passed, the default description configured in your \[quote template settings\](https://dashboard.stripe.com/settings/billing/quote) will be used.
    #[must_use]
    pub fn description(mut self, value: serde_json::Value) -> Self {
        self.params.insert("description".to_owned(), value);
        self
    }

    /// The discounts applied to the quote.
    #[must_use]
    pub fn discounts(mut self, value: serde_json::Value) -> Self {
        self.params.insert("discounts".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A future timestamp on which the quote will be canceled if in `open` or `draft` status. Measured in seconds since the Unix epoch. If no value is passed, the default expiration date configured in your \[quote template settings\](https://dash…
    #[must_use]
    pub fn expires_at(mut self, value: i64) -> Self {
        self.params.insert("expires_at".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A footer that will be displayed on the quote PDF. If no value is passed, the default footer configured in your \[quote template settings\](https://dashboard.stripe.com/settings/billing/quote) will be used.
    #[must_use]
    pub fn footer(mut self, value: serde_json::Value) -> Self {
        self.params.insert("footer".to_owned(), value);
        self
    }

    /// Clone an existing quote. The new quote will be created in `status=draft`. When using this parameter, you cannot specify any other parameters except for `expires_at`.
    #[must_use]
    pub fn from_quote(mut self, value: serde_json::Value) -> Self {
        self.params.insert("from_quote".to_owned(), value);
        self
    }

    /// A header that will be displayed on the quote PDF. If no value is passed, the default header configured in your \[quote template settings\](https://dashboard.stripe.com/settings/billing/quote) will be used.
    #[must_use]
    pub fn header(mut self, value: serde_json::Value) -> Self {
        self.params.insert("header".to_owned(), value);
        self
    }

    /// All invoices will be billed using the specified settings.
    #[must_use]
    pub fn invoice_settings(mut self, value: serde_json::Value) -> Self {
        self.params.insert("invoice_settings".to_owned(), value);
        self
    }

    /// A list of line items the customer is being quoted for. Each line item includes information about the product, the quantity, and the resulting cost.
    #[must_use]
    pub fn line_items(mut self, value: serde_json::Value) -> Self {
        self.params.insert("line_items".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert("metadata".to_owned(), serde_json::Value::Object(value.into_iter().map(|(k, v)| (k, serde_json::Value::String(v))).collect()));
        self
    }

    /// The account on behalf of which to charge.
    #[must_use]
    pub fn on_behalf_of(mut self, value: serde_json::Value) -> Self {
        self.params.insert("on_behalf_of".to_owned(), value);
        self
    }

    /// When creating a subscription or subscription schedule, the specified configuration data will be used. There must be at least one line item with a recurring price for a subscription or subscription schedule to be created. A subscription sch…
    #[must_use]
    pub fn subscription_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("subscription_data".to_owned(), value);
        self
    }

    /// ID of the test clock to attach to the quote.
    #[must_use]
    pub fn test_clock(mut self, value: impl Into<String>) -> Self {
        self.params.insert("test_clock".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The data with which to automatically create a Transfer for each of the invoices.
    #[must_use]
    pub fn transfer_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("transfer_data".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Quote, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/quotes/{quote}`
///
/// Retrieves the quote with the given ID.
#[derive(Debug, Clone)]
pub struct GetQuotesQuote {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetQuotesQuote {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(quote: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let quote = quote.into();
        Self { path: format!("/v1/quotes/{quote}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Quote, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/quotes/{quote}`
///
/// A quote models prices and services for a customer.
#[derive(Debug, Clone)]
pub struct PostQuotesQuote {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostQuotesQuote {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(quote: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let quote = quote.into();
        Self { path: format!("/v1/quotes/{quote}"), params }
    }

    /// The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. There cannot be any line items with recurring prices when using this field.
    #[must_use]
    pub fn application_fee_amount(mut self, value: serde_json::Value) -> Self {
        self.params.insert("application_fee_amount".to_owned(), value);
        self
    }

    /// A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the application owner's Stripe account. There must be at least 1 line i…
    #[must_use]
    pub fn application_fee_percent(mut self, value: serde_json::Value) -> Self {
        self.params.insert("application_fee_percent".to_owned(), value);
        self
    }

    /// Settings for automatic tax lookup for this quote and resulting invoices and subscriptions.
    #[must_use]
    pub fn automatic_tax(mut self, value: serde_json::Value) -> Self {
        self.params.insert("automatic_tax".to_owned(), value);
        self
    }

    /// Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay invoices at the end of the subscription cycle or at invoice finalization using the default payment method attached to the subscriptio…
    #[must_use]
    pub fn collection_method(mut self, value: impl Into<String>) -> Self {
        self.params.insert("collection_method".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The customer for which this quote belongs to. A customer is required before finalizing the quote. Once specified, it cannot be changed.
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The account for which this quote belongs to. A customer or account is required before finalizing the quote. Once specified, it cannot be changed.
    #[must_use]
    pub fn customer_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The tax rates that will apply to any line item that does not have `tax_rates` set.
    #[must_use]
    pub fn default_tax_rates(mut self, value: serde_json::Value) -> Self {
        self.params.insert("default_tax_rates".to_owned(), value);
        self
    }

    /// A description that will be displayed on the quote PDF.
    #[must_use]
    pub fn description(mut self, value: serde_json::Value) -> Self {
        self.params.insert("description".to_owned(), value);
        self
    }

    /// The discounts applied to the quote.
    #[must_use]
    pub fn discounts(mut self, value: serde_json::Value) -> Self {
        self.params.insert("discounts".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A future timestamp on which the quote will be canceled if in `open` or `draft` status. Measured in seconds since the Unix epoch.
    #[must_use]
    pub fn expires_at(mut self, value: i64) -> Self {
        self.params.insert("expires_at".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A footer that will be displayed on the quote PDF.
    #[must_use]
    pub fn footer(mut self, value: serde_json::Value) -> Self {
        self.params.insert("footer".to_owned(), value);
        self
    }

    /// A header that will be displayed on the quote PDF.
    #[must_use]
    pub fn header(mut self, value: serde_json::Value) -> Self {
        self.params.insert("header".to_owned(), value);
        self
    }

    /// All invoices will be billed using the specified settings.
    #[must_use]
    pub fn invoice_settings(mut self, value: serde_json::Value) -> Self {
        self.params.insert("invoice_settings".to_owned(), value);
        self
    }

    /// A list of line items the customer is being quoted for. Each line item includes information about the product, the quantity, and the resulting cost.
    #[must_use]
    pub fn line_items(mut self, value: serde_json::Value) -> Self {
        self.params.insert("line_items".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert("metadata".to_owned(), serde_json::Value::Object(value.into_iter().map(|(k, v)| (k, serde_json::Value::String(v))).collect()));
        self
    }

    /// The account on behalf of which to charge.
    #[must_use]
    pub fn on_behalf_of(mut self, value: serde_json::Value) -> Self {
        self.params.insert("on_behalf_of".to_owned(), value);
        self
    }

    /// When creating a subscription or subscription schedule, the specified configuration data will be used. There must be at least one line item with a recurring price for a subscription or subscription schedule to be created. A subscription sch…
    #[must_use]
    pub fn subscription_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("subscription_data".to_owned(), value);
        self
    }

    /// The data with which to automatically create a Transfer for each of the invoices.
    #[must_use]
    pub fn transfer_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("transfer_data".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Quote, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/quotes/{quote}/accept`
///
/// Accepts the specified quote.
#[derive(Debug, Clone)]
pub struct PostQuotesQuoteAccept {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostQuotesQuoteAccept {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(quote: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let quote = quote.into();
        Self { path: format!("/v1/quotes/{quote}/accept"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Quote, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/quotes/{quote}/cancel`
///
/// Cancels the quote.
#[derive(Debug, Clone)]
pub struct PostQuotesQuoteCancel {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostQuotesQuoteCancel {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(quote: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let quote = quote.into();
        Self { path: format!("/v1/quotes/{quote}/cancel"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Quote, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/quotes/{quote}/computed_upfront_line_items`
///
/// When retrieving a quote, there is an includable computed.upfront.line_items property containing the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of upfront line items.
#[derive(Debug, Clone)]
pub struct GetQuotesQuoteComputedUpfrontLineItems {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetQuotesQuoteComputedUpfrontLineItems {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(quote: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let quote = quote.into();
        Self { path: format!("/v1/quotes/{quote}/computed_upfront_line_items"), params }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Item> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::Item>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/quotes/{quote}/finalize`
///
/// Finalizes the quote.
#[derive(Debug, Clone)]
pub struct PostQuotesQuoteFinalize {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostQuotesQuoteFinalize {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(quote: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let quote = quote.into();
        Self { path: format!("/v1/quotes/{quote}/finalize"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A future timestamp on which the quote will be canceled if in `open` or `draft` status. Measured in seconds since the Unix epoch.
    #[must_use]
    pub fn expires_at(mut self, value: i64) -> Self {
        self.params.insert("expires_at".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Quote, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/quotes/{quote}/line_items`
///
/// When retrieving a quote, there is an includable line_items property containing the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of line items.
#[derive(Debug, Clone)]
pub struct GetQuotesQuoteLineItems {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetQuotesQuoteLineItems {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(quote: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let quote = quote.into();
        Self { path: format!("/v1/quotes/{quote}/line_items"), params }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Item> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::Item>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/quotes/{quote}/pdf`
///
/// Download the PDF for a finalized quote. Explanation for special handling can be found here
#[derive(Debug, Clone)]
pub struct GetQuotesQuotePdf {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetQuotesQuotePdf {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(quote: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let quote = quote.into();
        Self { path: format!("/v1/quotes/{quote}/pdf"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}
