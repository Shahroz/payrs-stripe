//! Generated operations for the `plans` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(clippy::doc_markdown, clippy::too_many_arguments, clippy::new_without_default, rustdoc::bare_urls)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/plans`
///
/// Returns a list of your plans.
#[derive(Debug, Clone)]
pub struct GetPlans {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetPlans {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/plans".to_owned(), params }
    }

    /// Only return plans that are active or inactive (e.g., pass `false` to list all inactive plans).
    #[must_use]
    pub fn active(mut self, value: bool) -> Self {
        self.params.insert("active".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Only return plans for the given product.
    #[must_use]
    pub fn product(mut self, value: impl Into<String>) -> Self {
        self.params.insert("product".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Plan> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::Plan>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/plans`
///
/// You can now model subscriptions more flexibly using the Prices API. It replaces the Plans API and is backwards compatible to simplify your migration.
#[derive(Debug, Clone)]
pub struct PostPlans {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPlans {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(currency: impl Into<String>, interval: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("currency".to_owned(), serde_json::Value::String(currency.into()));
        params.insert("interval".to_owned(), serde_json::Value::String(interval.into()));
        Self { path: "/v1/plans".to_owned(), params }
    }

    /// Whether the plan is currently available for new subscriptions. Defaults to `true`.
    #[must_use]
    pub fn active(mut self, value: bool) -> Self {
        self.params.insert("active".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// A positive integer in cents (or local equivalent) (or 0 for a free plan) representing how much to charge on a recurring basis.
    #[must_use]
    pub fn amount(mut self, value: i64) -> Self {
        self.params.insert("amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Same as `amount`, but accepts a decimal value with at most 12 decimal places. Only one of `amount` and `amount_decimal` can be set.
    #[must_use]
    pub fn amount_decimal(mut self, value: impl Into<String>) -> Self {
        self.params.insert("amount_decimal".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Describes how to compute the price per period. Either `per_unit` or `tiered`. `per_unit` indicates that the fixed amount (specified in `amount`) will be charged per unit in `quantity` (for plans with `usage_type=licensed`), or per unit of…
    #[must_use]
    pub fn billing_scheme(mut self, value: impl Into<String>) -> Self {
        self.params.insert("billing_scheme".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// An identifier randomly generated by Stripe. Used to identify this plan when subscribing a customer. You can optionally override this ID, but the ID must be unique across all plans in your Stripe account. You can, however, use the same plan…
    #[must_use]
    pub fn id(mut self, value: impl Into<String>) -> Self {
        self.params.insert("id".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The number of intervals between subscription billings. For example, `interval=month` and `interval_count=3` bills every 3 months. Maximum of three years interval allowed (3 years, 36 months, or 156 weeks).
    #[must_use]
    pub fn interval_count(mut self, value: i64) -> Self {
        self.params.insert("interval_count".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// The meter tracking the usage of a metered price
    #[must_use]
    pub fn meter(mut self, value: impl Into<String>) -> Self {
        self.params.insert("meter".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A brief description of the plan, hidden from customers.
    #[must_use]
    pub fn nickname(mut self, value: impl Into<String>) -> Self {
        self.params.insert("nickname".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set the `product` parameter.
    #[must_use]
    pub fn product(mut self, value: serde_json::Value) -> Self {
        self.params.insert("product".to_owned(), value);
        self
    }

    /// Each element represents a pricing tier. This parameter requires `billing_scheme` to be set to `tiered`. See also the documentation for `billing_scheme`.
    #[must_use]
    pub fn tiers(mut self, value: serde_json::Value) -> Self {
        self.params.insert("tiers".to_owned(), value);
        self
    }

    /// Defines if the tiering price should be `graduated` or `volume` based. In `volume`-based tiering, the maximum quantity within a period determines the per unit price, in `graduated` tiering pricing can successively change as the quantity gro…
    #[must_use]
    pub fn tiers_mode(mut self, value: impl Into<String>) -> Self {
        self.params.insert("tiers_mode".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Apply a transformation to the reported usage or set quantity before computing the billed price. Cannot be combined with `tiers`.
    #[must_use]
    pub fn transform_usage(mut self, value: serde_json::Value) -> Self {
        self.params.insert("transform_usage".to_owned(), value);
        self
    }

    /// Default number of trial days when subscribing a customer to this plan using \[`trial_from_plan=true`\](https://docs.stripe.com/api#create_subscription-trial_from_plan).
    #[must_use]
    pub fn trial_period_days(mut self, value: i64) -> Self {
        self.params.insert("trial_period_days".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Configures how the quantity per period should be determined. Can be either `metered` or `licensed`. `licensed` automatically bills the `quantity` set when adding it to a subscription. `metered` aggregates the total usage based on usage rec…
    #[must_use]
    pub fn usage_type(mut self, value: impl Into<String>) -> Self {
        self.params.insert("usage_type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Plan, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/plans/{plan}`
///
/// Retrieves the plan with the given ID.
#[derive(Debug, Clone)]
pub struct GetPlansPlan {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetPlansPlan {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(plan: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let plan = plan.into();
        Self { path: format!("/v1/plans/{plan}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Plan, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/plans/{plan}`
///
/// Updates the specified plan by setting the values of the parameters passed. Any parameters not provided are left unchanged. By design, you cannot change a plan’s ID, amount, currency, or billing cycle.
#[derive(Debug, Clone)]
pub struct PostPlansPlan {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPlansPlan {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(plan: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let plan = plan.into();
        Self { path: format!("/v1/plans/{plan}"), params }
    }

    /// Whether the plan is currently available for new subscriptions.
    #[must_use]
    pub fn active(mut self, value: bool) -> Self {
        self.params.insert("active".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// A brief description of the plan, hidden from customers.
    #[must_use]
    pub fn nickname(mut self, value: impl Into<String>) -> Self {
        self.params.insert("nickname".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The product the plan belongs to. This cannot be changed once it has been used in a subscription or subscription schedule.
    #[must_use]
    pub fn product(mut self, value: impl Into<String>) -> Self {
        self.params.insert("product".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Default number of trial days when subscribing a customer to this plan using \[`trial_from_plan=true`\](https://docs.stripe.com/api#create_subscription-trial_from_plan).
    #[must_use]
    pub fn trial_period_days(mut self, value: i64) -> Self {
        self.params.insert("trial_period_days".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Plan, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `DELETE /v1/plans/{plan}`
///
/// Deleting plans means new subscribers can’t be added. Existing subscribers aren’t affected.
#[derive(Debug, Clone)]
pub struct DeletePlansPlan {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeletePlansPlan {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(plan: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let plan = plan.into();
        Self { path: format!("/v1/plans/{plan}"), params }
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::DeletedPlan, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
