//! Generated operations for the `payment_method_configurations` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(
    clippy::doc_markdown,
    clippy::too_many_arguments,
    clippy::new_without_default,
    rustdoc::bare_urls
)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/payment_method_configurations`
///
/// List payment method configurations
#[derive(Debug, Clone)]
pub struct GetPaymentMethodConfigurations {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetPaymentMethodConfigurations {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/payment_method_configurations".to_owned(),
            params,
        }
    }

    /// Whether the configuration is active.
    #[must_use]
    pub fn active(mut self, value: bool) -> Self {
        self.params
            .insert("active".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// The Connect application to filter by.
    #[must_use]
    pub fn application(mut self, value: serde_json::Value) -> Self {
        self.params.insert("application".to_owned(), value);
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::PaymentMethodConfiguration> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::PaymentMethodConfiguration>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/payment_method_configurations`
///
/// Creates a payment method configuration
#[derive(Debug, Clone)]
pub struct PostPaymentMethodConfigurations {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPaymentMethodConfigurations {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/payment_method_configurations".to_owned(),
            params,
        }
    }

    /// Canadian pre-authorized debit payments, check this \[page\](https://docs.stripe.com/payments/acss-debit) for more details like country availability.
    #[must_use]
    pub fn acss_debit(mut self, value: serde_json::Value) -> Self {
        self.params.insert("acss_debit".to_owned(), value);
        self
    }

    /// \[Affirm\](https://www.affirm.com/) gives your customers a way to split purchases over a series of payments. Depending on the purchase, they can pay with four interest-free payments (Split Pay) or pay over a longer term (Installments), whi…
    #[must_use]
    pub fn affirm(mut self, value: serde_json::Value) -> Self {
        self.params.insert("affirm".to_owned(), value);
        self
    }

    /// Afterpay gives your customers a way to pay for purchases in installments, check this \[page\](https://docs.stripe.com/payments/afterpay-clearpay) for more details like country availability. Afterpay is particularly popular among businesses…
    #[must_use]
    pub fn afterpay_clearpay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("afterpay_clearpay".to_owned(), value);
        self
    }

    /// Alipay is a digital wallet in China that has more than a billion active users worldwide. Alipay users can pay on the web or on a mobile device using login credentials or their Alipay app. Alipay has a low dispute rate and reduces fraud by…
    #[must_use]
    pub fn alipay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("alipay".to_owned(), value);
        self
    }

    /// Alma is a Buy Now, Pay Later payment method that offers customers the ability to pay in 2, 3, or 4 installments.
    #[must_use]
    pub fn alma(mut self, value: serde_json::Value) -> Self {
        self.params.insert("alma".to_owned(), value);
        self
    }

    /// Amazon Pay is a wallet payment method that lets your customers check out the same way as on Amazon.
    #[must_use]
    pub fn amazon_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("amazon_pay".to_owned(), value);
        self
    }

    /// Stripe users can accept \[Apple Pay\](https://stripe.com/payments/apple-pay) in iOS applications in iOS 9 and later, and on the web in Safari starting with iOS 10 or macOS Sierra. There are no additional fees to process Apple Pay payments,…
    #[must_use]
    pub fn apple_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("apple_pay".to_owned(), value);
        self
    }

    /// Apple Pay Later, a payment method for customers to buy now and pay later, gives your customers a way to split purchases into four installments across six weeks.
    #[must_use]
    pub fn apple_pay_later(mut self, value: serde_json::Value) -> Self {
        self.params.insert("apple_pay_later".to_owned(), value);
        self
    }

    /// Stripe users in Australia can accept Bulk Electronic Clearing System (BECS) direct debit payments from customers with an Australian bank account. Check this \[page\](https://docs.stripe.com/payments/au-becs-debit) for more details.
    #[must_use]
    pub fn au_becs_debit(mut self, value: serde_json::Value) -> Self {
        self.params.insert("au_becs_debit".to_owned(), value);
        self
    }

    /// Stripe users in the UK can accept Bacs Direct Debit payments from customers with a UK bank account, check this \[page\](https://docs.stripe.com/payments/payment-methods/bacs-debit) for more details.
    #[must_use]
    pub fn bacs_debit(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bacs_debit".to_owned(), value);
        self
    }

    /// Bancontact is the most popular online payment method in Belgium, with over 15 million cards in circulation. \[Customers\](https://docs.stripe.com/api/customers) use a Bancontact card or mobile app linked to a Belgian bank account to make o…
    #[must_use]
    pub fn bancontact(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bancontact".to_owned(), value);
        self
    }

    /// Billie is a \[single-use\](https://docs.stripe.com/payments/payment-methods#usage) payment method that offers businesses Pay by Invoice where they offer payment terms ranging from 7-120 days. Customers are redirected from your website or a…
    #[must_use]
    pub fn billie(mut self, value: serde_json::Value) -> Self {
        self.params.insert("billie".to_owned(), value);
        self
    }

    /// To enable Bizum, buyers need a Spanish IBAN from a bank connected to Bizum. Within their banking app, they can enable Bizum and link their mobile number to their IBAN.
    #[must_use]
    pub fn bizum(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bizum".to_owned(), value);
        self
    }

    /// BLIK is a \[single use\](https://docs.stripe.com/payments/payment-methods#usage) payment method that requires customers to authenticate their payments. When customers want to pay online using BLIK, they request a six-digit code from their…
    #[must_use]
    pub fn blik(mut self, value: serde_json::Value) -> Self {
        self.params.insert("blik".to_owned(), value);
        self
    }

    /// Boleto is an official (regulated by the Central Bank of Brazil) payment method in Brazil. Check this \[page\](https://docs.stripe.com/payments/boleto) for more details.
    #[must_use]
    pub fn boleto(mut self, value: serde_json::Value) -> Self {
        self.params.insert("boleto".to_owned(), value);
        self
    }

    /// Cards are a popular way for consumers and businesses to pay online or in person. Stripe supports global and local card networks.
    #[must_use]
    pub fn card(mut self, value: serde_json::Value) -> Self {
        self.params.insert("card".to_owned(), value);
        self
    }

    /// Cartes Bancaires is France's local card network. More than 95% of these cards are co-branded with either Visa or Mastercard, meaning you can process these cards over either Cartes Bancaires or the Visa or Mastercard networks. Check this \[…
    #[must_use]
    pub fn cartes_bancaires(mut self, value: serde_json::Value) -> Self {
        self.params.insert("cartes_bancaires".to_owned(), value);
        self
    }

    /// Cash App is a popular consumer app in the US that allows customers to bank, invest, send, and receive money using their digital wallet. Check this \[page\](https://docs.stripe.com/payments/cash-app-pay) for more details.
    #[must_use]
    pub fn cashapp(mut self, value: serde_json::Value) -> Self {
        self.params.insert("cashapp".to_owned(), value);
        self
    }

    /// \[Stablecoin payments\](https://docs.stripe.com/payments/stablecoin-payments) enable customers to pay in stablecoins like USDC from 100s of wallets including Phantom and Metamask.
    #[must_use]
    pub fn crypto(mut self, value: serde_json::Value) -> Self {
        self.params.insert("crypto".to_owned(), value);
        self
    }

    /// Uses a customer’s \[cash balance\](https://docs.stripe.com/payments/customer-balance) for the payment. The cash balance can be funded via a bank transfer. Check this \[page\](https://docs.stripe.com/payments/bank-transfers) for more detail…
    #[must_use]
    pub fn customer_balance(mut self, value: serde_json::Value) -> Self {
        self.params.insert("customer_balance".to_owned(), value);
        self
    }

    /// EPS is an Austria-based payment method that allows customers to complete transactions online using their bank credentials. EPS is supported by all Austrian banks and is accepted by over 80% of Austrian online retailers. Check this \[page\]…
    #[must_use]
    pub fn eps(mut self, value: serde_json::Value) -> Self {
        self.params.insert("eps".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Financial Process Exchange (FPX) is a Malaysia-based payment method that allows customers to complete transactions online using their bank credentials. Bank Negara Malaysia (BNM), the Central Bank of Malaysia, and eleven other major Malays…
    #[must_use]
    pub fn fpx(mut self, value: serde_json::Value) -> Self {
        self.params.insert("fpx".to_owned(), value);
        self
    }

    /// Meal vouchers in France, or “titres-restaurant”, is a local benefits program commonly offered by employers for their employees to purchase prepared food and beverages on working days. Check this \[page\](https://docs.stripe.com/payments/me…
    #[must_use]
    pub fn fr_meal_voucher_conecs(mut self, value: serde_json::Value) -> Self {
        self.params
            .insert("fr_meal_voucher_conecs".to_owned(), value);
        self
    }

    /// giropay is a German payment method based on online banking, introduced in 2006. It allows customers to complete transactions online using their online banking environment, with funds debited from their bank account. Depending on their bank…
    #[must_use]
    pub fn giropay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("giropay".to_owned(), value);
        self
    }

    /// Google Pay allows customers to make payments in your app or website using any credit or debit card saved to their Google Account, including those from Google Play, YouTube, Chrome, or an Android device. Use the Google Pay API to request an…
    #[must_use]
    pub fn google_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("google_pay".to_owned(), value);
        self
    }

    /// GrabPay is a payment method developed by \[Grab\](https://www.grab.com/sg/consumer/finance/pay/). GrabPay is a digital wallet - customers maintain a balance in their wallets that they pay out with. Check this \[page\](https://docs.stripe.c…
    #[must_use]
    pub fn grabpay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("grabpay".to_owned(), value);
        self
    }

    /// iDEAL is a Netherlands-based payment method that allows customers to complete transactions online using their bank credentials. All major Dutch banks are members of Currence, the scheme that operates iDEAL, making it the most popular onlin…
    #[must_use]
    pub fn ideal(mut self, value: serde_json::Value) -> Self {
        self.params.insert("ideal".to_owned(), value);
        self
    }

    /// JCB is a credit card company based in Japan. JCB is currently available in Japan to businesses approved by JCB, and available to all businesses in Australia, Canada, Hong Kong, Japan, New Zealand, Singapore, Switzerland, United Kingdom, Un…
    #[must_use]
    pub fn jcb(mut self, value: serde_json::Value) -> Self {
        self.params.insert("jcb".to_owned(), value);
        self
    }

    /// Kakao Pay is a popular local wallet available in South Korea.
    #[must_use]
    pub fn kakao_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("kakao_pay".to_owned(), value);
        self
    }

    /// Klarna gives customers a range of \[payment options\](https://docs.stripe.com/payments/klarna#payment-options) during checkout. Available payment options vary depending on the customer's billing address and the transaction amount. These pa…
    #[must_use]
    pub fn klarna(mut self, value: serde_json::Value) -> Self {
        self.params.insert("klarna".to_owned(), value);
        self
    }

    /// Konbini allows customers in Japan to pay for bills and online purchases at convenience stores with cash. Check this \[page\](https://docs.stripe.com/payments/konbini) for more details.
    #[must_use]
    pub fn konbini(mut self, value: serde_json::Value) -> Self {
        self.params.insert("konbini".to_owned(), value);
        self
    }

    /// Korean cards let users pay using locally issued cards from South Korea.
    #[must_use]
    pub fn kr_card(mut self, value: serde_json::Value) -> Self {
        self.params.insert("kr_card".to_owned(), value);
        self
    }

    /// \[Link\](https://docs.stripe.com/payments/link) is a payment method network. With Link, users save their payment details once, then reuse that information to pay with one click for any business on the network.
    #[must_use]
    pub fn link(mut self, value: serde_json::Value) -> Self {
        self.params.insert("link".to_owned(), value);
        self
    }

    /// MB WAY is the most popular wallet in Portugal. After entering their phone number in your checkout, customers approve the payment directly in their MB WAY app. Check this \[page\](https://docs.stripe.com/payments/mb-way) for more details.
    #[must_use]
    pub fn mb_way(mut self, value: serde_json::Value) -> Self {
        self.params.insert("mb_way".to_owned(), value);
        self
    }

    /// MobilePay is a \[single-use\](https://docs.stripe.com/payments/payment-methods#usage) card wallet payment method used in Denmark and Finland. It allows customers to \[authenticate and approve\](https://docs.stripe.com/payments/payment-meth…
    #[must_use]
    pub fn mobilepay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("mobilepay".to_owned(), value);
        self
    }

    /// Stripe users in Europe and the United States can accept Multibanco payments from customers in Portugal using \[Sources\](https://stripe.com/docs/sources)—a single integration path for creating payments using any supported method.
    #[must_use]
    pub fn multibanco(mut self, value: serde_json::Value) -> Self {
        self.params.insert("multibanco".to_owned(), value);
        self
    }

    /// Configuration name.
    #[must_use]
    pub fn name(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Naver Pay is a popular local wallet available in South Korea.
    #[must_use]
    pub fn naver_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("naver_pay".to_owned(), value);
        self
    }

    /// Stripe users in New Zealand can accept Bulk Electronic Clearing System (BECS) direct debit payments from customers with a New Zeland bank account. Check this \[page\](https://docs.stripe.com/payments/nz-bank-account) for more details.
    #[must_use]
    pub fn nz_bank_account(mut self, value: serde_json::Value) -> Self {
        self.params.insert("nz_bank_account".to_owned(), value);
        self
    }

    /// OXXO is a Mexican chain of convenience stores with thousands of locations across Latin America and represents nearly 20% of online transactions in Mexico. OXXO allows customers to pay bills and online purchases in-store with cash. Check th…
    #[must_use]
    pub fn oxxo(mut self, value: serde_json::Value) -> Self {
        self.params.insert("oxxo".to_owned(), value);
        self
    }

    /// Przelewy24 is a Poland-based payment method aggregator that allows customers to complete transactions online using bank transfers and other methods. Bank transfers account for 30% of online payments in Poland and Przelewy24 provides a way…
    #[must_use]
    pub fn p24(mut self, value: serde_json::Value) -> Self {
        self.params.insert("p24".to_owned(), value);
        self
    }

    /// Configuration's parent configuration. Specify to create a child configuration.
    #[must_use]
    pub fn parent(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("parent".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Pay by bank is a redirect payment method backed by bank transfers. A customer is redirected to their bank to authorize a bank transfer for a given amount. This removes a lot of the error risks inherent in waiting for the customer to initia…
    #[must_use]
    pub fn pay_by_bank(mut self, value: serde_json::Value) -> Self {
        self.params.insert("pay_by_bank".to_owned(), value);
        self
    }

    /// PAYCO is a \[single-use\](https://docs.stripe.com/payments/payment-methods#usage local wallet available in South Korea.
    #[must_use]
    pub fn payco(mut self, value: serde_json::Value) -> Self {
        self.params.insert("payco".to_owned(), value);
        self
    }

    /// PayNow is a Singapore-based payment method that allows customers to make a payment using their preferred app from participating banks and participating non-bank financial institutions. Check this \[page\](https://docs.stripe.com/payments/p…
    #[must_use]
    pub fn paynow(mut self, value: serde_json::Value) -> Self {
        self.params.insert("paynow".to_owned(), value);
        self
    }

    /// PayPal, a digital wallet popular with customers in Europe, allows your customers worldwide to pay using their PayPal account. Check this \[page\](https://docs.stripe.com/payments/paypal) for more details.
    #[must_use]
    pub fn paypal(mut self, value: serde_json::Value) -> Self {
        self.params.insert("paypal".to_owned(), value);
        self
    }

    /// PayTo is a \[real-time\](https://docs.stripe.com/payments/real-time) payment method that enables customers in Australia to pay by providing their bank account details. Customers must accept a mandate authorizing you to debit their account.…
    #[must_use]
    pub fn payto(mut self, value: serde_json::Value) -> Self {
        self.params.insert("payto".to_owned(), value);
        self
    }

    /// Pix is a payment method popular in Brazil. When paying with Pix, customers authenticate and approve payments by scanning a QR code in their preferred banking app. Check this \[page\](https://docs.stripe.com/payments/pix) for more details.
    #[must_use]
    pub fn pix(mut self, value: serde_json::Value) -> Self {
        self.params.insert("pix".to_owned(), value);
        self
    }

    /// PromptPay is a Thailand-based payment method that allows customers to make a payment using their preferred app from participating banks. Check this \[page\](https://docs.stripe.com/payments/promptpay) for more details.
    #[must_use]
    pub fn promptpay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("promptpay".to_owned(), value);
        self
    }

    /// Revolut Pay, developed by Revolut, a global finance app, is a digital wallet payment method. Revolut Pay uses the customer’s stored balance or cards to fund the payment, and offers the option for non-Revolut customers to save their details…
    #[must_use]
    pub fn revolut_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("revolut_pay".to_owned(), value);
        self
    }

    /// Samsung Pay is a \[single-use\](https://docs.stripe.com/payments/payment-methods#usage local wallet available in South Korea.
    #[must_use]
    pub fn samsung_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("samsung_pay".to_owned(), value);
        self
    }

    /// Satispay is a \[single-use\](/payments/payment-methods#usage) payment method where customers are required to \[authenticate\](/payments/payment-methods#customer-actions) their payment. Customers pay by being redirected from your website or…
    #[must_use]
    pub fn satispay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("satispay".to_owned(), value);
        self
    }

    /// Scalapay is a \[single-use\](/payments/payment-methods#usage) payment method that lets customers pay in 3 or 4 installments. Customers are redirected from your website or app, authorize the payment with Scalapay, then return to your websit…
    #[must_use]
    pub fn scalapay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("scalapay".to_owned(), value);
        self
    }

    /// The \[Single Euro Payments Area (SEPA)\](https://en.wikipedia.org/wiki/Single_Euro_Payments_Area) is an initiative of the European Union to simplify payments within and across member countries. SEPA established and enforced banking standar…
    #[must_use]
    pub fn sepa_debit(mut self, value: serde_json::Value) -> Self {
        self.params.insert("sepa_debit".to_owned(), value);
        self
    }

    /// Stripe users in Europe and the United States can use the \[Payment Intents API\](https://stripe.com/docs/payments/payment-intents)—a single integration path for creating payments using any supported method—to accept \[Sofort\](https://www.…
    #[must_use]
    pub fn sofort(mut self, value: serde_json::Value) -> Self {
        self.params.insert("sofort".to_owned(), value);
        self
    }

    /// Sunbit is a \[single-use\](https://docs.stripe.com/payments/payment-methods#usage) payment method where customers choose to pay in 3, 6, or 12 installments. Customers are redirected from your website or app, authorize the payment with Sunb…
    #[must_use]
    pub fn sunbit(mut self, value: serde_json::Value) -> Self {
        self.params.insert("sunbit".to_owned(), value);
        self
    }

    /// Swish is a \[real-time\](https://docs.stripe.com/payments/real-time) payment method popular in Sweden. It allows customers to \[authenticate and approve\](https://docs.stripe.com/payments/payment-methods#customer-actions) payments using th…
    #[must_use]
    pub fn swish(mut self, value: serde_json::Value) -> Self {
        self.params.insert("swish".to_owned(), value);
        self
    }

    /// Twint is a payment method popular in Switzerland. It allows customers to pay using their mobile phone. Check this \[page\](https://docs.stripe.com/payments/twint) for more details.
    #[must_use]
    pub fn twint(mut self, value: serde_json::Value) -> Self {
        self.params.insert("twint".to_owned(), value);
        self
    }

    /// Unified Payment Interface (UPI) is India's leading payment method with exponential growth since it launched in 2016.
    #[must_use]
    pub fn upi(mut self, value: serde_json::Value) -> Self {
        self.params.insert("upi".to_owned(), value);
        self
    }

    /// Stripe users in the United States can accept ACH direct debit payments from customers with a US bank account using the Automated Clearing House (ACH) payments system operated by Nacha. Check this \[page\](https://docs.stripe.com/payments/a…
    #[must_use]
    pub fn us_bank_account(mut self, value: serde_json::Value) -> Self {
        self.params.insert("us_bank_account".to_owned(), value);
        self
    }

    /// WeChat, owned by Tencent, is China's leading mobile app with over 1 billion monthly active users. Chinese consumers can use WeChat Pay to pay for goods and services inside of businesses' apps and websites. WeChat Pay users buy most frequen…
    #[must_use]
    pub fn wechat_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("wechat_pay".to_owned(), value);
        self
    }

    /// Zip gives your customers a way to split purchases over a series of payments. Check this \[page\](https://docs.stripe.com/payments/zip) for more details like country availability.
    #[must_use]
    pub fn zip(mut self, value: serde_json::Value) -> Self {
        self.params.insert("zip".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<crate::models::PaymentMethodConfiguration, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/payment_method_configurations/{configuration}`
///
/// Retrieve payment method configuration
#[derive(Debug, Clone)]
pub struct GetPaymentMethodConfigurationsConfiguration {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetPaymentMethodConfigurationsConfiguration {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(configuration: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let configuration = configuration.into();
        Self {
            path: format!("/v1/payment_method_configurations/{configuration}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<crate::models::PaymentMethodConfiguration, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/payment_method_configurations/{configuration}`
///
/// Update payment method configuration
#[derive(Debug, Clone)]
pub struct PostPaymentMethodConfigurationsConfiguration {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPaymentMethodConfigurationsConfiguration {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(configuration: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let configuration = configuration.into();
        Self {
            path: format!("/v1/payment_method_configurations/{configuration}"),
            params,
        }
    }

    /// Canadian pre-authorized debit payments, check this \[page\](https://docs.stripe.com/payments/acss-debit) for more details like country availability.
    #[must_use]
    pub fn acss_debit(mut self, value: serde_json::Value) -> Self {
        self.params.insert("acss_debit".to_owned(), value);
        self
    }

    /// Whether the configuration can be used for new payments.
    #[must_use]
    pub fn active(mut self, value: bool) -> Self {
        self.params
            .insert("active".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// \[Affirm\](https://www.affirm.com/) gives your customers a way to split purchases over a series of payments. Depending on the purchase, they can pay with four interest-free payments (Split Pay) or pay over a longer term (Installments), whi…
    #[must_use]
    pub fn affirm(mut self, value: serde_json::Value) -> Self {
        self.params.insert("affirm".to_owned(), value);
        self
    }

    /// Afterpay gives your customers a way to pay for purchases in installments, check this \[page\](https://docs.stripe.com/payments/afterpay-clearpay) for more details like country availability. Afterpay is particularly popular among businesses…
    #[must_use]
    pub fn afterpay_clearpay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("afterpay_clearpay".to_owned(), value);
        self
    }

    /// Alipay is a digital wallet in China that has more than a billion active users worldwide. Alipay users can pay on the web or on a mobile device using login credentials or their Alipay app. Alipay has a low dispute rate and reduces fraud by…
    #[must_use]
    pub fn alipay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("alipay".to_owned(), value);
        self
    }

    /// Alma is a Buy Now, Pay Later payment method that offers customers the ability to pay in 2, 3, or 4 installments.
    #[must_use]
    pub fn alma(mut self, value: serde_json::Value) -> Self {
        self.params.insert("alma".to_owned(), value);
        self
    }

    /// Amazon Pay is a wallet payment method that lets your customers check out the same way as on Amazon.
    #[must_use]
    pub fn amazon_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("amazon_pay".to_owned(), value);
        self
    }

    /// Stripe users can accept \[Apple Pay\](https://stripe.com/payments/apple-pay) in iOS applications in iOS 9 and later, and on the web in Safari starting with iOS 10 or macOS Sierra. There are no additional fees to process Apple Pay payments,…
    #[must_use]
    pub fn apple_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("apple_pay".to_owned(), value);
        self
    }

    /// Apple Pay Later, a payment method for customers to buy now and pay later, gives your customers a way to split purchases into four installments across six weeks.
    #[must_use]
    pub fn apple_pay_later(mut self, value: serde_json::Value) -> Self {
        self.params.insert("apple_pay_later".to_owned(), value);
        self
    }

    /// Stripe users in Australia can accept Bulk Electronic Clearing System (BECS) direct debit payments from customers with an Australian bank account. Check this \[page\](https://docs.stripe.com/payments/au-becs-debit) for more details.
    #[must_use]
    pub fn au_becs_debit(mut self, value: serde_json::Value) -> Self {
        self.params.insert("au_becs_debit".to_owned(), value);
        self
    }

    /// Stripe users in the UK can accept Bacs Direct Debit payments from customers with a UK bank account, check this \[page\](https://docs.stripe.com/payments/payment-methods/bacs-debit) for more details.
    #[must_use]
    pub fn bacs_debit(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bacs_debit".to_owned(), value);
        self
    }

    /// Bancontact is the most popular online payment method in Belgium, with over 15 million cards in circulation. \[Customers\](https://docs.stripe.com/api/customers) use a Bancontact card or mobile app linked to a Belgian bank account to make o…
    #[must_use]
    pub fn bancontact(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bancontact".to_owned(), value);
        self
    }

    /// Billie is a \[single-use\](https://docs.stripe.com/payments/payment-methods#usage) payment method that offers businesses Pay by Invoice where they offer payment terms ranging from 7-120 days. Customers are redirected from your website or a…
    #[must_use]
    pub fn billie(mut self, value: serde_json::Value) -> Self {
        self.params.insert("billie".to_owned(), value);
        self
    }

    /// To enable Bizum, buyers need a Spanish IBAN from a bank connected to Bizum. Within their banking app, they can enable Bizum and link their mobile number to their IBAN.
    #[must_use]
    pub fn bizum(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bizum".to_owned(), value);
        self
    }

    /// BLIK is a \[single use\](https://docs.stripe.com/payments/payment-methods#usage) payment method that requires customers to authenticate their payments. When customers want to pay online using BLIK, they request a six-digit code from their…
    #[must_use]
    pub fn blik(mut self, value: serde_json::Value) -> Self {
        self.params.insert("blik".to_owned(), value);
        self
    }

    /// Boleto is an official (regulated by the Central Bank of Brazil) payment method in Brazil. Check this \[page\](https://docs.stripe.com/payments/boleto) for more details.
    #[must_use]
    pub fn boleto(mut self, value: serde_json::Value) -> Self {
        self.params.insert("boleto".to_owned(), value);
        self
    }

    /// Cards are a popular way for consumers and businesses to pay online or in person. Stripe supports global and local card networks.
    #[must_use]
    pub fn card(mut self, value: serde_json::Value) -> Self {
        self.params.insert("card".to_owned(), value);
        self
    }

    /// Cartes Bancaires is France's local card network. More than 95% of these cards are co-branded with either Visa or Mastercard, meaning you can process these cards over either Cartes Bancaires or the Visa or Mastercard networks. Check this \[…
    #[must_use]
    pub fn cartes_bancaires(mut self, value: serde_json::Value) -> Self {
        self.params.insert("cartes_bancaires".to_owned(), value);
        self
    }

    /// Cash App is a popular consumer app in the US that allows customers to bank, invest, send, and receive money using their digital wallet. Check this \[page\](https://docs.stripe.com/payments/cash-app-pay) for more details.
    #[must_use]
    pub fn cashapp(mut self, value: serde_json::Value) -> Self {
        self.params.insert("cashapp".to_owned(), value);
        self
    }

    /// \[Stablecoin payments\](https://docs.stripe.com/payments/stablecoin-payments) enable customers to pay in stablecoins like USDC from 100s of wallets including Phantom and Metamask.
    #[must_use]
    pub fn crypto(mut self, value: serde_json::Value) -> Self {
        self.params.insert("crypto".to_owned(), value);
        self
    }

    /// Uses a customer’s \[cash balance\](https://docs.stripe.com/payments/customer-balance) for the payment. The cash balance can be funded via a bank transfer. Check this \[page\](https://docs.stripe.com/payments/bank-transfers) for more detail…
    #[must_use]
    pub fn customer_balance(mut self, value: serde_json::Value) -> Self {
        self.params.insert("customer_balance".to_owned(), value);
        self
    }

    /// EPS is an Austria-based payment method that allows customers to complete transactions online using their bank credentials. EPS is supported by all Austrian banks and is accepted by over 80% of Austrian online retailers. Check this \[page\]…
    #[must_use]
    pub fn eps(mut self, value: serde_json::Value) -> Self {
        self.params.insert("eps".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Financial Process Exchange (FPX) is a Malaysia-based payment method that allows customers to complete transactions online using their bank credentials. Bank Negara Malaysia (BNM), the Central Bank of Malaysia, and eleven other major Malays…
    #[must_use]
    pub fn fpx(mut self, value: serde_json::Value) -> Self {
        self.params.insert("fpx".to_owned(), value);
        self
    }

    /// Meal vouchers in France, or “titres-restaurant”, is a local benefits program commonly offered by employers for their employees to purchase prepared food and beverages on working days. Check this \[page\](https://docs.stripe.com/payments/me…
    #[must_use]
    pub fn fr_meal_voucher_conecs(mut self, value: serde_json::Value) -> Self {
        self.params
            .insert("fr_meal_voucher_conecs".to_owned(), value);
        self
    }

    /// giropay is a German payment method based on online banking, introduced in 2006. It allows customers to complete transactions online using their online banking environment, with funds debited from their bank account. Depending on their bank…
    #[must_use]
    pub fn giropay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("giropay".to_owned(), value);
        self
    }

    /// Google Pay allows customers to make payments in your app or website using any credit or debit card saved to their Google Account, including those from Google Play, YouTube, Chrome, or an Android device. Use the Google Pay API to request an…
    #[must_use]
    pub fn google_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("google_pay".to_owned(), value);
        self
    }

    /// GrabPay is a payment method developed by \[Grab\](https://www.grab.com/sg/consumer/finance/pay/). GrabPay is a digital wallet - customers maintain a balance in their wallets that they pay out with. Check this \[page\](https://docs.stripe.c…
    #[must_use]
    pub fn grabpay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("grabpay".to_owned(), value);
        self
    }

    /// iDEAL is a Netherlands-based payment method that allows customers to complete transactions online using their bank credentials. All major Dutch banks are members of Currence, the scheme that operates iDEAL, making it the most popular onlin…
    #[must_use]
    pub fn ideal(mut self, value: serde_json::Value) -> Self {
        self.params.insert("ideal".to_owned(), value);
        self
    }

    /// JCB is a credit card company based in Japan. JCB is currently available in Japan to businesses approved by JCB, and available to all businesses in Australia, Canada, Hong Kong, Japan, New Zealand, Singapore, Switzerland, United Kingdom, Un…
    #[must_use]
    pub fn jcb(mut self, value: serde_json::Value) -> Self {
        self.params.insert("jcb".to_owned(), value);
        self
    }

    /// Kakao Pay is a popular local wallet available in South Korea.
    #[must_use]
    pub fn kakao_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("kakao_pay".to_owned(), value);
        self
    }

    /// Klarna gives customers a range of \[payment options\](https://docs.stripe.com/payments/klarna#payment-options) during checkout. Available payment options vary depending on the customer's billing address and the transaction amount. These pa…
    #[must_use]
    pub fn klarna(mut self, value: serde_json::Value) -> Self {
        self.params.insert("klarna".to_owned(), value);
        self
    }

    /// Konbini allows customers in Japan to pay for bills and online purchases at convenience stores with cash. Check this \[page\](https://docs.stripe.com/payments/konbini) for more details.
    #[must_use]
    pub fn konbini(mut self, value: serde_json::Value) -> Self {
        self.params.insert("konbini".to_owned(), value);
        self
    }

    /// Korean cards let users pay using locally issued cards from South Korea.
    #[must_use]
    pub fn kr_card(mut self, value: serde_json::Value) -> Self {
        self.params.insert("kr_card".to_owned(), value);
        self
    }

    /// \[Link\](https://docs.stripe.com/payments/link) is a payment method network. With Link, users save their payment details once, then reuse that information to pay with one click for any business on the network.
    #[must_use]
    pub fn link(mut self, value: serde_json::Value) -> Self {
        self.params.insert("link".to_owned(), value);
        self
    }

    /// MB WAY is the most popular wallet in Portugal. After entering their phone number in your checkout, customers approve the payment directly in their MB WAY app. Check this \[page\](https://docs.stripe.com/payments/mb-way) for more details.
    #[must_use]
    pub fn mb_way(mut self, value: serde_json::Value) -> Self {
        self.params.insert("mb_way".to_owned(), value);
        self
    }

    /// MobilePay is a \[single-use\](https://docs.stripe.com/payments/payment-methods#usage) card wallet payment method used in Denmark and Finland. It allows customers to \[authenticate and approve\](https://docs.stripe.com/payments/payment-meth…
    #[must_use]
    pub fn mobilepay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("mobilepay".to_owned(), value);
        self
    }

    /// Stripe users in Europe and the United States can accept Multibanco payments from customers in Portugal using \[Sources\](https://stripe.com/docs/sources)—a single integration path for creating payments using any supported method.
    #[must_use]
    pub fn multibanco(mut self, value: serde_json::Value) -> Self {
        self.params.insert("multibanco".to_owned(), value);
        self
    }

    /// Configuration name.
    #[must_use]
    pub fn name(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Naver Pay is a popular local wallet available in South Korea.
    #[must_use]
    pub fn naver_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("naver_pay".to_owned(), value);
        self
    }

    /// Stripe users in New Zealand can accept Bulk Electronic Clearing System (BECS) direct debit payments from customers with a New Zeland bank account. Check this \[page\](https://docs.stripe.com/payments/nz-bank-account) for more details.
    #[must_use]
    pub fn nz_bank_account(mut self, value: serde_json::Value) -> Self {
        self.params.insert("nz_bank_account".to_owned(), value);
        self
    }

    /// OXXO is a Mexican chain of convenience stores with thousands of locations across Latin America and represents nearly 20% of online transactions in Mexico. OXXO allows customers to pay bills and online purchases in-store with cash. Check th…
    #[must_use]
    pub fn oxxo(mut self, value: serde_json::Value) -> Self {
        self.params.insert("oxxo".to_owned(), value);
        self
    }

    /// Przelewy24 is a Poland-based payment method aggregator that allows customers to complete transactions online using bank transfers and other methods. Bank transfers account for 30% of online payments in Poland and Przelewy24 provides a way…
    #[must_use]
    pub fn p24(mut self, value: serde_json::Value) -> Self {
        self.params.insert("p24".to_owned(), value);
        self
    }

    /// Pay by bank is a redirect payment method backed by bank transfers. A customer is redirected to their bank to authorize a bank transfer for a given amount. This removes a lot of the error risks inherent in waiting for the customer to initia…
    #[must_use]
    pub fn pay_by_bank(mut self, value: serde_json::Value) -> Self {
        self.params.insert("pay_by_bank".to_owned(), value);
        self
    }

    /// PAYCO is a \[single-use\](https://docs.stripe.com/payments/payment-methods#usage local wallet available in South Korea.
    #[must_use]
    pub fn payco(mut self, value: serde_json::Value) -> Self {
        self.params.insert("payco".to_owned(), value);
        self
    }

    /// PayNow is a Singapore-based payment method that allows customers to make a payment using their preferred app from participating banks and participating non-bank financial institutions. Check this \[page\](https://docs.stripe.com/payments/p…
    #[must_use]
    pub fn paynow(mut self, value: serde_json::Value) -> Self {
        self.params.insert("paynow".to_owned(), value);
        self
    }

    /// PayPal, a digital wallet popular with customers in Europe, allows your customers worldwide to pay using their PayPal account. Check this \[page\](https://docs.stripe.com/payments/paypal) for more details.
    #[must_use]
    pub fn paypal(mut self, value: serde_json::Value) -> Self {
        self.params.insert("paypal".to_owned(), value);
        self
    }

    /// PayTo is a \[real-time\](https://docs.stripe.com/payments/real-time) payment method that enables customers in Australia to pay by providing their bank account details. Customers must accept a mandate authorizing you to debit their account.…
    #[must_use]
    pub fn payto(mut self, value: serde_json::Value) -> Self {
        self.params.insert("payto".to_owned(), value);
        self
    }

    /// Pix is a payment method popular in Brazil. When paying with Pix, customers authenticate and approve payments by scanning a QR code in their preferred banking app. Check this \[page\](https://docs.stripe.com/payments/pix) for more details.
    #[must_use]
    pub fn pix(mut self, value: serde_json::Value) -> Self {
        self.params.insert("pix".to_owned(), value);
        self
    }

    /// PromptPay is a Thailand-based payment method that allows customers to make a payment using their preferred app from participating banks. Check this \[page\](https://docs.stripe.com/payments/promptpay) for more details.
    #[must_use]
    pub fn promptpay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("promptpay".to_owned(), value);
        self
    }

    /// Revolut Pay, developed by Revolut, a global finance app, is a digital wallet payment method. Revolut Pay uses the customer’s stored balance or cards to fund the payment, and offers the option for non-Revolut customers to save their details…
    #[must_use]
    pub fn revolut_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("revolut_pay".to_owned(), value);
        self
    }

    /// Samsung Pay is a \[single-use\](https://docs.stripe.com/payments/payment-methods#usage local wallet available in South Korea.
    #[must_use]
    pub fn samsung_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("samsung_pay".to_owned(), value);
        self
    }

    /// Satispay is a \[single-use\](/payments/payment-methods#usage) payment method where customers are required to \[authenticate\](/payments/payment-methods#customer-actions) their payment. Customers pay by being redirected from your website or…
    #[must_use]
    pub fn satispay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("satispay".to_owned(), value);
        self
    }

    /// Scalapay is a \[single-use\](/payments/payment-methods#usage) payment method that lets customers pay in 3 or 4 installments. Customers are redirected from your website or app, authorize the payment with Scalapay, then return to your websit…
    #[must_use]
    pub fn scalapay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("scalapay".to_owned(), value);
        self
    }

    /// The \[Single Euro Payments Area (SEPA)\](https://en.wikipedia.org/wiki/Single_Euro_Payments_Area) is an initiative of the European Union to simplify payments within and across member countries. SEPA established and enforced banking standar…
    #[must_use]
    pub fn sepa_debit(mut self, value: serde_json::Value) -> Self {
        self.params.insert("sepa_debit".to_owned(), value);
        self
    }

    /// Stripe users in Europe and the United States can use the \[Payment Intents API\](https://stripe.com/docs/payments/payment-intents)—a single integration path for creating payments using any supported method—to accept \[Sofort\](https://www.…
    #[must_use]
    pub fn sofort(mut self, value: serde_json::Value) -> Self {
        self.params.insert("sofort".to_owned(), value);
        self
    }

    /// Sunbit is a \[single-use\](https://docs.stripe.com/payments/payment-methods#usage) payment method where customers choose to pay in 3, 6, or 12 installments. Customers are redirected from your website or app, authorize the payment with Sunb…
    #[must_use]
    pub fn sunbit(mut self, value: serde_json::Value) -> Self {
        self.params.insert("sunbit".to_owned(), value);
        self
    }

    /// Swish is a \[real-time\](https://docs.stripe.com/payments/real-time) payment method popular in Sweden. It allows customers to \[authenticate and approve\](https://docs.stripe.com/payments/payment-methods#customer-actions) payments using th…
    #[must_use]
    pub fn swish(mut self, value: serde_json::Value) -> Self {
        self.params.insert("swish".to_owned(), value);
        self
    }

    /// Twint is a payment method popular in Switzerland. It allows customers to pay using their mobile phone. Check this \[page\](https://docs.stripe.com/payments/twint) for more details.
    #[must_use]
    pub fn twint(mut self, value: serde_json::Value) -> Self {
        self.params.insert("twint".to_owned(), value);
        self
    }

    /// Unified Payment Interface (UPI) is India's leading payment method with exponential growth since it launched in 2016.
    #[must_use]
    pub fn upi(mut self, value: serde_json::Value) -> Self {
        self.params.insert("upi".to_owned(), value);
        self
    }

    /// Stripe users in the United States can accept ACH direct debit payments from customers with a US bank account using the Automated Clearing House (ACH) payments system operated by Nacha. Check this \[page\](https://docs.stripe.com/payments/a…
    #[must_use]
    pub fn us_bank_account(mut self, value: serde_json::Value) -> Self {
        self.params.insert("us_bank_account".to_owned(), value);
        self
    }

    /// WeChat, owned by Tencent, is China's leading mobile app with over 1 billion monthly active users. Chinese consumers can use WeChat Pay to pay for goods and services inside of businesses' apps and websites. WeChat Pay users buy most frequen…
    #[must_use]
    pub fn wechat_pay(mut self, value: serde_json::Value) -> Self {
        self.params.insert("wechat_pay".to_owned(), value);
        self
    }

    /// Zip gives your customers a way to split purchases over a series of payments. Check this \[page\](https://docs.stripe.com/payments/zip) for more details like country availability.
    #[must_use]
    pub fn zip(mut self, value: serde_json::Value) -> Self {
        self.params.insert("zip".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<crate::models::PaymentMethodConfiguration, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
