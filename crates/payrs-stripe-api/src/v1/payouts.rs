//! Generated operations for the `payouts` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(clippy::doc_markdown, clippy::too_many_arguments, clippy::new_without_default, rustdoc::bare_urls)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/payouts`
///
/// Returns a list of existing payouts sent to third-party bank accounts or payouts that Stripe sent to you. The payouts return in sorted order, with the most recently created payouts appearing first.
#[derive(Debug, Clone)]
pub struct GetPayouts {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetPayouts {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/payouts".to_owned(), params }
    }

    /// Only return payouts that are expected to arrive during the given date interval.
    #[must_use]
    pub fn arrival_date(mut self, value: serde_json::Value) -> Self {
        self.params.insert("arrival_date".to_owned(), value);
        self
    }

    /// Only return payouts that were created during the given date interval.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// The ID of an external account - only return payouts sent to this external account.
    #[must_use]
    pub fn destination(mut self, value: impl Into<String>) -> Self {
        self.params.insert("destination".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Only return payouts that have the given status: `pending`, `paid`, `failed`, or `canceled`.
    #[must_use]
    pub fn status(mut self, value: impl Into<String>) -> Self {
        self.params.insert("status".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Payout> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::Payout>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/payouts`
///
/// To send funds to your own bank account, create a new payout object. Your Stripe balance must cover the payout amount. If it doesn’t, you receive an “Insufficient Funds” error. If your API key is in test mode, money won’t actually be sent, though every other action occurs as if you’re in live mode. If you create a manual payout on a Stripe account that uses multiple payment source types, you need to specify the source type balance that the payout draws from. The balance object details available…
#[derive(Debug, Clone)]
pub struct PostPayouts {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPayouts {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(amount: i64, currency: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("amount".to_owned(), serde_json::Value::from(amount));
        params.insert("currency".to_owned(), serde_json::Value::String(currency.into()));
        Self { path: "/v1/payouts".to_owned(), params }
    }

    /// An arbitrary string attached to the object. Often useful for displaying to users.
    #[must_use]
    pub fn description(mut self, value: impl Into<String>) -> Self {
        self.params.insert("description".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The ID of a bank account or a card to send the payout to. If you don't provide a destination, we use the default external account for the specified currency.
    #[must_use]
    pub fn destination(mut self, value: impl Into<String>) -> Self {
        self.params.insert("destination".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert("metadata".to_owned(), serde_json::Value::Object(value.into_iter().map(|(k, v)| (k, serde_json::Value::String(v))).collect()));
        self
    }

    /// The method used to send this payout, which is `standard` or `instant`. We support `instant` for payouts to debit cards and bank accounts in certain countries. Learn more about \[bank support for Instant Payouts\](https://stripe.com/docs/pa…
    #[must_use]
    pub fn method(mut self, value: impl Into<String>) -> Self {
        self.params.insert("method".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The ID of a v2 FinancialAccount to send funds to.
    #[must_use]
    pub fn payout_method(mut self, value: impl Into<String>) -> Self {
        self.params.insert("payout_method".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The balance type of your Stripe balance to draw this payout from. Balances for different payment sources are kept separately. You can find the amounts with the Balances API. One of `bank_account`, `card`, or `fpx`.
    #[must_use]
    pub fn source_type(mut self, value: impl Into<String>) -> Self {
        self.params.insert("source_type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A string that displays on the recipient's bank or card statement (up to 22 characters). A `statement_descriptor` that's longer than 22 characters return an error. Most banks truncate this information and display it inconsistently. Some ban…
    #[must_use]
    pub fn statement_descriptor(mut self, value: impl Into<String>) -> Self {
        self.params.insert("statement_descriptor".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Payout, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/payouts/{payout}`
///
/// Retrieves the details of an existing payout. Supply the unique payout ID from either a payout creation request or the payout list. Stripe returns the corresponding payout information.
#[derive(Debug, Clone)]
pub struct GetPayoutsPayout {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetPayoutsPayout {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(payout: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let payout = payout.into();
        Self { path: format!("/v1/payouts/{payout}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Payout, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/payouts/{payout}`
///
/// Updates the specified payout by setting the values of the parameters you pass. We don’t change parameters that you don’t provide. This request only accepts the metadata as arguments.
#[derive(Debug, Clone)]
pub struct PostPayoutsPayout {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPayoutsPayout {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(payout: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let payout = payout.into();
        Self { path: format!("/v1/payouts/{payout}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Payout, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/payouts/{payout}/cancel`
///
/// You can cancel a previously created payout if its status is pending. Stripe refunds the funds to your available balance. You can’t cancel automatic Stripe payouts.
#[derive(Debug, Clone)]
pub struct PostPayoutsPayoutCancel {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPayoutsPayoutCancel {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(payout: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let payout = payout.into();
        Self { path: format!("/v1/payouts/{payout}/cancel"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Payout, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/payouts/{payout}/reverse`
///
/// Reverses a payout by debiting the destination bank account. At this time, you can only reverse payouts for connected accounts to US and Canadian bank accounts. If the payout is manual and in the pending status, use /v1/payouts/:id/cancel instead. By requesting a reversal through /v1/payouts/:id/reverse, you confirm that the authorized signatory of the selected bank account authorizes the debit on the bank account and that no other authorization is required.
#[derive(Debug, Clone)]
pub struct PostPayoutsPayoutReverse {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostPayoutsPayoutReverse {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(payout: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let payout = payout.into();
        Self { path: format!("/v1/payouts/{payout}/reverse"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert("metadata".to_owned(), serde_json::Value::Object(value.into_iter().map(|(k, v)| (k, serde_json::Value::String(v))).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Payout, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
