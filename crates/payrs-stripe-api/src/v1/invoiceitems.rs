//! Generated operations for the `invoiceitems` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(clippy::doc_markdown, clippy::too_many_arguments, clippy::new_without_default, rustdoc::bare_urls)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/invoiceitems`
///
/// Returns a list of your invoice items. Invoice items are returned sorted by creation date, with the most recently created invoice items appearing first.
#[derive(Debug, Clone)]
pub struct GetInvoiceitems {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetInvoiceitems {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/invoiceitems".to_owned(), params }
    }

    /// Only return invoice items that were created during the given date interval.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// The identifier of the customer whose invoice items to return. If none is provided, returns all invoice items.
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The identifier of the account representing the customer whose invoice items to return. If none is provided, returns all invoice items.
    #[must_use]
    pub fn customer_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Only return invoice items belonging to this invoice. If none is provided, all invoice items will be returned. If specifying an invoice, no customer identifier is needed.
    #[must_use]
    pub fn invoice(mut self, value: impl Into<String>) -> Self {
        self.params.insert("invoice".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Set to `true` to only show pending invoice items, which are not yet attached to any invoices. Set to `false` to only show invoice items already attached to invoices. If unspecified, no filter is applied.
    #[must_use]
    pub fn pending(mut self, value: bool) -> Self {
        self.params.insert("pending".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Invoiceitem> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::Invoiceitem>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/invoiceitems`
///
/// Creates an item to be added to a draft invoice (up to 250 items per invoice). If no invoice is specified, the item will be on the next invoice created for the customer specified.
#[derive(Debug, Clone)]
pub struct PostInvoiceitems {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostInvoiceitems {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/invoiceitems".to_owned(), params }
    }

    /// The integer amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. Passing in a negative `amount` will reduce the `amount_due` on the invoice.
    #[must_use]
    pub fn amount(mut self, value: i64) -> Self {
        self.params.insert("amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Three-letter \[ISO currency code\](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a \[supported currency\](https://stripe.com/docs/currencies).
    #[must_use]
    pub fn currency(mut self, value: impl Into<String>) -> Self {
        self.params.insert("currency".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The ID of the customer to bill for this invoice item.
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The ID of the account representing the customer to bill for this invoice item.
    #[must_use]
    pub fn customer_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// An arbitrary string which you can attach to the invoice item. The description is displayed in the invoice for easy tracking.
    #[must_use]
    pub fn description(mut self, value: impl Into<String>) -> Self {
        self.params.insert("description".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Controls whether discounts apply to this invoice item. Defaults to false for prorations or negative invoice items, and true for all other invoice items.
    #[must_use]
    pub fn discountable(mut self, value: bool) -> Self {
        self.params.insert("discountable".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// The coupons and promotion codes to redeem into discounts for the invoice item or invoice line item.
    #[must_use]
    pub fn discounts(mut self, value: serde_json::Value) -> Self {
        self.params.insert("discounts".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// The ID of an existing invoice to add this invoice item to. For subscription invoices, when left blank, the invoice item will be added to the next upcoming scheduled invoice. For standalone invoices, the invoice item won't be automatically…
    #[must_use]
    pub fn invoice(mut self, value: impl Into<String>) -> Self {
        self.params.insert("invoice".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// The period associated with this invoice item. When set to different values, the period will be rendered on the invoice. If you have \[Stripe Revenue Recognition\](https://docs.stripe.com/revenue-recognition) enabled, the period will be use…
    #[must_use]
    pub fn period(mut self, value: serde_json::Value) -> Self {
        self.params.insert("period".to_owned(), value);
        self
    }

    /// Data used to generate a new \[Price\](https://docs.stripe.com/api/prices) object inline.
    #[must_use]
    pub fn price_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("price_data".to_owned(), value);
        self
    }

    /// The pricing information for the invoice item.
    #[must_use]
    pub fn pricing(mut self, value: serde_json::Value) -> Self {
        self.params.insert("pricing".to_owned(), value);
        self
    }

    /// Non-negative integer. The quantity of units for the invoice item. Use `quantity_decimal` instead to provide decimal precision. This field will be deprecated in favor of `quantity_decimal` in a future version.
    #[must_use]
    pub fn quantity(mut self, value: i64) -> Self {
        self.params.insert("quantity".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Non-negative decimal with at most 12 decimal places. The quantity of units for the invoice item.
    #[must_use]
    pub fn quantity_decimal(mut self, value: impl Into<String>) -> Self {
        self.params.insert("quantity_decimal".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The ID of a subscription to add this invoice item to. When left blank, the invoice item is added to the next upcoming scheduled invoice. When set, scheduled invoices for subscriptions other than the specified subscription will ignore the i…
    #[must_use]
    pub fn subscription(mut self, value: impl Into<String>) -> Self {
        self.params.insert("subscription".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Only required if a \[default tax behavior\](https://docs.stripe.com/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is c…
    #[must_use]
    pub fn tax_behavior(mut self, value: impl Into<String>) -> Self {
        self.params.insert("tax_behavior".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A \[tax code\](https://docs.stripe.com/tax/tax-categories) ID.
    #[must_use]
    pub fn tax_code(mut self, value: serde_json::Value) -> Self {
        self.params.insert("tax_code".to_owned(), value);
        self
    }

    /// The tax rates which apply to the invoice item. When set, the `default_tax_rates` on the invoice do not apply to this invoice item.
    #[must_use]
    pub fn tax_rates(mut self, value: Vec<String>) -> Self {
        self.params.insert("tax_rates".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// The decimal unit amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. This `unit_amount_decimal` will be multiplied by the quantity to get the full amount. Passing in a negative `unit_amount_decimal` w…
    #[must_use]
    pub fn unit_amount_decimal(mut self, value: impl Into<String>) -> Self {
        self.params.insert("unit_amount_decimal".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Invoiceitem, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/invoiceitems/{invoiceitem}`
///
/// Retrieves the invoice item with the given ID.
#[derive(Debug, Clone)]
pub struct GetInvoiceitemsInvoiceitem {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetInvoiceitemsInvoiceitem {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(invoiceitem: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let invoiceitem = invoiceitem.into();
        Self { path: format!("/v1/invoiceitems/{invoiceitem}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Invoiceitem, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/invoiceitems/{invoiceitem}`
///
/// Updates the amount or description of an invoice item on an upcoming invoice. Updating an invoice item is only possible before the invoice it’s attached to is closed.
#[derive(Debug, Clone)]
pub struct PostInvoiceitemsInvoiceitem {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostInvoiceitemsInvoiceitem {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(invoiceitem: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let invoiceitem = invoiceitem.into();
        Self { path: format!("/v1/invoiceitems/{invoiceitem}"), params }
    }

    /// The integer amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. If you want to apply a credit to the customer's account, pass a negative amount.
    #[must_use]
    pub fn amount(mut self, value: i64) -> Self {
        self.params.insert("amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// An arbitrary string which you can attach to the invoice item. The description is displayed in the invoice for easy tracking.
    #[must_use]
    pub fn description(mut self, value: impl Into<String>) -> Self {
        self.params.insert("description".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Controls whether discounts apply to this invoice item. Defaults to false for prorations or negative invoice items, and true for all other invoice items. Cannot be set to true for prorations.
    #[must_use]
    pub fn discountable(mut self, value: bool) -> Self {
        self.params.insert("discountable".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// The coupons, promotion codes & existing discounts which apply to the invoice item or invoice line item. Item discounts are applied before invoice discounts. Pass an empty string to remove previously-defined discounts.
    #[must_use]
    pub fn discounts(mut self, value: serde_json::Value) -> Self {
        self.params.insert("discounts".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// The period associated with this invoice item. When set to different values, the period will be rendered on the invoice. If you have \[Stripe Revenue Recognition\](https://docs.stripe.com/revenue-recognition) enabled, the period will be use…
    #[must_use]
    pub fn period(mut self, value: serde_json::Value) -> Self {
        self.params.insert("period".to_owned(), value);
        self
    }

    /// Data used to generate a new \[Price\](https://docs.stripe.com/api/prices) object inline.
    #[must_use]
    pub fn price_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("price_data".to_owned(), value);
        self
    }

    /// The pricing information for the invoice item.
    #[must_use]
    pub fn pricing(mut self, value: serde_json::Value) -> Self {
        self.params.insert("pricing".to_owned(), value);
        self
    }

    /// Non-negative integer. The quantity of units for the invoice item. Use `quantity_decimal` instead to provide decimal precision. This field will be deprecated in favor of `quantity_decimal` in a future version.
    #[must_use]
    pub fn quantity(mut self, value: i64) -> Self {
        self.params.insert("quantity".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Non-negative decimal with at most 12 decimal places. The quantity of units for the line item.
    #[must_use]
    pub fn quantity_decimal(mut self, value: impl Into<String>) -> Self {
        self.params.insert("quantity_decimal".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Only required if a \[default tax behavior\](https://docs.stripe.com/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is c…
    #[must_use]
    pub fn tax_behavior(mut self, value: impl Into<String>) -> Self {
        self.params.insert("tax_behavior".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A \[tax code\](https://docs.stripe.com/tax/tax-categories) ID.
    #[must_use]
    pub fn tax_code(mut self, value: serde_json::Value) -> Self {
        self.params.insert("tax_code".to_owned(), value);
        self
    }

    /// The tax rates which apply to the invoice item. When set, the `default_tax_rates` on the invoice do not apply to this invoice item. Pass an empty string to remove previously-defined tax rates.
    #[must_use]
    pub fn tax_rates(mut self, value: serde_json::Value) -> Self {
        self.params.insert("tax_rates".to_owned(), value);
        self
    }

    /// The decimal unit amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. This `unit_amount_decimal` will be multiplied by the quantity to get the full amount. Passing in a negative `unit_amount_decimal` w…
    #[must_use]
    pub fn unit_amount_decimal(mut self, value: impl Into<String>) -> Self {
        self.params.insert("unit_amount_decimal".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Invoiceitem, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `DELETE /v1/invoiceitems/{invoiceitem}`
///
/// Deletes an invoice item, removing it from an invoice. Deleting invoice items is only possible when they’re not attached to invoices, or if it’s attached to a draft invoice.
#[derive(Debug, Clone)]
pub struct DeleteInvoiceitemsInvoiceitem {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteInvoiceitemsInvoiceitem {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(invoiceitem: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let invoiceitem = invoiceitem.into();
        Self { path: format!("/v1/invoiceitems/{invoiceitem}"), params }
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::DeletedInvoiceitem, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
