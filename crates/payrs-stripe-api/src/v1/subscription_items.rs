//! Generated operations for the `subscription_items` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(clippy::doc_markdown, clippy::too_many_arguments, clippy::new_without_default, rustdoc::bare_urls)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/subscription_items`
///
/// Returns a list of your subscription items for a given subscription.
#[derive(Debug, Clone)]
pub struct GetSubscriptionItems {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetSubscriptionItems {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(subscription: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("subscription".to_owned(), serde_json::Value::String(subscription.into()));
        Self { path: "/v1/subscription_items".to_owned(), params }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::SubscriptionItem> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::SubscriptionItem>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/subscription_items`
///
/// Adds a new item to an existing subscription. No existing items will be changed or replaced.
#[derive(Debug, Clone)]
pub struct PostSubscriptionItems {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostSubscriptionItems {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(subscription: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("subscription".to_owned(), serde_json::Value::String(subscription.into()));
        Self { path: "/v1/subscription_items".to_owned(), params }
    }

    /// Define thresholds at which an invoice will be sent, and the subscription advanced to a new billing period. Pass an empty string to remove previously-defined thresholds.
    #[must_use]
    pub fn billing_thresholds(mut self, value: serde_json::Value) -> Self {
        self.params.insert("billing_thresholds".to_owned(), value);
        self
    }

    /// The coupons to redeem into discounts for the subscription item.
    #[must_use]
    pub fn discounts(mut self, value: serde_json::Value) -> Self {
        self.params.insert("discounts".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert("metadata".to_owned(), serde_json::Value::Object(value.into_iter().map(|(k, v)| (k, serde_json::Value::String(v))).collect()));
        self
    }

    /// Controls how Stripe handles payment when a subscription update requires payment and `collection_method=charge_automatically`.
    #[must_use]
    pub fn payment_behavior(mut self, value: impl Into<String>) -> Self {
        self.params.insert("payment_behavior".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The ID of the price object.
    #[must_use]
    pub fn price(mut self, value: impl Into<String>) -> Self {
        self.params.insert("price".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Data used to generate a new \[Price\](https://docs.stripe.com/api/prices) object inline.
    #[must_use]
    pub fn price_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("price_data".to_owned(), value);
        self
    }

    /// Determines how to handle \[prorations\](https://docs.stripe.com/billing/subscriptions/prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quan…
    #[must_use]
    pub fn proration_behavior(mut self, value: impl Into<String>) -> Self {
        self.params.insert("proration_behavior".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// If set, the proration will be calculated as though the subscription was updated at the given time. This can be used to apply the same proration that was previewed with the \[upcoming invoice\](/api/invoices/create_preview) endpoint.
    #[must_use]
    pub fn proration_date(mut self, value: i64) -> Self {
        self.params.insert("proration_date".to_owned(), serde_json::Value::from(value));
        self
    }

    /// The quantity you'd like to apply to the subscription item you're creating.
    #[must_use]
    pub fn quantity(mut self, value: i64) -> Self {
        self.params.insert("quantity".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A list of \[Tax Rate\](https://docs.stripe.com/api/tax_rates) ids. These Tax Rates will override the \[`default_tax_rates`\](https://docs.stripe.com/api/subscriptions/create#create_subscription-default_tax_rates) on the Subscription. When…
    #[must_use]
    pub fn tax_rates(mut self, value: serde_json::Value) -> Self {
        self.params.insert("tax_rates".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::SubscriptionItem, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/subscription_items/{item}`
///
/// Retrieves the subscription item with the given ID.
#[derive(Debug, Clone)]
pub struct GetSubscriptionItemsItem {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetSubscriptionItemsItem {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(item: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let item = item.into();
        Self { path: format!("/v1/subscription_items/{item}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::SubscriptionItem, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/subscription_items/{item}`
///
/// Updates the plan or quantity of an item on a current subscription.
#[derive(Debug, Clone)]
pub struct PostSubscriptionItemsItem {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostSubscriptionItemsItem {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(item: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let item = item.into();
        Self { path: format!("/v1/subscription_items/{item}"), params }
    }

    /// Define thresholds at which an invoice will be sent, and the subscription advanced to a new billing period. Pass an empty string to remove previously-defined thresholds.
    #[must_use]
    pub fn billing_thresholds(mut self, value: serde_json::Value) -> Self {
        self.params.insert("billing_thresholds".to_owned(), value);
        self
    }

    /// The coupons to redeem into discounts for the subscription item.
    #[must_use]
    pub fn discounts(mut self, value: serde_json::Value) -> Self {
        self.params.insert("discounts".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `false` (on-session).
    #[must_use]
    pub fn off_session(mut self, value: bool) -> Self {
        self.params.insert("off_session".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Controls how Stripe handles payment when a subscription update requires payment and `collection_method=charge_automatically`.
    #[must_use]
    pub fn payment_behavior(mut self, value: impl Into<String>) -> Self {
        self.params.insert("payment_behavior".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The ID of the price object. One of `price` or `price_data` is required. When changing a subscription item's price, `quantity` is set to 1 unless a `quantity` parameter is provided.
    #[must_use]
    pub fn price(mut self, value: impl Into<String>) -> Self {
        self.params.insert("price".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Data used to generate a new \[Price\](https://docs.stripe.com/api/prices) object inline. One of `price` or `price_data` is required.
    #[must_use]
    pub fn price_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("price_data".to_owned(), value);
        self
    }

    /// Determines how to handle \[prorations\](https://docs.stripe.com/billing/subscriptions/prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quan…
    #[must_use]
    pub fn proration_behavior(mut self, value: impl Into<String>) -> Self {
        self.params.insert("proration_behavior".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// If set, the proration will be calculated as though the subscription was updated at the given time. This can be used to apply the same proration that was previewed with the \[upcoming invoice\](/api/invoices/create_preview) endpoint.
    #[must_use]
    pub fn proration_date(mut self, value: i64) -> Self {
        self.params.insert("proration_date".to_owned(), serde_json::Value::from(value));
        self
    }

    /// The quantity you'd like to apply to the subscription item you're creating.
    #[must_use]
    pub fn quantity(mut self, value: i64) -> Self {
        self.params.insert("quantity".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A list of \[Tax Rate\](https://docs.stripe.com/api/tax_rates) ids. These Tax Rates will override the \[`default_tax_rates`\](https://docs.stripe.com/api/subscriptions/create#create_subscription-default_tax_rates) on the Subscription. When…
    #[must_use]
    pub fn tax_rates(mut self, value: serde_json::Value) -> Self {
        self.params.insert("tax_rates".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::SubscriptionItem, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `DELETE /v1/subscription_items/{item}`
///
/// Deletes an item from the subscription. Removing a subscription item from a subscription will not cancel the subscription.
#[derive(Debug, Clone)]
pub struct DeleteSubscriptionItemsItem {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteSubscriptionItemsItem {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(item: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let item = item.into();
        Self { path: format!("/v1/subscription_items/{item}"), params }
    }

    /// Delete all usage for the given subscription item. Allowed only when the current plan's `usage_type` is `metered`.
    #[must_use]
    pub fn clear_usage(mut self, value: bool) -> Self {
        self.params.insert("clear_usage".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Controls how Stripe handles payment when a subscription update requires payment and `collection_method=charge_automatically`.
    #[must_use]
    pub fn payment_behavior(mut self, value: impl Into<String>) -> Self {
        self.params.insert("payment_behavior".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Determines how to handle \[prorations\](https://docs.stripe.com/billing/subscriptions/prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quan…
    #[must_use]
    pub fn proration_behavior(mut self, value: impl Into<String>) -> Self {
        self.params.insert("proration_behavior".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// If set, the proration will be calculated as though the subscription was updated at the given time. This can be used to apply the same proration that was previewed with the \[upcoming invoice\](/api/invoices/create_preview) endpoint.
    #[must_use]
    pub fn proration_date(mut self, value: i64) -> Self {
        self.params.insert("proration_date".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::DeletedSubscriptionItem, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
