//! Generated operations for the `ephemeral_keys` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(
    clippy::doc_markdown,
    clippy::too_many_arguments,
    clippy::new_without_default,
    rustdoc::bare_urls
)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `POST /v1/ephemeral_keys`
///
/// Creates a short-lived API key for a given resource.
#[derive(Debug, Clone)]
pub struct PostEphemeralKeys {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostEphemeralKeys {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/ephemeral_keys".to_owned(),
            params,
        }
    }

    /// The ID of the Customer you'd like to modify using the resulting ephemeral key.
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "customer".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// The ID of the Issuing Card you'd like to access using the resulting ephemeral key.
    #[must_use]
    pub fn issuing_card(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "issuing_card".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// A single-use token, created by Stripe.js, used for creating ephemeral keys for Issuing Cards without exchanging sensitive information.
    #[must_use]
    pub fn nonce(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("nonce".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The ID of the Identity VerificationSession you'd like to access using the resulting ephemeral key
    #[must_use]
    pub fn verification_session(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "verification_session".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::EphemeralKey, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `DELETE /v1/ephemeral_keys/{key}`
///
/// Invalidates a short-lived API key for a given resource.
#[derive(Debug, Clone)]
pub struct DeleteEphemeralKeysKey {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteEphemeralKeysKey {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(key: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let key = key.into();
        Self {
            path: format!("/v1/ephemeral_keys/{key}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::EphemeralKey, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
