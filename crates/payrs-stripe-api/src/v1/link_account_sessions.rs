//! Generated operations for the `link_account_sessions` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(
    clippy::doc_markdown,
    clippy::too_many_arguments,
    clippy::new_without_default,
    rustdoc::bare_urls
)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `POST /v1/link_account_sessions`
///
/// To launch the Financial Connections authorization flow, create a Session. The session’s client_secret can be used to launch the flow using Stripe.js.
#[derive(Debug, Clone)]
pub struct PostLinkAccountSessions {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostLinkAccountSessions {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account_holder: serde_json::Value, permissions: serde_json::Value) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("account_holder".to_owned(), account_holder);
        params.insert("permissions".to_owned(), permissions);
        Self {
            path: "/v1/link_account_sessions".to_owned(),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Filters to restrict the kinds of accounts to collect.
    #[must_use]
    pub fn filters(mut self, value: serde_json::Value) -> Self {
        self.params.insert("filters".to_owned(), value);
        self
    }

    /// List of data features that you would like to retrieve upon account creation.
    #[must_use]
    pub fn prefetch(mut self, value: serde_json::Value) -> Self {
        self.params.insert("prefetch".to_owned(), value);
        self
    }

    /// For webview integrations only. Upon completing OAuth login in the native browser, the user will be redirected to this URL to return to your app.
    #[must_use]
    pub fn return_url(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "return_url".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<crate::models::FinancialConnectionsSession, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/link_account_sessions/{session}`
///
/// Retrieves the details of a Financial Connections Session
#[derive(Debug, Clone)]
pub struct GetLinkAccountSessionsSession {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetLinkAccountSessionsSession {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(session: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let session = session.into();
        Self {
            path: format!("/v1/link_account_sessions/{session}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<crate::models::FinancialConnectionsSession, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}
