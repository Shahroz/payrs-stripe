//! Generated operations for the `sources` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(clippy::doc_markdown, clippy::too_many_arguments, clippy::new_without_default, rustdoc::bare_urls)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `POST /v1/sources`
///
/// Creates a new source object.
#[derive(Debug, Clone)]
pub struct PostSources {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostSources {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/sources".to_owned(), params }
    }

    /// Amount associated with the source. This is the amount for which the source will be chargeable once ready. Required for `single_use` sources. Not supported for `receiver` type sources, where charge amount may not be specified until funds la…
    #[must_use]
    pub fn amount(mut self, value: i64) -> Self {
        self.params.insert("amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Three-letter \[ISO code for the currency\](https://stripe.com/docs/currencies) associated with the source. This is the currency for which the source will be chargeable once ready.
    #[must_use]
    pub fn currency(mut self, value: impl Into<String>) -> Self {
        self.params.insert("currency".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The `Customer` to whom the original source is attached to. Must be set when the original source is not a `Source` (e.g., `Card`).
    #[must_use]
    pub fn customer(mut self, value: impl Into<String>) -> Self {
        self.params.insert("customer".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// The authentication `flow` of the source to create. `flow` is one of `redirect`, `receiver`, `code_verification`, `none`. It is generally inferred unless a type supports multiple flows.
    #[must_use]
    pub fn flow(mut self, value: impl Into<String>) -> Self {
        self.params.insert("flow".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Information about a mandate possibility attached to a source object (generally for bank debits) as well as its acceptance status.
    #[must_use]
    pub fn mandate(mut self, value: serde_json::Value) -> Self {
        self.params.insert("mandate".to_owned(), value);
        self
    }

    /// Set the `metadata` parameter.
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert("metadata".to_owned(), serde_json::Value::Object(value.into_iter().map(|(k, v)| (k, serde_json::Value::String(v))).collect()));
        self
    }

    /// The source to share.
    #[must_use]
    pub fn original_source(mut self, value: impl Into<String>) -> Self {
        self.params.insert("original_source".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Information about the owner of the payment instrument that may be used or required by particular source types.
    #[must_use]
    pub fn owner(mut self, value: serde_json::Value) -> Self {
        self.params.insert("owner".to_owned(), value);
        self
    }

    /// Optional parameters for the receiver flow. Can be set only if the source is a receiver (`flow` is `receiver`).
    #[must_use]
    pub fn receiver(mut self, value: serde_json::Value) -> Self {
        self.params.insert("receiver".to_owned(), value);
        self
    }

    /// Parameters required for the redirect flow. Required if the source is authenticated by a redirect (`flow` is `redirect`).
    #[must_use]
    pub fn redirect(mut self, value: serde_json::Value) -> Self {
        self.params.insert("redirect".to_owned(), value);
        self
    }

    /// Information about the items and shipping associated with the source. Required for transactional credit (for example Klarna) sources before you can charge it.
    #[must_use]
    pub fn source_order(mut self, value: serde_json::Value) -> Self {
        self.params.insert("source_order".to_owned(), value);
        self
    }

    /// An arbitrary string to be displayed on your customer's statement. As an example, if your website is `RunClub` and the item you're charging for is a race ticket, you may want to specify a `statement_descriptor` of `RunClub 5K race ticket.`…
    #[must_use]
    pub fn statement_descriptor(mut self, value: impl Into<String>) -> Self {
        self.params.insert("statement_descriptor".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// An optional token used to create the source. When passed, token properties will override source parameters.
    #[must_use]
    pub fn token(mut self, value: impl Into<String>) -> Self {
        self.params.insert("token".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The `type` of the source to create. Required unless `customer` and `original_source` are specified (see the \[Cloning card Sources\](https://docs.stripe.com/sources/connect#cloning-card-sources) guide)
    #[must_use]
    pub fn type_(mut self, value: impl Into<String>) -> Self {
        self.params.insert("type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set the `usage` parameter.
    #[must_use]
    pub fn usage(mut self, value: impl Into<String>) -> Self {
        self.params.insert("usage".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Source, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/sources/{source}`
///
/// Retrieves an existing source object. Supply the unique source ID from a source creation request and Stripe will return the corresponding up-to-date source object information.
#[derive(Debug, Clone)]
pub struct GetSourcesSource {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetSourcesSource {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(source: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let source = source.into();
        Self { path: format!("/v1/sources/{source}"), params }
    }

    /// The client secret of the source. Required if a publishable key is used to retrieve the source.
    #[must_use]
    pub fn client_secret(mut self, value: impl Into<String>) -> Self {
        self.params.insert("client_secret".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Source, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/sources/{source}`
///
/// Updates the specified source by setting the values of the parameters passed. Any parameters not provided will be left unchanged. This request accepts the metadata and owner as arguments. It is also possible to update type specific information for selected payment methods. Please refer to our payment method guides for more detail.
#[derive(Debug, Clone)]
pub struct PostSourcesSource {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostSourcesSource {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(source: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let source = source.into();
        Self { path: format!("/v1/sources/{source}"), params }
    }

    /// Amount associated with the source.
    #[must_use]
    pub fn amount(mut self, value: i64) -> Self {
        self.params.insert("amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Information about a mandate possibility attached to a source object (generally for bank debits) as well as its acceptance status.
    #[must_use]
    pub fn mandate(mut self, value: serde_json::Value) -> Self {
        self.params.insert("mandate".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Information about the owner of the payment instrument that may be used or required by particular source types.
    #[must_use]
    pub fn owner(mut self, value: serde_json::Value) -> Self {
        self.params.insert("owner".to_owned(), value);
        self
    }

    /// Information about the items and shipping associated with the source. Required for transactional credit (for example Klarna) sources before you can charge it.
    #[must_use]
    pub fn source_order(mut self, value: serde_json::Value) -> Self {
        self.params.insert("source_order".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Source, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/sources/{source}/mandate_notifications/{mandate_notification}`
///
/// Retrieves a new Source MandateNotification.
#[derive(Debug, Clone)]
pub struct GetSourcesSourceMandateNotificationsMandateNotification {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetSourcesSourceMandateNotificationsMandateNotification {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(mandate_notification: impl Into<String>, source: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let mandate_notification = mandate_notification.into();
        let source = source.into();
        Self { path: format!("/v1/sources/{source}/mandate_notifications/{mandate_notification}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::SourceMandateNotification, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/sources/{source}/source_transactions`
///
/// List source transactions for a given source.
#[derive(Debug, Clone)]
pub struct GetSourcesSourceSourceTransactions {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetSourcesSourceSourceTransactions {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(source: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let source = source.into();
        Self { path: format!("/v1/sources/{source}/source_transactions"), params }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::SourceTransaction> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::SourceTransaction>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/sources/{source}/source_transactions/{source_transaction}`
///
/// Retrieve an existing source transaction object. Supply the unique source ID from a source creation request and the source transaction ID and Stripe will return the corresponding up-to-date source object information.
#[derive(Debug, Clone)]
pub struct GetSourcesSourceSourceTransactionsSourceTransaction {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetSourcesSourceSourceTransactionsSourceTransaction {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(source: impl Into<String>, source_transaction: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let source = source.into();
        let source_transaction = source_transaction.into();
        Self { path: format!("/v1/sources/{source}/source_transactions/{source_transaction}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::SourceTransaction, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/sources/{source}/verify`
///
/// Verify a given source.
#[derive(Debug, Clone)]
pub struct PostSourcesSourceVerify {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostSourcesSourceVerify {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(source: impl Into<String>, values: Vec<String>) -> Self {
        let mut params = serde_json::Map::new();
        let source = source.into();
        params.insert("values".to_owned(), serde_json::Value::Array(values.into_iter().map(serde_json::Value::String).collect()));
        Self { path: format!("/v1/sources/{source}/verify"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Source, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
