//! Generated operations for the `products` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(
    clippy::doc_markdown,
    clippy::too_many_arguments,
    clippy::new_without_default,
    rustdoc::bare_urls
)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/products`
///
/// Returns a list of your products. The products are returned sorted by creation date, with the most recently created products appearing first.
#[derive(Debug, Clone)]
pub struct GetProducts {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetProducts {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/products".to_owned(),
            params,
        }
    }

    /// Only return products that are active or inactive (e.g., pass `false` to list all inactive products).
    #[must_use]
    pub fn active(mut self, value: bool) -> Self {
        self.params
            .insert("active".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Only return products that were created during the given date interval.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Only return products with the given IDs. Cannot be used with \[starting_after\](https://api.stripe.com#list_products-starting_after) or \[ending_before\](https://api.stripe.com#list_products-ending_before).
    #[must_use]
    pub fn ids(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "ids".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Only return products that can be shipped (i.e., physical, not digital products).
    #[must_use]
    pub fn shippable(mut self, value: bool) -> Self {
        self.params
            .insert("shippable".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Only return products with the given url.
    #[must_use]
    pub fn url(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("url".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Product> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::Product>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/products`
///
/// Creates a new product object.
#[derive(Debug, Clone)]
pub struct PostProducts {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostProducts {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("name".to_owned(), serde_json::Value::String(name.into()));
        Self {
            path: "/v1/products".to_owned(),
            params,
        }
    }

    /// Whether the product is currently available for purchase. Defaults to `true`.
    #[must_use]
    pub fn active(mut self, value: bool) -> Self {
        self.params
            .insert("active".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Data used to generate a new \[Price\](https://docs.stripe.com/api/prices) object. This Price will be set as the default price for this product.
    #[must_use]
    pub fn default_price_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("default_price_data".to_owned(), value);
        self
    }

    /// The product's description, meant to be displayable to the customer. Use this field to optionally store a long form explanation of the product being sold for your own rendering purposes.
    #[must_use]
    pub fn description(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "description".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// An identifier will be randomly generated by Stripe. You can optionally override this ID, but the ID must be unique across all products in your Stripe account.
    #[must_use]
    pub fn id(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("id".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A list of up to 8 URLs of images for this product, meant to be displayable to the customer.
    #[must_use]
    pub fn images(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "images".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A list of up to 15 marketing features for this product. These are displayed in \[pricing tables\](https://docs.stripe.com/payments/checkout/pricing-table).
    #[must_use]
    pub fn marketing_features(mut self, value: serde_json::Value) -> Self {
        self.params.insert("marketing_features".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert(
            "metadata".to_owned(),
            serde_json::Value::Object(
                value
                    .into_iter()
                    .map(|(k, v)| (k, serde_json::Value::String(v)))
                    .collect(),
            ),
        );
        self
    }

    /// The dimensions of this product for shipping purposes.
    #[must_use]
    pub fn package_dimensions(mut self, value: serde_json::Value) -> Self {
        self.params.insert("package_dimensions".to_owned(), value);
        self
    }

    /// Whether this product is shipped (i.e., physical goods).
    #[must_use]
    pub fn shippable(mut self, value: bool) -> Self {
        self.params
            .insert("shippable".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// An arbitrary string to be displayed on your customer's credit card or bank statement. While most banks display this information consistently, some may display it incorrectly or not at all. This may be up to 22 characters. The statement des…
    #[must_use]
    pub fn statement_descriptor(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "statement_descriptor".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// A \[tax code\](https://docs.stripe.com/tax/tax-categories) ID.
    #[must_use]
    pub fn tax_code(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "tax_code".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// A label that represents units of this product. When set, this will be included in customers' receipts, invoices, Checkout, and the customer portal.
    #[must_use]
    pub fn unit_label(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "unit_label".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// A URL of a publicly-accessible webpage for this product.
    #[must_use]
    pub fn url(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("url".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Product, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/products/search`
///
/// Search for products you’ve previously created using Stripe’s Search Query Language. Don’t use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages. Search functionality is not available to merchants in India.
#[derive(Debug, Clone)]
pub struct GetProductsSearch {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetProductsSearch {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(query: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("query".to_owned(), serde_json::Value::String(query.into()));
        Self {
            path: "/v1/products/search".to_owned(),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
    #[must_use]
    pub fn page(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("page".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Product> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::Product>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/products/{id}`
///
/// Retrieves the details of an existing product. Supply the unique product ID from either a product creation request or the product list, and Stripe will return the corresponding product information.
#[derive(Debug, Clone)]
pub struct GetProductsId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetProductsId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let id = id.into();
        Self {
            path: format!("/v1/products/{id}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Product, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/products/{id}`
///
/// Updates the specific product by setting the values of the parameters passed. Any parameters not provided will be left unchanged.
#[derive(Debug, Clone)]
pub struct PostProductsId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostProductsId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let id = id.into();
        Self {
            path: format!("/v1/products/{id}"),
            params,
        }
    }

    /// Whether the product is available for purchase.
    #[must_use]
    pub fn active(mut self, value: bool) -> Self {
        self.params
            .insert("active".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// The ID of the \[Price\](https://docs.stripe.com/api/prices) object that is the default price for this product.
    #[must_use]
    pub fn default_price(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "default_price".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// The product's description, meant to be displayable to the customer. Use this field to optionally store a long form explanation of the product being sold for your own rendering purposes.
    #[must_use]
    pub fn description(mut self, value: serde_json::Value) -> Self {
        self.params.insert("description".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A list of up to 8 URLs of images for this product, meant to be displayable to the customer.
    #[must_use]
    pub fn images(mut self, value: serde_json::Value) -> Self {
        self.params.insert("images".to_owned(), value);
        self
    }

    /// A list of up to 15 marketing features for this product. These are displayed in \[pricing tables\](https://docs.stripe.com/payments/checkout/pricing-table).
    #[must_use]
    pub fn marketing_features(mut self, value: serde_json::Value) -> Self {
        self.params.insert("marketing_features".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// The product's name, meant to be displayable to the customer.
    #[must_use]
    pub fn name(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The dimensions of this product for shipping purposes.
    #[must_use]
    pub fn package_dimensions(mut self, value: serde_json::Value) -> Self {
        self.params.insert("package_dimensions".to_owned(), value);
        self
    }

    /// Whether this product is shipped (i.e., physical goods).
    #[must_use]
    pub fn shippable(mut self, value: bool) -> Self {
        self.params
            .insert("shippable".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// An arbitrary string to be displayed on your customer's credit card or bank statement. While most banks display this information consistently, some may display it incorrectly or not at all. This may be up to 22 characters. The statement des…
    #[must_use]
    pub fn statement_descriptor(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "statement_descriptor".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// A \[tax code\](https://docs.stripe.com/tax/tax-categories) ID.
    #[must_use]
    pub fn tax_code(mut self, value: serde_json::Value) -> Self {
        self.params.insert("tax_code".to_owned(), value);
        self
    }

    /// A label that represents units of this product. When set, this will be included in customers' receipts, invoices, Checkout, and the customer portal. May only be set if `type=service`.
    #[must_use]
    pub fn unit_label(mut self, value: serde_json::Value) -> Self {
        self.params.insert("unit_label".to_owned(), value);
        self
    }

    /// A URL of a publicly-accessible webpage for this product.
    #[must_use]
    pub fn url(mut self, value: serde_json::Value) -> Self {
        self.params.insert("url".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Product, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `DELETE /v1/products/{id}`
///
/// Delete a product. Deleting a product is only possible if it has no prices associated with it. Additionally, deleting a product with type=good is only possible if it has no SKUs associated with it.
#[derive(Debug, Clone)]
pub struct DeleteProductsId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteProductsId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let id = id.into();
        Self {
            path: format!("/v1/products/{id}"),
            params,
        }
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::DeletedProduct, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/products/{product}/features`
///
/// Retrieve a list of features for a product
#[derive(Debug, Clone)]
pub struct GetProductsProductFeatures {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetProductsProductFeatures {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(product: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let product = product.into();
        Self {
            path: format!("/v1/products/{product}/features"),
            params,
        }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::ProductFeature> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::ProductFeature>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/products/{product}/features`
///
/// Creates a product_feature, which represents a feature attachment to a product
#[derive(Debug, Clone)]
pub struct PostProductsProductFeatures {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostProductsProductFeatures {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(product: impl Into<String>, entitlement_feature: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        let product = product.into();
        params.insert(
            "entitlement_feature".to_owned(),
            serde_json::Value::String(entitlement_feature.into()),
        );
        Self {
            path: format!("/v1/products/{product}/features"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::ProductFeature, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/products/{product}/features/{id}`
///
/// Retrieves a product_feature, which represents a feature attachment to a product
#[derive(Debug, Clone)]
pub struct GetProductsProductFeaturesId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetProductsProductFeaturesId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>, product: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let id = id.into();
        let product = product.into();
        Self {
            path: format!("/v1/products/{product}/features/{id}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::ProductFeature, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `DELETE /v1/products/{product}/features/{id}`
///
/// Deletes the feature attachment to a product
#[derive(Debug, Clone)]
pub struct DeleteProductsProductFeaturesId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteProductsProductFeaturesId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(id: impl Into<String>, product: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let id = id.into();
        let product = product.into();
        Self {
            path: format!("/v1/products/{product}/features/{id}"),
            params,
        }
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<crate::models::DeletedProductFeature, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
