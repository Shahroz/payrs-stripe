//! Generated operations for the `webhook_endpoints` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(
    clippy::doc_markdown,
    clippy::too_many_arguments,
    clippy::new_without_default,
    rustdoc::bare_urls
)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/webhook_endpoints`
///
/// Returns a list of your webhook endpoints.
#[derive(Debug, Clone)]
pub struct GetWebhookEndpoints {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetWebhookEndpoints {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/webhook_endpoints".to_owned(),
            params,
        }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::WebhookEndpoint> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::WebhookEndpoint>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/webhook_endpoints`
///
/// A webhook endpoint must have a url and a list of enabled_events. You may optionally specify the Boolean connect parameter. If set to true, then a Connect webhook endpoint that notifies the specified url about events from all connected accounts is created; otherwise an account webhook endpoint that notifies the specified url only about events from your account is created. You can also create webhook endpoints in the webhooks settings section of the Dashboard.
#[derive(Debug, Clone)]
pub struct PostWebhookEndpoints {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostWebhookEndpoints {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(enabled_events: serde_json::Value, url: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("enabled_events".to_owned(), enabled_events);
        params.insert("url".to_owned(), serde_json::Value::String(url.into()));
        Self {
            path: "/v1/webhook_endpoints".to_owned(),
            params,
        }
    }

    /// Events sent to this endpoint will be generated with this Stripe Version instead of your account's default Stripe Version.
    #[must_use]
    pub fn api_version(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "api_version".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Whether this endpoint should receive events from connected accounts (`true`), or from your account (`false`). Defaults to `false`.
    #[must_use]
    pub fn connect(mut self, value: bool) -> Self {
        self.params
            .insert("connect".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// An optional description of what the webhook is used for.
    #[must_use]
    pub fn description(mut self, value: serde_json::Value) -> Self {
        self.params.insert("description".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::WebhookEndpoint, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/webhook_endpoints/{webhook_endpoint}`
///
/// Retrieves the webhook endpoint with the given ID.
#[derive(Debug, Clone)]
pub struct GetWebhookEndpointsWebhookEndpoint {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetWebhookEndpointsWebhookEndpoint {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(webhook_endpoint: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let webhook_endpoint = webhook_endpoint.into();
        Self {
            path: format!("/v1/webhook_endpoints/{webhook_endpoint}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::WebhookEndpoint, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/webhook_endpoints/{webhook_endpoint}`
///
/// Updates the webhook endpoint. You may edit the url, the list of enabled_events, and the status of your endpoint.
#[derive(Debug, Clone)]
pub struct PostWebhookEndpointsWebhookEndpoint {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostWebhookEndpointsWebhookEndpoint {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(webhook_endpoint: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let webhook_endpoint = webhook_endpoint.into();
        Self {
            path: format!("/v1/webhook_endpoints/{webhook_endpoint}"),
            params,
        }
    }

    /// An optional description of what the webhook is used for.
    #[must_use]
    pub fn description(mut self, value: serde_json::Value) -> Self {
        self.params.insert("description".to_owned(), value);
        self
    }

    /// Disable the webhook endpoint if set to true.
    #[must_use]
    pub fn disabled(mut self, value: bool) -> Self {
        self.params
            .insert("disabled".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// The list of events to enable for this endpoint. You may specify `\['*'\]` to enable all events, except those that require explicit selection.
    #[must_use]
    pub fn enabled_events(mut self, value: serde_json::Value) -> Self {
        self.params.insert("enabled_events".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// The URL of the webhook endpoint.
    #[must_use]
    pub fn url(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("url".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::WebhookEndpoint, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `DELETE /v1/webhook_endpoints/{webhook_endpoint}`
///
/// You can also delete webhook endpoints via the webhook endpoint management page of the Stripe dashboard.
#[derive(Debug, Clone)]
pub struct DeleteWebhookEndpointsWebhookEndpoint {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteWebhookEndpointsWebhookEndpoint {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(webhook_endpoint: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let webhook_endpoint = webhook_endpoint.into();
        Self {
            path: format!("/v1/webhook_endpoints/{webhook_endpoint}"),
            params,
        }
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<crate::models::DeletedWebhookEndpoint, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
