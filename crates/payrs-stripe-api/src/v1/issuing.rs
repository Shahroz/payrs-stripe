//! Generated operations for the `issuing` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(
    clippy::doc_markdown,
    clippy::too_many_arguments,
    clippy::new_without_default,
    rustdoc::bare_urls
)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/issuing/authorizations`
///
/// Returns a list of Issuing Authorization objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.
#[derive(Debug, Clone)]
pub struct GetIssuingAuthorizations {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIssuingAuthorizations {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/issuing/authorizations".to_owned(),
            params,
        }
    }

    /// Only return authorizations that belong to the given card.
    #[must_use]
    pub fn card(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("card".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Only return authorizations that belong to the given cardholder.
    #[must_use]
    pub fn cardholder(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "cardholder".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Only return authorizations that were created during the given date interval.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Only return authorizations with the given status. One of `pending`, `closed`, or `reversed`.
    #[must_use]
    pub fn status(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("status".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::IssuingAuthorization> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::IssuingAuthorization>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/issuing/authorizations/{authorization}`
///
/// Retrieves an Issuing Authorization object.
#[derive(Debug, Clone)]
pub struct GetIssuingAuthorizationsAuthorization {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIssuingAuthorizationsAuthorization {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(authorization: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let authorization = authorization.into();
        Self {
            path: format!("/v1/issuing/authorizations/{authorization}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingAuthorization, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/issuing/authorizations/{authorization}`
///
/// Updates the specified Issuing Authorization object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.
#[derive(Debug, Clone)]
pub struct PostIssuingAuthorizationsAuthorization {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostIssuingAuthorizationsAuthorization {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(authorization: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let authorization = authorization.into();
        Self {
            path: format!("/v1/issuing/authorizations/{authorization}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingAuthorization, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/issuing/authorizations/{authorization}/approve`
///
/// \[Deprecated\] Approves a pending Issuing Authorization object. This request should be made within the timeout window of the real-time authorization flow. This method is deprecated. Instead, respond directly to the webhook request to approve an authorization.
#[derive(Debug, Clone)]
pub struct PostIssuingAuthorizationsAuthorizationApprove {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostIssuingAuthorizationsAuthorizationApprove {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(authorization: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let authorization = authorization.into();
        Self {
            path: format!("/v1/issuing/authorizations/{authorization}/approve"),
            params,
        }
    }

    /// If the authorization's `pending_request.is_amount_controllable` property is `true`, you may provide this value to control how much to hold for the authorization. Must be positive (use \[`decline`\](https://docs.stripe.com/api/issuing/autho…
    #[must_use]
    pub fn amount(mut self, value: i64) -> Self {
        self.params
            .insert("amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingAuthorization, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/issuing/authorizations/{authorization}/decline`
///
/// \[Deprecated\] Declines a pending Issuing Authorization object. This request should be made within the timeout window of the real time authorization flow. This method is deprecated. Instead, respond directly to the webhook request to decline an authorization.
#[derive(Debug, Clone)]
pub struct PostIssuingAuthorizationsAuthorizationDecline {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostIssuingAuthorizationsAuthorizationDecline {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(authorization: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let authorization = authorization.into();
        Self {
            path: format!("/v1/issuing/authorizations/{authorization}/decline"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingAuthorization, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/issuing/cardholders`
///
/// Returns a list of Issuing Cardholder objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.
#[derive(Debug, Clone)]
pub struct GetIssuingCardholders {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIssuingCardholders {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/issuing/cardholders".to_owned(),
            params,
        }
    }

    /// Only return cardholders that were created during the given date interval.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// Only return cardholders that have the given email address.
    #[must_use]
    pub fn email(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("email".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Only return cardholders that have the given phone number.
    #[must_use]
    pub fn phone_number(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "phone_number".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Only return cardholders that have the given status. One of `active`, `inactive`, or `blocked`.
    #[must_use]
    pub fn status(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("status".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Only return cardholders that have the given type. One of `individual` or `company`.
    #[must_use]
    pub fn type_(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::IssuingCardholder> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::IssuingCardholder>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/issuing/cardholders`
///
/// Creates a new Issuing Cardholder object that can be issued cards.
#[derive(Debug, Clone)]
pub struct PostIssuingCardholders {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostIssuingCardholders {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(billing: serde_json::Value, name: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("billing".to_owned(), billing);
        params.insert("name".to_owned(), serde_json::Value::String(name.into()));
        Self {
            path: "/v1/issuing/cardholders".to_owned(),
            params,
        }
    }

    /// Additional information about a `company` cardholder.
    #[must_use]
    pub fn company(mut self, value: serde_json::Value) -> Self {
        self.params.insert("company".to_owned(), value);
        self
    }

    /// The cardholder's email address.
    #[must_use]
    pub fn email(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("email".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Additional information about an `individual` cardholder.
    #[must_use]
    pub fn individual(mut self, value: serde_json::Value) -> Self {
        self.params.insert("individual".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert(
            "metadata".to_owned(),
            serde_json::Value::Object(
                value
                    .into_iter()
                    .map(|(k, v)| (k, serde_json::Value::String(v)))
                    .collect(),
            ),
        );
        self
    }

    /// The cardholder's phone number. This will be transformed to \[E.164\](https://en.wikipedia.org/wiki/E.164) if it is not provided in that format already. This is required for all cardholders who will be creating EU cards. See the \[3D Secure…
    #[must_use]
    pub fn phone_number(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "phone_number".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// The cardholder’s preferred locales (languages), ordered by preference. Locales can be `da`, `de`, `en`, `es`, `fr`, `it`, `pl`, or `sv`. This changes the language of the \[3D Secure flow\](https://docs.stripe.com/issuing/3d-secure) and one…
    #[must_use]
    pub fn preferred_locales(mut self, value: serde_json::Value) -> Self {
        self.params.insert("preferred_locales".to_owned(), value);
        self
    }

    /// Rules that control spending across this cardholder's cards. Refer to our \[documentation\](https://docs.stripe.com/issuing/controls/spending-controls) for more details.
    #[must_use]
    pub fn spending_controls(mut self, value: serde_json::Value) -> Self {
        self.params.insert("spending_controls".to_owned(), value);
        self
    }

    /// Specifies whether to permit authorizations on this cardholder's cards. Defaults to `active`.
    #[must_use]
    pub fn status(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("status".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// One of `individual` or `company`. See \[Choose a cardholder type\](https://docs.stripe.com/issuing/other/choose-cardholder) for more details.
    #[must_use]
    pub fn type_(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingCardholder, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/issuing/cardholders/{cardholder}`
///
/// Retrieves an Issuing Cardholder object.
#[derive(Debug, Clone)]
pub struct GetIssuingCardholdersCardholder {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIssuingCardholdersCardholder {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(cardholder: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let cardholder = cardholder.into();
        Self {
            path: format!("/v1/issuing/cardholders/{cardholder}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingCardholder, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/issuing/cardholders/{cardholder}`
///
/// Updates the specified Issuing Cardholder object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.
#[derive(Debug, Clone)]
pub struct PostIssuingCardholdersCardholder {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostIssuingCardholdersCardholder {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(cardholder: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let cardholder = cardholder.into();
        Self {
            path: format!("/v1/issuing/cardholders/{cardholder}"),
            params,
        }
    }

    /// The cardholder's billing address.
    #[must_use]
    pub fn billing(mut self, value: serde_json::Value) -> Self {
        self.params.insert("billing".to_owned(), value);
        self
    }

    /// Additional information about a `company` cardholder.
    #[must_use]
    pub fn company(mut self, value: serde_json::Value) -> Self {
        self.params.insert("company".to_owned(), value);
        self
    }

    /// The cardholder's email address.
    #[must_use]
    pub fn email(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("email".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Additional information about an `individual` cardholder.
    #[must_use]
    pub fn individual(mut self, value: serde_json::Value) -> Self {
        self.params.insert("individual".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert(
            "metadata".to_owned(),
            serde_json::Value::Object(
                value
                    .into_iter()
                    .map(|(k, v)| (k, serde_json::Value::String(v)))
                    .collect(),
            ),
        );
        self
    }

    /// The cardholder's phone number. This is required for all cardholders who will be creating EU cards. See the \[3D Secure documentation\](https://docs.stripe.com/issuing/3d-secure) for more details.
    #[must_use]
    pub fn phone_number(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "phone_number".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// The cardholder’s preferred locales (languages), ordered by preference. Locales can be `da`, `de`, `en`, `es`, `fr`, `it`, `pl`, or `sv`. This changes the language of the \[3D Secure flow\](https://docs.stripe.com/issuing/3d-secure) and one…
    #[must_use]
    pub fn preferred_locales(mut self, value: serde_json::Value) -> Self {
        self.params.insert("preferred_locales".to_owned(), value);
        self
    }

    /// Rules that control spending across this cardholder's cards. Refer to our \[documentation\](https://docs.stripe.com/issuing/controls/spending-controls) for more details.
    #[must_use]
    pub fn spending_controls(mut self, value: serde_json::Value) -> Self {
        self.params.insert("spending_controls".to_owned(), value);
        self
    }

    /// Specifies whether to permit authorizations on this cardholder's cards.
    #[must_use]
    pub fn status(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("status".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingCardholder, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/issuing/cards`
///
/// Returns a list of Issuing Card objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.
#[derive(Debug, Clone)]
pub struct GetIssuingCards {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIssuingCards {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/issuing/cards".to_owned(),
            params,
        }
    }

    /// Only return cards belonging to the Cardholder with the provided ID.
    #[must_use]
    pub fn cardholder(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "cardholder".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Only return cards that were issued during the given date interval.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Only return cards that have the given expiration month.
    #[must_use]
    pub fn exp_month(mut self, value: i64) -> Self {
        self.params
            .insert("exp_month".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Only return cards that have the given expiration year.
    #[must_use]
    pub fn exp_year(mut self, value: i64) -> Self {
        self.params
            .insert("exp_year".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Only return cards that have the given last four digits.
    #[must_use]
    pub fn last4(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("last4".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Set the `personalization_design` parameter.
    #[must_use]
    pub fn personalization_design(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "personalization_design".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Only return cards that have the given status. One of `active`, `inactive`, or `canceled`.
    #[must_use]
    pub fn status(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("status".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Only return cards that have the given type. One of `virtual` or `physical`.
    #[must_use]
    pub fn type_(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::IssuingCard> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::IssuingCard>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/issuing/cards`
///
/// Creates an Issuing Card object.
#[derive(Debug, Clone)]
pub struct PostIssuingCards {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostIssuingCards {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(currency: impl Into<String>, type_: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert(
            "currency".to_owned(),
            serde_json::Value::String(currency.into()),
        );
        params.insert("type".to_owned(), serde_json::Value::String(type_.into()));
        Self {
            path: "/v1/issuing/cards".to_owned(),
            params,
        }
    }

    /// The \[Cardholder\](https://docs.stripe.com/api#issuing_cardholder_object) object with which the card will be associated.
    #[must_use]
    pub fn cardholder(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "cardholder".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// The desired expiration month (1-12) for this card if \[specifying a custom expiration date\](/issuing/cards/virtual/issue-cards?testing-method=with-code#exp-dates).
    #[must_use]
    pub fn exp_month(mut self, value: i64) -> Self {
        self.params
            .insert("exp_month".to_owned(), serde_json::Value::from(value));
        self
    }

    /// The desired 4-digit expiration year for this card if \[specifying a custom expiration date\](/issuing/cards/virtual/issue-cards?testing-method=with-code#exp-dates).
    #[must_use]
    pub fn exp_year(mut self, value: i64) -> Self {
        self.params
            .insert("exp_year".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// The new financial account ID the card will be associated with. This field allows a card to be reassigned to a different financial account.
    #[must_use]
    pub fn financial_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "financial_account".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Rules that control the lifecycle of this card, such as automatic cancellation. Refer to our \[documentation\](/issuing/controls/lifecycle-controls) for more details.
    #[must_use]
    pub fn lifecycle_controls(mut self, value: serde_json::Value) -> Self {
        self.params.insert("lifecycle_controls".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert(
            "metadata".to_owned(),
            serde_json::Value::Object(
                value
                    .into_iter()
                    .map(|(k, v)| (k, serde_json::Value::String(v)))
                    .collect(),
            ),
        );
        self
    }

    /// The personalization design object belonging to this card.
    #[must_use]
    pub fn personalization_design(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "personalization_design".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// The desired PIN for this card.
    #[must_use]
    pub fn pin(mut self, value: serde_json::Value) -> Self {
        self.params.insert("pin".to_owned(), value);
        self
    }

    /// The card this is meant to be a replacement for (if any).
    #[must_use]
    pub fn replacement_for(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "replacement_for".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// If `replacement_for` is specified, this should indicate why that card is being replaced.
    #[must_use]
    pub fn replacement_reason(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "replacement_reason".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// The second line to print on the card. Max length: 24 characters.
    #[must_use]
    pub fn second_line(mut self, value: serde_json::Value) -> Self {
        self.params.insert("second_line".to_owned(), value);
        self
    }

    /// The address where the card will be shipped.
    #[must_use]
    pub fn shipping(mut self, value: serde_json::Value) -> Self {
        self.params.insert("shipping".to_owned(), value);
        self
    }

    /// Rules that control spending for this card. Refer to our \[documentation\](https://docs.stripe.com/issuing/controls/spending-controls) for more details.
    #[must_use]
    pub fn spending_controls(mut self, value: serde_json::Value) -> Self {
        self.params.insert("spending_controls".to_owned(), value);
        self
    }

    /// Whether authorizations can be approved on this card. May be blocked from activating cards depending on past-due Cardholder requirements. Defaults to `inactive`.
    #[must_use]
    pub fn status(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("status".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingCard, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/issuing/cards/{card}`
///
/// Retrieves an Issuing Card object.
#[derive(Debug, Clone)]
pub struct GetIssuingCardsCard {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIssuingCardsCard {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(card: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let card = card.into();
        Self {
            path: format!("/v1/issuing/cards/{card}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingCard, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/issuing/cards/{card}`
///
/// Updates the specified Issuing Card object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.
#[derive(Debug, Clone)]
pub struct PostIssuingCardsCard {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostIssuingCardsCard {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(card: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let card = card.into();
        Self {
            path: format!("/v1/issuing/cards/{card}"),
            params,
        }
    }

    /// Reason why the `status` of this card is `canceled`.
    #[must_use]
    pub fn cancellation_reason(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "cancellation_reason".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Set the `personalization_design` parameter.
    #[must_use]
    pub fn personalization_design(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "personalization_design".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// The desired new PIN for this card.
    #[must_use]
    pub fn pin(mut self, value: serde_json::Value) -> Self {
        self.params.insert("pin".to_owned(), value);
        self
    }

    /// Updated shipping information for the card.
    #[must_use]
    pub fn shipping(mut self, value: serde_json::Value) -> Self {
        self.params.insert("shipping".to_owned(), value);
        self
    }

    /// Rules that control spending for this card. Refer to our \[documentation\](https://docs.stripe.com/issuing/controls/spending-controls) for more details.
    #[must_use]
    pub fn spending_controls(mut self, value: serde_json::Value) -> Self {
        self.params.insert("spending_controls".to_owned(), value);
        self
    }

    /// Dictates whether authorizations can be approved on this card. May be blocked from activating cards depending on past-due Cardholder requirements. Defaults to `inactive`. If this card is being canceled because it was lost or stolen, this in…
    #[must_use]
    pub fn status(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("status".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingCard, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/issuing/disputes`
///
/// Returns a list of Issuing Dispute objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.
#[derive(Debug, Clone)]
pub struct GetIssuingDisputes {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIssuingDisputes {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/issuing/disputes".to_owned(),
            params,
        }
    }

    /// Only return Issuing disputes that were created during the given date interval.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Select Issuing disputes with the given status.
    #[must_use]
    pub fn status(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("status".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Select the Issuing dispute for the given transaction.
    #[must_use]
    pub fn transaction(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "transaction".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::IssuingDispute> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::IssuingDispute>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/issuing/disputes`
///
/// Creates an Issuing Dispute object. Individual pieces of evidence within the evidence object are optional at this point. Stripe only validates that required evidence is present during submission. Refer to Dispute reasons and evidence for more details about evidence requirements.
#[derive(Debug, Clone)]
pub struct PostIssuingDisputes {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostIssuingDisputes {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/issuing/disputes".to_owned(),
            params,
        }
    }

    /// The dispute amount in the card's currency and in the \[smallest currency unit\](https://docs.stripe.com/currencies#zero-decimal). If not set, defaults to the full transaction amount.
    #[must_use]
    pub fn amount(mut self, value: i64) -> Self {
        self.params
            .insert("amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Evidence provided for the dispute.
    #[must_use]
    pub fn evidence(mut self, value: serde_json::Value) -> Self {
        self.params.insert("evidence".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert(
            "metadata".to_owned(),
            serde_json::Value::Object(
                value
                    .into_iter()
                    .map(|(k, v)| (k, serde_json::Value::String(v)))
                    .collect(),
            ),
        );
        self
    }

    /// The ID of the issuing transaction to create a dispute for. For transaction on Treasury FinancialAccounts, use `treasury.received_debit`.
    #[must_use]
    pub fn transaction(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "transaction".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Params for disputes related to Treasury FinancialAccounts
    #[must_use]
    pub fn treasury(mut self, value: serde_json::Value) -> Self {
        self.params.insert("treasury".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingDispute, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/issuing/disputes/{dispute}`
///
/// Retrieves an Issuing Dispute object.
#[derive(Debug, Clone)]
pub struct GetIssuingDisputesDispute {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIssuingDisputesDispute {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(dispute: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let dispute = dispute.into();
        Self {
            path: format!("/v1/issuing/disputes/{dispute}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingDispute, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/issuing/disputes/{dispute}`
///
/// Updates the specified Issuing Dispute object by setting the values of the parameters passed. Any parameters not provided will be left unchanged. Properties on the evidence object can be unset by passing in an empty string.
#[derive(Debug, Clone)]
pub struct PostIssuingDisputesDispute {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostIssuingDisputesDispute {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(dispute: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let dispute = dispute.into();
        Self {
            path: format!("/v1/issuing/disputes/{dispute}"),
            params,
        }
    }

    /// The dispute amount in the card's currency and in the \[smallest currency unit\](https://docs.stripe.com/currencies#zero-decimal).
    #[must_use]
    pub fn amount(mut self, value: i64) -> Self {
        self.params
            .insert("amount".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Evidence provided for the dispute.
    #[must_use]
    pub fn evidence(mut self, value: serde_json::Value) -> Self {
        self.params.insert("evidence".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingDispute, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/issuing/disputes/{dispute}/submit`
///
/// Submits an Issuing Dispute to the card network. Stripe validates that all evidence fields required for the dispute’s reason are present. For more details, see Dispute reasons and evidence.
#[derive(Debug, Clone)]
pub struct PostIssuingDisputesDisputeSubmit {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostIssuingDisputesDisputeSubmit {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(dispute: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let dispute = dispute.into();
        Self {
            path: format!("/v1/issuing/disputes/{dispute}/submit"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingDispute, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/issuing/personalization_designs`
///
/// Returns a list of personalization design objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.
#[derive(Debug, Clone)]
pub struct GetIssuingPersonalizationDesigns {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIssuingPersonalizationDesigns {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/issuing/personalization_designs".to_owned(),
            params,
        }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Only return personalization designs with the given lookup keys.
    #[must_use]
    pub fn lookup_keys(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "lookup_keys".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Only return personalization designs with the given preferences.
    #[must_use]
    pub fn preferences(mut self, value: serde_json::Value) -> Self {
        self.params.insert("preferences".to_owned(), value);
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Only return personalization designs with the given status.
    #[must_use]
    pub fn status(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("status".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::IssuingPersonalizationDesign> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::IssuingPersonalizationDesign>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/issuing/personalization_designs`
///
/// Creates a personalization design object.
#[derive(Debug, Clone)]
pub struct PostIssuingPersonalizationDesigns {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostIssuingPersonalizationDesigns {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(physical_bundle: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert(
            "physical_bundle".to_owned(),
            serde_json::Value::String(physical_bundle.into()),
        );
        Self {
            path: "/v1/issuing/personalization_designs".to_owned(),
            params,
        }
    }

    /// The file for the card logo, for use with physical bundles that support card logos. Must have a `purpose` value of `issuing_logo`.
    #[must_use]
    pub fn card_logo(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "card_logo".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Hash containing carrier text, for use with physical bundles that support carrier text.
    #[must_use]
    pub fn carrier_text(mut self, value: serde_json::Value) -> Self {
        self.params.insert("carrier_text".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A lookup key used to retrieve personalization designs dynamically from a static string. This may be up to 200 characters.
    #[must_use]
    pub fn lookup_key(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "lookup_key".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert(
            "metadata".to_owned(),
            serde_json::Value::Object(
                value
                    .into_iter()
                    .map(|(k, v)| (k, serde_json::Value::String(v)))
                    .collect(),
            ),
        );
        self
    }

    /// Friendly display name.
    #[must_use]
    pub fn name(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Information on whether this personalization design is used to create cards when one is not specified.
    #[must_use]
    pub fn preferences(mut self, value: serde_json::Value) -> Self {
        self.params.insert("preferences".to_owned(), value);
        self
    }

    /// If set to true, will atomically remove the lookup key from the existing personalization design, and assign it to this personalization design.
    #[must_use]
    pub fn transfer_lookup_key(mut self, value: bool) -> Self {
        self.params.insert(
            "transfer_lookup_key".to_owned(),
            serde_json::Value::Bool(value),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<crate::models::IssuingPersonalizationDesign, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/issuing/personalization_designs/{personalization_design}`
///
/// Retrieves a personalization design object.
#[derive(Debug, Clone)]
pub struct GetIssuingPersonalizationDesignsPersonalizationDesign {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIssuingPersonalizationDesignsPersonalizationDesign {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(personalization_design: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let personalization_design = personalization_design.into();
        Self {
            path: format!("/v1/issuing/personalization_designs/{personalization_design}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<crate::models::IssuingPersonalizationDesign, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/issuing/personalization_designs/{personalization_design}`
///
/// Updates a card personalization object.
#[derive(Debug, Clone)]
pub struct PostIssuingPersonalizationDesignsPersonalizationDesign {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostIssuingPersonalizationDesignsPersonalizationDesign {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(personalization_design: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let personalization_design = personalization_design.into();
        Self {
            path: format!("/v1/issuing/personalization_designs/{personalization_design}"),
            params,
        }
    }

    /// The file for the card logo, for use with physical bundles that support card logos. Must have a `purpose` value of `issuing_logo`.
    #[must_use]
    pub fn card_logo(mut self, value: serde_json::Value) -> Self {
        self.params.insert("card_logo".to_owned(), value);
        self
    }

    /// Hash containing carrier text, for use with physical bundles that support carrier text.
    #[must_use]
    pub fn carrier_text(mut self, value: serde_json::Value) -> Self {
        self.params.insert("carrier_text".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A lookup key used to retrieve personalization designs dynamically from a static string. This may be up to 200 characters.
    #[must_use]
    pub fn lookup_key(mut self, value: serde_json::Value) -> Self {
        self.params.insert("lookup_key".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert(
            "metadata".to_owned(),
            serde_json::Value::Object(
                value
                    .into_iter()
                    .map(|(k, v)| (k, serde_json::Value::String(v)))
                    .collect(),
            ),
        );
        self
    }

    /// Friendly display name. Providing an empty string will set the field to null.
    #[must_use]
    pub fn name(mut self, value: serde_json::Value) -> Self {
        self.params.insert("name".to_owned(), value);
        self
    }

    /// The physical bundle object belonging to this personalization design.
    #[must_use]
    pub fn physical_bundle(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "physical_bundle".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Information on whether this personalization design is used to create cards when one is not specified.
    #[must_use]
    pub fn preferences(mut self, value: serde_json::Value) -> Self {
        self.params.insert("preferences".to_owned(), value);
        self
    }

    /// If set to true, will atomically remove the lookup key from the existing personalization design, and assign it to this personalization design.
    #[must_use]
    pub fn transfer_lookup_key(mut self, value: bool) -> Self {
        self.params.insert(
            "transfer_lookup_key".to_owned(),
            serde_json::Value::Bool(value),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<crate::models::IssuingPersonalizationDesign, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/issuing/physical_bundles`
///
/// Returns a list of physical bundle objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.
#[derive(Debug, Clone)]
pub struct GetIssuingPhysicalBundles {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIssuingPhysicalBundles {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/issuing/physical_bundles".to_owned(),
            params,
        }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Only return physical bundles with the given status.
    #[must_use]
    pub fn status(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("status".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Only return physical bundles with the given type.
    #[must_use]
    pub fn type_(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::IssuingPhysicalBundle> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::IssuingPhysicalBundle>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/issuing/physical_bundles/{physical_bundle}`
///
/// Retrieves a physical bundle object.
#[derive(Debug, Clone)]
pub struct GetIssuingPhysicalBundlesPhysicalBundle {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIssuingPhysicalBundlesPhysicalBundle {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(physical_bundle: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let physical_bundle = physical_bundle.into();
        Self {
            path: format!("/v1/issuing/physical_bundles/{physical_bundle}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<crate::models::IssuingPhysicalBundle, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/issuing/settlements/{settlement}`
///
/// Retrieves an Issuing Settlement object.
#[derive(Debug, Clone)]
pub struct GetIssuingSettlementsSettlement {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIssuingSettlementsSettlement {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(settlement: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let settlement = settlement.into();
        Self {
            path: format!("/v1/issuing/settlements/{settlement}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingSettlement, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/issuing/settlements/{settlement}`
///
/// Updates the specified Issuing Settlement object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.
#[derive(Debug, Clone)]
pub struct PostIssuingSettlementsSettlement {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostIssuingSettlementsSettlement {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(settlement: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let settlement = settlement.into();
        Self {
            path: format!("/v1/issuing/settlements/{settlement}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert(
            "metadata".to_owned(),
            serde_json::Value::Object(
                value
                    .into_iter()
                    .map(|(k, v)| (k, serde_json::Value::String(v)))
                    .collect(),
            ),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingSettlement, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/issuing/tokens`
///
/// Lists all Issuing Token objects for a given card.
#[derive(Debug, Clone)]
pub struct GetIssuingTokens {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIssuingTokens {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(card: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("card".to_owned(), serde_json::Value::String(card.into()));
        Self {
            path: "/v1/issuing/tokens".to_owned(),
            params,
        }
    }

    /// Only return Issuing tokens that were created during the given date interval.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Select Issuing tokens with the given status.
    #[must_use]
    pub fn status(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("status".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::IssuingToken> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::IssuingToken>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/issuing/tokens/{token}`
///
/// Retrieves an Issuing Token object.
#[derive(Debug, Clone)]
pub struct GetIssuingTokensToken {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIssuingTokensToken {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(token: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let token = token.into();
        Self {
            path: format!("/v1/issuing/tokens/{token}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingToken, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/issuing/tokens/{token}`
///
/// Attempts to update the specified Issuing Token object to the status specified.
#[derive(Debug, Clone)]
pub struct PostIssuingTokensToken {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostIssuingTokensToken {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(token: impl Into<String>, status: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        let token = token.into();
        params.insert(
            "status".to_owned(),
            serde_json::Value::String(status.into()),
        );
        Self {
            path: format!("/v1/issuing/tokens/{token}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingToken, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/issuing/transactions`
///
/// Returns a list of Issuing Transaction objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.
#[derive(Debug, Clone)]
pub struct GetIssuingTransactions {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIssuingTransactions {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self {
            path: "/v1/issuing/transactions".to_owned(),
            params,
        }
    }

    /// Only return transactions that belong to the given card.
    #[must_use]
    pub fn card(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("card".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Only return transactions that belong to the given cardholder.
    #[must_use]
    pub fn cardholder(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "cardholder".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Only return transactions that were created during the given date interval.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "ending_before".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params
            .insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "starting_after".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Only return transactions that have the given type. One of `capture` or `refund`.
    #[must_use]
    pub fn type_(mut self, value: impl Into<String>) -> Self {
        self.params
            .insert("type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::IssuingTransaction> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(
        self,
        client: &Client,
    ) -> Result<payrs_stripe_types::List<crate::models::IssuingTransaction>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/issuing/transactions/{transaction}`
///
/// Retrieves an Issuing Transaction object.
#[derive(Debug, Clone)]
pub struct GetIssuingTransactionsTransaction {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetIssuingTransactionsTransaction {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(transaction: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let transaction = transaction.into();
        Self {
            path: format!("/v1/issuing/transactions/{transaction}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingTransaction, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/issuing/transactions/{transaction}`
///
/// Updates the specified Issuing Transaction object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.
#[derive(Debug, Clone)]
pub struct PostIssuingTransactionsTransaction {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostIssuingTransactionsTransaction {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(transaction: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let transaction = transaction.into();
        Self {
            path: format!("/v1/issuing/transactions/{transaction}"),
            params,
        }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::IssuingTransaction, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
