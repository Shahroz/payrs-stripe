//! Generated operations for the `customers` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(clippy::doc_markdown, clippy::too_many_arguments, clippy::new_without_default, rustdoc::bare_urls)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `GET /v1/customers`
///
/// Returns a list of your customers. The customers are returned sorted by creation date, with the most recent customers appearing first.
#[derive(Debug, Clone)]
pub struct GetCustomers {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomers {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/customers".to_owned(), params }
    }

    /// Only return customers that were created during the given date interval.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// A case-sensitive filter on the list based on the customer's `email` field. The value must be a string.
    #[must_use]
    pub fn email(mut self, value: impl Into<String>) -> Self {
        self.params.insert("email".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Provides a list of customers that are associated with the specified test clock. The response will not include customers with test clocks if this parameter is not set.
    #[must_use]
    pub fn test_clock(mut self, value: impl Into<String>) -> Self {
        self.params.insert("test_clock".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Customer> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::Customer>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/customers`
///
/// Creates a new customer object.
#[derive(Debug, Clone)]
pub struct PostCustomers {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCustomers {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new() -> Self {
        let params = serde_json::Map::new();
        Self { path: "/v1/customers".to_owned(), params }
    }

    /// The customer's address. Learn about \[country-specific requirements for calculating tax\](https://docs.stripe.com/invoicing/taxes?dashboard-or-api=dashboard#set-up-customer).
    #[must_use]
    pub fn address(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address".to_owned(), value);
        self
    }

    /// An integer amount in cents (or local equivalent) that represents the customer's current balance, which affect the customer's future invoices. A negative amount represents a credit that decreases the amount due on an invoice; a positive amo…
    #[must_use]
    pub fn balance(mut self, value: i64) -> Self {
        self.params.insert("balance".to_owned(), serde_json::Value::from(value));
        self
    }

    /// The customer's business name. This may be up to *150 characters*.
    #[must_use]
    pub fn business_name(mut self, value: serde_json::Value) -> Self {
        self.params.insert("business_name".to_owned(), value);
        self
    }

    /// Balance information and default balance settings for this customer.
    #[must_use]
    pub fn cash_balance(mut self, value: serde_json::Value) -> Self {
        self.params.insert("cash_balance".to_owned(), value);
        self
    }

    /// An arbitrary string that you can attach to a customer object. It is displayed alongside the customer in the dashboard.
    #[must_use]
    pub fn description(mut self, value: impl Into<String>) -> Self {
        self.params.insert("description".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Customer's email address. It's displayed alongside the customer in your dashboard and can be useful for searching and tracking. This may be up to *512 characters*.
    #[must_use]
    pub fn email(mut self, value: impl Into<String>) -> Self {
        self.params.insert("email".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// The customer's full name. This may be up to *150 characters*.
    #[must_use]
    pub fn individual_name(mut self, value: serde_json::Value) -> Self {
        self.params.insert("individual_name".to_owned(), value);
        self
    }

    /// The prefix for the customer used to generate unique invoice numbers. Must be 3–12 uppercase letters or numbers.
    #[must_use]
    pub fn invoice_prefix(mut self, value: impl Into<String>) -> Self {
        self.params.insert("invoice_prefix".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Default invoice settings for this customer.
    #[must_use]
    pub fn invoice_settings(mut self, value: serde_json::Value) -> Self {
        self.params.insert("invoice_settings".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// The customer's full name or business name.
    #[must_use]
    pub fn name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The sequence to be used on the customer's next invoice. Defaults to 1.
    #[must_use]
    pub fn next_invoice_sequence(mut self, value: i64) -> Self {
        self.params.insert("next_invoice_sequence".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Set the `payment_method` parameter.
    #[must_use]
    pub fn payment_method(mut self, value: impl Into<String>) -> Self {
        self.params.insert("payment_method".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The customer's phone number.
    #[must_use]
    pub fn phone(mut self, value: impl Into<String>) -> Self {
        self.params.insert("phone".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Customer's preferred languages, ordered by preference.
    #[must_use]
    pub fn preferred_locales(mut self, value: Vec<String>) -> Self {
        self.params.insert("preferred_locales".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// The customer's shipping information. Appears on invoices emailed to this customer.
    #[must_use]
    pub fn shipping(mut self, value: serde_json::Value) -> Self {
        self.params.insert("shipping".to_owned(), value);
        self
    }

    /// Set the `source` parameter.
    #[must_use]
    pub fn source(mut self, value: impl Into<String>) -> Self {
        self.params.insert("source".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Tax details about the customer.
    #[must_use]
    pub fn tax(mut self, value: serde_json::Value) -> Self {
        self.params.insert("tax".to_owned(), value);
        self
    }

    /// The customer's tax exemption. One of `none`, `exempt`, or `reverse`.
    #[must_use]
    pub fn tax_exempt(mut self, value: impl Into<String>) -> Self {
        self.params.insert("tax_exempt".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The customer's tax IDs.
    #[must_use]
    pub fn tax_id_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("tax_id_data".to_owned(), value);
        self
    }

    /// ID of the test clock to attach to the customer.
    #[must_use]
    pub fn test_clock(mut self, value: impl Into<String>) -> Self {
        self.params.insert("test_clock".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Customer, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/customers/search`
///
/// Search for customers you’ve previously created using Stripe’s Search Query Language. Don’t use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages. Search functionality is not available to merchants in India.
#[derive(Debug, Clone)]
pub struct GetCustomersSearch {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersSearch {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(query: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert("query".to_owned(), serde_json::Value::String(query.into()));
        Self { path: "/v1/customers/search".to_owned(), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
    #[must_use]
    pub fn page(mut self, value: impl Into<String>) -> Self {
        self.params.insert("page".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Customer> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::Customer>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}`
///
/// Retrieves a Customer object.
#[derive(Debug, Clone)]
pub struct GetCustomersCustomer {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomer {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        Self { path: format!("/v1/customers/{customer}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/customers/{customer}`
///
/// Updates the specified customer by setting the values of the parameters passed. Any parameters not provided are left unchanged. For example, if you pass the source parameter, that becomes the customer’s active source (such as a card) to be used for all charges in the future. When you update a customer to a new valid card source by passing the source parameter: for each of the customer’s current subscriptions, if the subscription bills automatically and is in the past_due state, then the latest o…
#[derive(Debug, Clone)]
pub struct PostCustomersCustomer {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCustomersCustomer {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        Self { path: format!("/v1/customers/{customer}"), params }
    }

    /// The customer's address. Learn about \[country-specific requirements for calculating tax\](https://docs.stripe.com/invoicing/taxes?dashboard-or-api=dashboard#set-up-customer).
    #[must_use]
    pub fn address(mut self, value: serde_json::Value) -> Self {
        self.params.insert("address".to_owned(), value);
        self
    }

    /// An integer amount in cents (or local equivalent) that represents the customer's current balance, which affect the customer's future invoices. A negative amount represents a credit that decreases the amount due on an invoice; a positive amo…
    #[must_use]
    pub fn balance(mut self, value: i64) -> Self {
        self.params.insert("balance".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Either a token, like the ones returned by \[Stripe.js\](https://stripe.com/docs/js), or a dictionary containing a user's bank account details.
    #[must_use]
    pub fn bank_account(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bank_account".to_owned(), value);
        self
    }

    /// The customer's business name. This may be up to *150 characters*.
    #[must_use]
    pub fn business_name(mut self, value: serde_json::Value) -> Self {
        self.params.insert("business_name".to_owned(), value);
        self
    }

    /// A token, like the ones returned by \[Stripe.js\](https://stripe.com/docs/js).
    #[must_use]
    pub fn card(mut self, value: serde_json::Value) -> Self {
        self.params.insert("card".to_owned(), value);
        self
    }

    /// Balance information and default balance settings for this customer.
    #[must_use]
    pub fn cash_balance(mut self, value: serde_json::Value) -> Self {
        self.params.insert("cash_balance".to_owned(), value);
        self
    }

    /// ID of Alipay account to make the customer's new default for invoice payments.
    #[must_use]
    pub fn default_alipay_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("default_alipay_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// ID of bank account to make the customer's new default for invoice payments.
    #[must_use]
    pub fn default_bank_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("default_bank_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// ID of card to make the customer's new default for invoice payments.
    #[must_use]
    pub fn default_card(mut self, value: impl Into<String>) -> Self {
        self.params.insert("default_card".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// If you are using payment methods created via the PaymentMethods API, see the \[invoice_settings.default_payment_method\](https://docs.stripe.com/api/customers/update#update_customer-invoice_settings-default_payment_method) parameter. Provi…
    #[must_use]
    pub fn default_source(mut self, value: impl Into<String>) -> Self {
        self.params.insert("default_source".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// An arbitrary string that you can attach to a customer object. It is displayed alongside the customer in the dashboard.
    #[must_use]
    pub fn description(mut self, value: impl Into<String>) -> Self {
        self.params.insert("description".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Customer's email address. It's displayed alongside the customer in your dashboard and can be useful for searching and tracking. This may be up to *512 characters*.
    #[must_use]
    pub fn email(mut self, value: impl Into<String>) -> Self {
        self.params.insert("email".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// The customer's full name. This may be up to *150 characters*.
    #[must_use]
    pub fn individual_name(mut self, value: serde_json::Value) -> Self {
        self.params.insert("individual_name".to_owned(), value);
        self
    }

    /// The prefix for the customer used to generate unique invoice numbers. Must be 3–12 uppercase letters or numbers.
    #[must_use]
    pub fn invoice_prefix(mut self, value: impl Into<String>) -> Self {
        self.params.insert("invoice_prefix".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Default invoice settings for this customer.
    #[must_use]
    pub fn invoice_settings(mut self, value: serde_json::Value) -> Self {
        self.params.insert("invoice_settings".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// The customer's full name or business name.
    #[must_use]
    pub fn name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The sequence to be used on the customer's next invoice. Defaults to 1.
    #[must_use]
    pub fn next_invoice_sequence(mut self, value: i64) -> Self {
        self.params.insert("next_invoice_sequence".to_owned(), serde_json::Value::from(value));
        self
    }

    /// The customer's phone number.
    #[must_use]
    pub fn phone(mut self, value: impl Into<String>) -> Self {
        self.params.insert("phone".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Customer's preferred languages, ordered by preference.
    #[must_use]
    pub fn preferred_locales(mut self, value: Vec<String>) -> Self {
        self.params.insert("preferred_locales".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// The customer's shipping information. Appears on invoices emailed to this customer.
    #[must_use]
    pub fn shipping(mut self, value: serde_json::Value) -> Self {
        self.params.insert("shipping".to_owned(), value);
        self
    }

    /// Set the `source` parameter.
    #[must_use]
    pub fn source(mut self, value: impl Into<String>) -> Self {
        self.params.insert("source".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Tax details about the customer.
    #[must_use]
    pub fn tax(mut self, value: serde_json::Value) -> Self {
        self.params.insert("tax".to_owned(), value);
        self
    }

    /// The customer's tax exemption. One of `none`, `exempt`, or `reverse`.
    #[must_use]
    pub fn tax_exempt(mut self, value: impl Into<String>) -> Self {
        self.params.insert("tax_exempt".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Customer, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `DELETE /v1/customers/{customer}`
///
/// Permanently deletes a customer. It cannot be undone. Also immediately cancels any active subscriptions on the customer.
#[derive(Debug, Clone)]
pub struct DeleteCustomersCustomer {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteCustomersCustomer {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        Self { path: format!("/v1/customers/{customer}"), params }
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::DeletedCustomer, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}/balance_transactions`
///
/// Returns a list of transactions that updated the customer’s balances.
#[derive(Debug, Clone)]
pub struct GetCustomersCustomerBalanceTransactions {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomerBalanceTransactions {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        Self { path: format!("/v1/customers/{customer}/balance_transactions"), params }
    }

    /// Only return customer balance transactions that were created during the given date interval.
    #[must_use]
    pub fn created(mut self, value: serde_json::Value) -> Self {
        self.params.insert("created".to_owned(), value);
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Only return transactions that are related to the specified invoice.
    #[must_use]
    pub fn invoice(mut self, value: impl Into<String>) -> Self {
        self.params.insert("invoice".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::CustomerBalanceTransaction> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::CustomerBalanceTransaction>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/customers/{customer}/balance_transactions`
///
/// Creates an immutable transaction that updates the customer’s credit balance.
#[derive(Debug, Clone)]
pub struct PostCustomersCustomerBalanceTransactions {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCustomersCustomerBalanceTransactions {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, amount: i64, currency: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        let customer = customer.into();
        params.insert("amount".to_owned(), serde_json::Value::from(amount));
        params.insert("currency".to_owned(), serde_json::Value::String(currency.into()));
        Self { path: format!("/v1/customers/{customer}/balance_transactions"), params }
    }

    /// An arbitrary string attached to the object. Often useful for displaying to users.
    #[must_use]
    pub fn description(mut self, value: impl Into<String>) -> Self {
        self.params.insert("description".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::CustomerBalanceTransaction, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}/balance_transactions/{transaction}`
///
/// Retrieves a specific customer balance transaction that updated the customer’s balances.
#[derive(Debug, Clone)]
pub struct GetCustomersCustomerBalanceTransactionsTransaction {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomerBalanceTransactionsTransaction {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, transaction: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let transaction = transaction.into();
        Self { path: format!("/v1/customers/{customer}/balance_transactions/{transaction}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::CustomerBalanceTransaction, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/customers/{customer}/balance_transactions/{transaction}`
///
/// Most credit balance transaction fields are immutable, but you may update its description and metadata.
#[derive(Debug, Clone)]
pub struct PostCustomersCustomerBalanceTransactionsTransaction {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCustomersCustomerBalanceTransactionsTransaction {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, transaction: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let transaction = transaction.into();
        Self { path: format!("/v1/customers/{customer}/balance_transactions/{transaction}"), params }
    }

    /// An arbitrary string attached to the object. Often useful for displaying to users.
    #[must_use]
    pub fn description(mut self, value: impl Into<String>) -> Self {
        self.params.insert("description".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::CustomerBalanceTransaction, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}/bank_accounts`
///
/// You can see a list of the bank accounts belonging to a Customer. Note that the 10 most recent sources are always available by default on the Customer. If you need more than those 10, you can use this API method and the limit and starting_after parameters to page through additional bank accounts.
#[derive(Debug, Clone)]
pub struct GetCustomersCustomerBankAccounts {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomerBankAccounts {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        Self { path: format!("/v1/customers/{customer}/bank_accounts"), params }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::BankAccount> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::BankAccount>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/customers/{customer}/bank_accounts`
///
/// When you create a new credit card, you must specify a customer or recipient on which to create it. If the card’s owner has no default card, then the new card will become the default. However, if the owner already has a default, then it will not change. To change the default, you should update the customer to have a new default_source.
#[derive(Debug, Clone)]
pub struct PostCustomersCustomerBankAccounts {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCustomersCustomerBankAccounts {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        Self { path: format!("/v1/customers/{customer}/bank_accounts"), params }
    }

    /// A token returned by \[Stripe.js\](https://stripe.com/docs/js) representing the user’s Alipay account details.
    #[must_use]
    pub fn alipay_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("alipay_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Either a token, like the ones returned by \[Stripe.js\](https://stripe.com/docs/js), or a dictionary containing a user's bank account details.
    #[must_use]
    pub fn bank_account(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bank_account".to_owned(), value);
        self
    }

    /// A token, like the ones returned by \[Stripe.js\](https://stripe.com/docs/js).
    #[must_use]
    pub fn card(mut self, value: serde_json::Value) -> Self {
        self.params.insert("card".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert("metadata".to_owned(), serde_json::Value::Object(value.into_iter().map(|(k, v)| (k, serde_json::Value::String(v))).collect()));
        self
    }

    /// Please refer to full \[documentation\](https://api.stripe.com) instead.
    #[must_use]
    pub fn source(mut self, value: impl Into<String>) -> Self {
        self.params.insert("source".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}/bank_accounts/{id}`
///
/// By default, you can see the 10 most recent sources stored on a Customer directly on the object, but you can also retrieve details about a specific bank account stored on the Stripe account.
#[derive(Debug, Clone)]
pub struct GetCustomersCustomerBankAccountsId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomerBankAccountsId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let id = id.into();
        Self { path: format!("/v1/customers/{customer}/bank_accounts/{id}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::BankAccount, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/customers/{customer}/bank_accounts/{id}`
///
/// Update a specified source for a given customer.
#[derive(Debug, Clone)]
pub struct PostCustomersCustomerBankAccountsId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCustomersCustomerBankAccountsId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let id = id.into();
        Self { path: format!("/v1/customers/{customer}/bank_accounts/{id}"), params }
    }

    /// The name of the person or business that owns the bank account.
    #[must_use]
    pub fn account_holder_name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("account_holder_name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The type of entity that holds the account. This can be either `individual` or `company`.
    #[must_use]
    pub fn account_holder_type(mut self, value: impl Into<String>) -> Self {
        self.params.insert("account_holder_type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// City/District/Suburb/Town/Village.
    #[must_use]
    pub fn address_city(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_city".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Billing address country, if provided when creating card.
    #[must_use]
    pub fn address_country(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_country".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Address line 1 (Street address/PO Box/Company name).
    #[must_use]
    pub fn address_line1(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_line1".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Address line 2 (Apartment/Suite/Unit/Building).
    #[must_use]
    pub fn address_line2(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_line2".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// State/County/Province/Region.
    #[must_use]
    pub fn address_state(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_state".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// ZIP or postal code.
    #[must_use]
    pub fn address_zip(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_zip".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Two digit number representing the card’s expiration month.
    #[must_use]
    pub fn exp_month(mut self, value: impl Into<String>) -> Self {
        self.params.insert("exp_month".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Four digit number representing the card’s expiration year.
    #[must_use]
    pub fn exp_year(mut self, value: impl Into<String>) -> Self {
        self.params.insert("exp_year".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Cardholder name.
    #[must_use]
    pub fn name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set the `owner` parameter.
    #[must_use]
    pub fn owner(mut self, value: serde_json::Value) -> Self {
        self.params.insert("owner".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `DELETE /v1/customers/{customer}/bank_accounts/{id}`
///
/// Delete a specified source for a given customer.
#[derive(Debug, Clone)]
pub struct DeleteCustomersCustomerBankAccountsId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteCustomersCustomerBankAccountsId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let id = id.into();
        Self { path: format!("/v1/customers/{customer}/bank_accounts/{id}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/customers/{customer}/bank_accounts/{id}/verify`
///
/// Verify a specified bank account for a given customer.
#[derive(Debug, Clone)]
pub struct PostCustomersCustomerBankAccountsIdVerify {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCustomersCustomerBankAccountsIdVerify {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let id = id.into();
        Self { path: format!("/v1/customers/{customer}/bank_accounts/{id}/verify"), params }
    }

    /// Two positive integers, in *cents*, equal to the values of the microdeposits sent to the bank account.
    #[must_use]
    pub fn amounts(mut self, value: serde_json::Value) -> Self {
        self.params.insert("amounts".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::BankAccount, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}/cards`
///
/// You can see a list of the cards belonging to a customer. Note that the 10 most recent sources are always available on the Customer object. If you need more than those 10, you can use this API method and the limit and starting_after parameters to page through additional cards.
#[derive(Debug, Clone)]
pub struct GetCustomersCustomerCards {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomerCards {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        Self { path: format!("/v1/customers/{customer}/cards"), params }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Card> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::Card>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/customers/{customer}/cards`
///
/// When you create a new credit card, you must specify a customer or recipient on which to create it. If the card’s owner has no default card, then the new card will become the default. However, if the owner already has a default, then it will not change. To change the default, you should update the customer to have a new default_source.
#[derive(Debug, Clone)]
pub struct PostCustomersCustomerCards {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCustomersCustomerCards {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        Self { path: format!("/v1/customers/{customer}/cards"), params }
    }

    /// A token returned by \[Stripe.js\](https://stripe.com/docs/js) representing the user’s Alipay account details.
    #[must_use]
    pub fn alipay_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("alipay_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Either a token, like the ones returned by \[Stripe.js\](https://stripe.com/docs/js), or a dictionary containing a user's bank account details.
    #[must_use]
    pub fn bank_account(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bank_account".to_owned(), value);
        self
    }

    /// A token, like the ones returned by \[Stripe.js\](https://stripe.com/docs/js).
    #[must_use]
    pub fn card(mut self, value: serde_json::Value) -> Self {
        self.params.insert("card".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert("metadata".to_owned(), serde_json::Value::Object(value.into_iter().map(|(k, v)| (k, serde_json::Value::String(v))).collect()));
        self
    }

    /// Please refer to full \[documentation\](https://api.stripe.com) instead.
    #[must_use]
    pub fn source(mut self, value: impl Into<String>) -> Self {
        self.params.insert("source".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}/cards/{id}`
///
/// You can always see the 10 most recent cards directly on a customer; this method lets you retrieve details about a specific card stored on the customer.
#[derive(Debug, Clone)]
pub struct GetCustomersCustomerCardsId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomerCardsId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let id = id.into();
        Self { path: format!("/v1/customers/{customer}/cards/{id}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Card, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/customers/{customer}/cards/{id}`
///
/// Update a specified source for a given customer.
#[derive(Debug, Clone)]
pub struct PostCustomersCustomerCardsId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCustomersCustomerCardsId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let id = id.into();
        Self { path: format!("/v1/customers/{customer}/cards/{id}"), params }
    }

    /// The name of the person or business that owns the bank account.
    #[must_use]
    pub fn account_holder_name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("account_holder_name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The type of entity that holds the account. This can be either `individual` or `company`.
    #[must_use]
    pub fn account_holder_type(mut self, value: impl Into<String>) -> Self {
        self.params.insert("account_holder_type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// City/District/Suburb/Town/Village.
    #[must_use]
    pub fn address_city(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_city".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Billing address country, if provided when creating card.
    #[must_use]
    pub fn address_country(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_country".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Address line 1 (Street address/PO Box/Company name).
    #[must_use]
    pub fn address_line1(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_line1".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Address line 2 (Apartment/Suite/Unit/Building).
    #[must_use]
    pub fn address_line2(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_line2".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// State/County/Province/Region.
    #[must_use]
    pub fn address_state(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_state".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// ZIP or postal code.
    #[must_use]
    pub fn address_zip(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_zip".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Two digit number representing the card’s expiration month.
    #[must_use]
    pub fn exp_month(mut self, value: impl Into<String>) -> Self {
        self.params.insert("exp_month".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Four digit number representing the card’s expiration year.
    #[must_use]
    pub fn exp_year(mut self, value: impl Into<String>) -> Self {
        self.params.insert("exp_year".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Cardholder name.
    #[must_use]
    pub fn name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set the `owner` parameter.
    #[must_use]
    pub fn owner(mut self, value: serde_json::Value) -> Self {
        self.params.insert("owner".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `DELETE /v1/customers/{customer}/cards/{id}`
///
/// Delete a specified source for a given customer.
#[derive(Debug, Clone)]
pub struct DeleteCustomersCustomerCardsId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteCustomersCustomerCardsId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let id = id.into();
        Self { path: format!("/v1/customers/{customer}/cards/{id}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}/cash_balance`
///
/// Retrieves a customer’s cash balance.
#[derive(Debug, Clone)]
pub struct GetCustomersCustomerCashBalance {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomerCashBalance {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        Self { path: format!("/v1/customers/{customer}/cash_balance"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::CashBalance, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/customers/{customer}/cash_balance`
///
/// Changes the settings on a customer’s cash balance.
#[derive(Debug, Clone)]
pub struct PostCustomersCustomerCashBalance {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCustomersCustomerCashBalance {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        Self { path: format!("/v1/customers/{customer}/cash_balance"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A hash of settings for this cash balance.
    #[must_use]
    pub fn settings(mut self, value: serde_json::Value) -> Self {
        self.params.insert("settings".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::CashBalance, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}/cash_balance_transactions`
///
/// Returns a list of transactions that modified the customer’s cash balance.
#[derive(Debug, Clone)]
pub struct GetCustomersCustomerCashBalanceTransactions {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomerCashBalanceTransactions {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        Self { path: format!("/v1/customers/{customer}/cash_balance_transactions"), params }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::CustomerCashBalanceTransaction> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::CustomerCashBalanceTransaction>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}/cash_balance_transactions/{transaction}`
///
/// Retrieves a specific cash balance transaction, which updated the customer’s cash balance.
#[derive(Debug, Clone)]
pub struct GetCustomersCustomerCashBalanceTransactionsTransaction {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomerCashBalanceTransactionsTransaction {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, transaction: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let transaction = transaction.into();
        Self { path: format!("/v1/customers/{customer}/cash_balance_transactions/{transaction}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::CustomerCashBalanceTransaction, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}/discount`
#[derive(Debug, Clone)]
pub struct GetCustomersCustomerDiscount {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomerDiscount {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        Self { path: format!("/v1/customers/{customer}/discount"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Discount, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `DELETE /v1/customers/{customer}/discount`
///
/// Removes the currently applied discount on a customer.
#[derive(Debug, Clone)]
pub struct DeleteCustomersCustomerDiscount {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteCustomersCustomerDiscount {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        Self { path: format!("/v1/customers/{customer}/discount"), params }
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::DeletedDiscount, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/customers/{customer}/funding_instructions`
///
/// Retrieve funding instructions for a customer cash balance. If funding instructions do not yet exist for the customer, new funding instructions will be created. If funding instructions have already been created for a given customer, the same funding instructions will be retrieved. In other words, we will return the same funding instructions each time.
#[derive(Debug, Clone)]
pub struct PostCustomersCustomerFundingInstructions {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCustomersCustomerFundingInstructions {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, bank_transfer: serde_json::Value, currency: impl Into<String>, funding_type: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        let customer = customer.into();
        params.insert("bank_transfer".to_owned(), bank_transfer);
        params.insert("currency".to_owned(), serde_json::Value::String(currency.into()));
        params.insert("funding_type".to_owned(), serde_json::Value::String(funding_type.into()));
        Self { path: format!("/v1/customers/{customer}/funding_instructions"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::FundingInstructions, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}/payment_methods`
///
/// Returns a list of PaymentMethods for a given Customer
#[derive(Debug, Clone)]
pub struct GetCustomersCustomerPaymentMethods {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomerPaymentMethods {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        Self { path: format!("/v1/customers/{customer}/payment_methods"), params }
    }

    /// This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment meth…
    #[must_use]
    pub fn allow_redisplay(mut self, value: impl Into<String>) -> Self {
        self.params.insert("allow_redisplay".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// An optional filter on the list, based on the object `type` field. Without the filter, the list includes all current and future payment method types. If your integration expects only one type of payment method in the response, make sure to…
    #[must_use]
    pub fn type_(mut self, value: impl Into<String>) -> Self {
        self.params.insert("type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::PaymentMethod> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::PaymentMethod>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}/payment_methods/{payment_method}`
///
/// Retrieves a PaymentMethod object for a given Customer.
#[derive(Debug, Clone)]
pub struct GetCustomersCustomerPaymentMethodsPaymentMethod {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomerPaymentMethodsPaymentMethod {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, payment_method: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let payment_method = payment_method.into();
        Self { path: format!("/v1/customers/{customer}/payment_methods/{payment_method}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::PaymentMethod, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}/sources`
///
/// List sources for a specified customer.
#[derive(Debug, Clone)]
pub struct GetCustomersCustomerSources {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomerSources {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        Self { path: format!("/v1/customers/{customer}/sources"), params }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Filter sources according to a particular object type.
    #[must_use]
    pub fn object(mut self, value: impl Into<String>) -> Self {
        self.params.insert("object".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/customers/{customer}/sources`
///
/// When you create a new credit card, you must specify a customer or recipient on which to create it. If the card’s owner has no default card, then the new card will become the default. However, if the owner already has a default, then it will not change. To change the default, you should update the customer to have a new default_source.
#[derive(Debug, Clone)]
pub struct PostCustomersCustomerSources {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCustomersCustomerSources {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        Self { path: format!("/v1/customers/{customer}/sources"), params }
    }

    /// A token returned by \[Stripe.js\](https://stripe.com/docs/js) representing the user’s Alipay account details.
    #[must_use]
    pub fn alipay_account(mut self, value: impl Into<String>) -> Self {
        self.params.insert("alipay_account".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Either a token, like the ones returned by \[Stripe.js\](https://stripe.com/docs/js), or a dictionary containing a user's bank account details.
    #[must_use]
    pub fn bank_account(mut self, value: serde_json::Value) -> Self {
        self.params.insert("bank_account".to_owned(), value);
        self
    }

    /// A token, like the ones returned by \[Stripe.js\](https://stripe.com/docs/js).
    #[must_use]
    pub fn card(mut self, value: serde_json::Value) -> Self {
        self.params.insert("card".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: std::collections::BTreeMap<String, String>) -> Self {
        self.params.insert("metadata".to_owned(), serde_json::Value::Object(value.into_iter().map(|(k, v)| (k, serde_json::Value::String(v))).collect()));
        self
    }

    /// Please refer to full \[documentation\](https://api.stripe.com) instead.
    #[must_use]
    pub fn source(mut self, value: impl Into<String>) -> Self {
        self.params.insert("source".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}/sources/{id}`
///
/// Retrieve a specified source for a given customer.
#[derive(Debug, Clone)]
pub struct GetCustomersCustomerSourcesId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomerSourcesId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let id = id.into();
        Self { path: format!("/v1/customers/{customer}/sources/{id}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/customers/{customer}/sources/{id}`
///
/// Update a specified source for a given customer.
#[derive(Debug, Clone)]
pub struct PostCustomersCustomerSourcesId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCustomersCustomerSourcesId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let id = id.into();
        Self { path: format!("/v1/customers/{customer}/sources/{id}"), params }
    }

    /// The name of the person or business that owns the bank account.
    #[must_use]
    pub fn account_holder_name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("account_holder_name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The type of entity that holds the account. This can be either `individual` or `company`.
    #[must_use]
    pub fn account_holder_type(mut self, value: impl Into<String>) -> Self {
        self.params.insert("account_holder_type".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// City/District/Suburb/Town/Village.
    #[must_use]
    pub fn address_city(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_city".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Billing address country, if provided when creating card.
    #[must_use]
    pub fn address_country(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_country".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Address line 1 (Street address/PO Box/Company name).
    #[must_use]
    pub fn address_line1(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_line1".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Address line 2 (Apartment/Suite/Unit/Building).
    #[must_use]
    pub fn address_line2(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_line2".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// State/County/Province/Region.
    #[must_use]
    pub fn address_state(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_state".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// ZIP or postal code.
    #[must_use]
    pub fn address_zip(mut self, value: impl Into<String>) -> Self {
        self.params.insert("address_zip".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Two digit number representing the card’s expiration month.
    #[must_use]
    pub fn exp_month(mut self, value: impl Into<String>) -> Self {
        self.params.insert("exp_month".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Four digit number representing the card’s expiration year.
    #[must_use]
    pub fn exp_year(mut self, value: impl Into<String>) -> Self {
        self.params.insert("exp_year".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Cardholder name.
    #[must_use]
    pub fn name(mut self, value: impl Into<String>) -> Self {
        self.params.insert("name".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Set the `owner` parameter.
    #[must_use]
    pub fn owner(mut self, value: serde_json::Value) -> Self {
        self.params.insert("owner".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `DELETE /v1/customers/{customer}/sources/{id}`
///
/// Delete a specified source for a given customer.
#[derive(Debug, Clone)]
pub struct DeleteCustomersCustomerSourcesId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteCustomersCustomerSourcesId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let id = id.into();
        Self { path: format!("/v1/customers/{customer}/sources/{id}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<serde_json::Value, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `POST /v1/customers/{customer}/sources/{id}/verify`
///
/// Verify a specified bank account for a given customer.
#[derive(Debug, Clone)]
pub struct PostCustomersCustomerSourcesIdVerify {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCustomersCustomerSourcesIdVerify {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let id = id.into();
        Self { path: format!("/v1/customers/{customer}/sources/{id}/verify"), params }
    }

    /// Two positive integers, in *cents*, equal to the values of the microdeposits sent to the bank account.
    #[must_use]
    pub fn amounts(mut self, value: serde_json::Value) -> Self {
        self.params.insert("amounts".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::BankAccount, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}/subscriptions`
///
/// You can see a list of the customer’s active subscriptions. Note that the 10 most recent active subscriptions are always available by default on the customer object. If you need more than those 10, you can use the limit and starting_after parameters to page through additional subscriptions.
#[derive(Debug, Clone)]
pub struct GetCustomersCustomerSubscriptions {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomerSubscriptions {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        Self { path: format!("/v1/customers/{customer}/subscriptions"), params }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::Subscription> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::Subscription>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/customers/{customer}/subscriptions`
///
/// Creates a new subscription on an existing customer.
#[derive(Debug, Clone)]
pub struct PostCustomersCustomerSubscriptions {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCustomersCustomerSubscriptions {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        Self { path: format!("/v1/customers/{customer}/subscriptions"), params }
    }

    /// A list of prices and quantities that will generate invoice items appended to the next invoice for this subscription. You may pass up to 20 items.
    #[must_use]
    pub fn add_invoice_items(mut self, value: serde_json::Value) -> Self {
        self.params.insert("add_invoice_items".to_owned(), value);
        self
    }

    /// A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the application owner's Stripe account. The request must be made by a p…
    #[must_use]
    pub fn application_fee_percent(mut self, value: serde_json::Value) -> Self {
        self.params.insert("application_fee_percent".to_owned(), value);
        self
    }

    /// Automatic tax settings for this subscription.
    #[must_use]
    pub fn automatic_tax(mut self, value: serde_json::Value) -> Self {
        self.params.insert("automatic_tax".to_owned(), value);
        self
    }

    /// A past timestamp to backdate the subscription's start date to. If set, the first invoice will contain line items for the timespan between the start date and the current time. Can be combined with trials and the billing cycle anchor.
    #[must_use]
    pub fn backdate_start_date(mut self, value: i64) -> Self {
        self.params.insert("backdate_start_date".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A future timestamp in UTC format to anchor the subscription's \[billing cycle\](https://docs.stripe.com/subscriptions/billing-cycle). The anchor is the reference point that aligns future billing cycle dates. It sets the day of week for `we…
    #[must_use]
    pub fn billing_cycle_anchor(mut self, value: i64) -> Self {
        self.params.insert("billing_cycle_anchor".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Define thresholds at which an invoice will be sent, and the subscription advanced to a new billing period. When updating, pass an empty string to remove previously-defined thresholds.
    #[must_use]
    pub fn billing_thresholds(mut self, value: serde_json::Value) -> Self {
        self.params.insert("billing_thresholds".to_owned(), value);
        self
    }

    /// A timestamp at which the subscription should cancel. If set to a date before the current period ends, this will cause a proration if prorations have been enabled using `proration_behavior`. If set during a future period, this will always c…
    #[must_use]
    pub fn cancel_at(mut self, value: serde_json::Value) -> Self {
        self.params.insert("cancel_at".to_owned(), value);
        self
    }

    /// Indicate whether this subscription should cancel at the end of the current period (`current_period_end`). Defaults to `false`.
    #[must_use]
    pub fn cancel_at_period_end(mut self, value: bool) -> Self {
        self.params.insert("cancel_at_period_end".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this subscription at the end of the cycle using the default source attached to the customer. When sending an invoice, Stripe will ema…
    #[must_use]
    pub fn collection_method(mut self, value: impl Into<String>) -> Self {
        self.params.insert("collection_method".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Three-letter \[ISO currency code\](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a \[supported currency\](https://stripe.com/docs/currencies).
    #[must_use]
    pub fn currency(mut self, value: impl Into<String>) -> Self {
        self.params.insert("currency".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Number of days a customer has to pay invoices generated by this subscription. Valid only for subscriptions where `collection_method` is set to `send_invoice`.
    #[must_use]
    pub fn days_until_due(mut self, value: i64) -> Self {
        self.params.insert("days_until_due".to_owned(), serde_json::Value::from(value));
        self
    }

    /// ID of the default payment method for the subscription. It must belong to the customer associated with the subscription. This takes precedence over `default_source`. If neither are set, invoices will use the customer's \[invoice_settings.de…
    #[must_use]
    pub fn default_payment_method(mut self, value: impl Into<String>) -> Self {
        self.params.insert("default_payment_method".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// ID of the default payment source for the subscription. It must belong to the customer associated with the subscription and be in a chargeable state. If `default_payment_method` is also set, `default_payment_method` will take precedence. If…
    #[must_use]
    pub fn default_source(mut self, value: impl Into<String>) -> Self {
        self.params.insert("default_source".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// The tax rates that will apply to any subscription item that does not have `tax_rates` set. Invoices created will have their `default_tax_rates` populated from the subscription.
    #[must_use]
    pub fn default_tax_rates(mut self, value: serde_json::Value) -> Self {
        self.params.insert("default_tax_rates".to_owned(), value);
        self
    }

    /// The coupons to redeem into discounts for the subscription. If not specified or empty, inherits the discount from the subscription's customer.
    #[must_use]
    pub fn discounts(mut self, value: serde_json::Value) -> Self {
        self.params.insert("discounts".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// All invoices will be billed using the specified settings.
    #[must_use]
    pub fn invoice_settings(mut self, value: serde_json::Value) -> Self {
        self.params.insert("invoice_settings".to_owned(), value);
        self
    }

    /// A list of up to 20 subscription items, each with an attached price.
    #[must_use]
    pub fn items(mut self, value: serde_json::Value) -> Self {
        self.params.insert("items".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `false` (on-session).
    #[must_use]
    pub fn off_session(mut self, value: bool) -> Self {
        self.params.insert("off_session".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Controls how Stripe handles the first invoice when payment is required and `collection_method=charge_automatically`. Subscriptions with `collection_method=send_invoice` are automatically activated regardless of the first Invoice status.
    #[must_use]
    pub fn payment_behavior(mut self, value: impl Into<String>) -> Self {
        self.params.insert("payment_behavior".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Payment settings to pass to invoices created by the subscription.
    #[must_use]
    pub fn payment_settings(mut self, value: serde_json::Value) -> Self {
        self.params.insert("payment_settings".to_owned(), value);
        self
    }

    /// Specifies an interval for how often to bill for any pending invoice items. It is analogous to calling \[Create an invoice\](/api/invoices/create) for the given subscription at the specified interval.
    #[must_use]
    pub fn pending_invoice_item_interval(mut self, value: serde_json::Value) -> Self {
        self.params.insert("pending_invoice_item_interval".to_owned(), value);
        self
    }

    /// Determines how to handle \[prorations\](https://docs.stripe.com/billing/subscriptions/prorations) resulting from the `billing_cycle_anchor`. If no value is passed, the default is `create_prorations`.
    #[must_use]
    pub fn proration_behavior(mut self, value: impl Into<String>) -> Self {
        self.params.insert("proration_behavior".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// If specified, the funds from the subscription's invoices will be transferred to the destination and the ID of the resulting transfers will be found on the resulting charges.
    #[must_use]
    pub fn transfer_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("transfer_data".to_owned(), value);
        self
    }

    /// Unix timestamp representing the end of the trial period the customer will get before being charged for the first time. If set, trial_end will override the default trial period of the plan the customer is being subscribed to. The special va…
    #[must_use]
    pub fn trial_end(mut self, value: serde_json::Value) -> Self {
        self.params.insert("trial_end".to_owned(), value);
        self
    }

    /// Indicates if a plan's `trial_period_days` should be applied to the subscription. Setting `trial_end` per subscription is preferred, and this defaults to `false`. Setting this flag to `true` together with `trial_end` is not allowed. See \[U…
    #[must_use]
    pub fn trial_from_plan(mut self, value: bool) -> Self {
        self.params.insert("trial_from_plan".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Integer representing the number of trial period days before the customer is charged for the first time. This will always overwrite any trials that might apply via a subscribed plan. See \[Using trial periods on subscriptions\](https://docs…
    #[must_use]
    pub fn trial_period_days(mut self, value: i64) -> Self {
        self.params.insert("trial_period_days".to_owned(), serde_json::Value::from(value));
        self
    }

    /// Settings related to subscription trials.
    #[must_use]
    pub fn trial_settings(mut self, value: serde_json::Value) -> Self {
        self.params.insert("trial_settings".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Subscription, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}/subscriptions/{subscription_exposed_id}`
///
/// Retrieves the subscription with the given ID.
#[derive(Debug, Clone)]
pub struct GetCustomersCustomerSubscriptionsSubscriptionExposedId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomerSubscriptionsSubscriptionExposedId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, subscription_exposed_id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let subscription_exposed_id = subscription_exposed_id.into();
        Self { path: format!("/v1/customers/{customer}/subscriptions/{subscription_exposed_id}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Subscription, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/customers/{customer}/subscriptions/{subscription_exposed_id}`
///
/// Updates an existing subscription on a customer to match the specified parameters. When changing plans or quantities, we will optionally prorate the price we charge next month to make up for any price changes. To preview how the proration will be calculated, use the upcoming invoice endpoint.
#[derive(Debug, Clone)]
pub struct PostCustomersCustomerSubscriptionsSubscriptionExposedId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCustomersCustomerSubscriptionsSubscriptionExposedId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, subscription_exposed_id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let subscription_exposed_id = subscription_exposed_id.into();
        Self { path: format!("/v1/customers/{customer}/subscriptions/{subscription_exposed_id}"), params }
    }

    /// A list of prices and quantities that will generate invoice items appended to the next invoice for this subscription. You may pass up to 20 items.
    #[must_use]
    pub fn add_invoice_items(mut self, value: serde_json::Value) -> Self {
        self.params.insert("add_invoice_items".to_owned(), value);
        self
    }

    /// A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the application owner's Stripe account. The request must be made by a p…
    #[must_use]
    pub fn application_fee_percent(mut self, value: serde_json::Value) -> Self {
        self.params.insert("application_fee_percent".to_owned(), value);
        self
    }

    /// Automatic tax settings for this subscription. We recommend you only include this parameter when the existing value is being changed.
    #[must_use]
    pub fn automatic_tax(mut self, value: serde_json::Value) -> Self {
        self.params.insert("automatic_tax".to_owned(), value);
        self
    }

    /// Either `now` or `unchanged`. Setting the value to `now` resets the subscription's billing cycle anchor to the current time. For more information, see the billing cycle \[documentation\](https://docs.stripe.com/billing/subscriptions/billing…
    #[must_use]
    pub fn billing_cycle_anchor(mut self, value: impl Into<String>) -> Self {
        self.params.insert("billing_cycle_anchor".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Define thresholds at which an invoice will be sent, and the subscription advanced to a new billing period. When updating, pass an empty string to remove previously-defined thresholds.
    #[must_use]
    pub fn billing_thresholds(mut self, value: serde_json::Value) -> Self {
        self.params.insert("billing_thresholds".to_owned(), value);
        self
    }

    /// A timestamp at which the subscription should cancel. If set to a date before the current period ends, this will cause a proration if prorations have been enabled using `proration_behavior`. If set during a future period, this will always c…
    #[must_use]
    pub fn cancel_at(mut self, value: serde_json::Value) -> Self {
        self.params.insert("cancel_at".to_owned(), value);
        self
    }

    /// Indicate whether this subscription should cancel at the end of the current period (`current_period_end`). Defaults to `false`.
    #[must_use]
    pub fn cancel_at_period_end(mut self, value: bool) -> Self {
        self.params.insert("cancel_at_period_end".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Details about why this subscription was cancelled
    #[must_use]
    pub fn cancellation_details(mut self, value: serde_json::Value) -> Self {
        self.params.insert("cancellation_details".to_owned(), value);
        self
    }

    /// Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this subscription at the end of the cycle using the default source attached to the customer. When sending an invoice, Stripe will ema…
    #[must_use]
    pub fn collection_method(mut self, value: impl Into<String>) -> Self {
        self.params.insert("collection_method".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Number of days a customer has to pay invoices generated by this subscription. Valid only for subscriptions where `collection_method` is set to `send_invoice`.
    #[must_use]
    pub fn days_until_due(mut self, value: i64) -> Self {
        self.params.insert("days_until_due".to_owned(), serde_json::Value::from(value));
        self
    }

    /// ID of the default payment method for the subscription. It must belong to the customer associated with the subscription. This takes precedence over `default_source`. If neither are set, invoices will use the customer's \[invoice_settings.de…
    #[must_use]
    pub fn default_payment_method(mut self, value: impl Into<String>) -> Self {
        self.params.insert("default_payment_method".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// ID of the default payment source for the subscription. It must belong to the customer associated with the subscription and be in a chargeable state. If `default_payment_method` is also set, `default_payment_method` will take precedence. If…
    #[must_use]
    pub fn default_source(mut self, value: serde_json::Value) -> Self {
        self.params.insert("default_source".to_owned(), value);
        self
    }

    /// The tax rates that will apply to any subscription item that does not have `tax_rates` set. Invoices created will have their `default_tax_rates` populated from the subscription. Pass an empty string to remove previously-defined tax rates.
    #[must_use]
    pub fn default_tax_rates(mut self, value: serde_json::Value) -> Self {
        self.params.insert("default_tax_rates".to_owned(), value);
        self
    }

    /// The coupons to redeem into discounts for the subscription. A populated array overwrites the existing discounts on the subscription. If not specified or empty array, it leaves the subscription's discounts unchanged. If empty string, it clea…
    #[must_use]
    pub fn discounts(mut self, value: serde_json::Value) -> Self {
        self.params.insert("discounts".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// All invoices will be billed using the specified settings.
    #[must_use]
    pub fn invoice_settings(mut self, value: serde_json::Value) -> Self {
        self.params.insert("invoice_settings".to_owned(), value);
        self
    }

    /// A list of up to 20 subscription items, each with an attached price.
    #[must_use]
    pub fn items(mut self, value: serde_json::Value) -> Self {
        self.params.insert("items".to_owned(), value);
        self
    }

    /// Set of \[key-value pairs\](https://docs.stripe.com/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an em…
    #[must_use]
    pub fn metadata(mut self, value: serde_json::Value) -> Self {
        self.params.insert("metadata".to_owned(), value);
        self
    }

    /// Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `false` (on-session).
    #[must_use]
    pub fn off_session(mut self, value: bool) -> Self {
        self.params.insert("off_session".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// If specified, payment collection for this subscription will be paused. Note that the subscription status will be unchanged and will not be updated to `paused`. Learn more about \[pausing collection\](https://docs.stripe.com/billing/subscri…
    #[must_use]
    pub fn pause_collection(mut self, value: serde_json::Value) -> Self {
        self.params.insert("pause_collection".to_owned(), value);
        self
    }

    /// Controls how Stripe handles payment when a subscription update requires payment and `collection_method=charge_automatically`.
    #[must_use]
    pub fn payment_behavior(mut self, value: impl Into<String>) -> Self {
        self.params.insert("payment_behavior".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Payment settings to pass to invoices created by the subscription.
    #[must_use]
    pub fn payment_settings(mut self, value: serde_json::Value) -> Self {
        self.params.insert("payment_settings".to_owned(), value);
        self
    }

    /// Specifies an interval for how often to bill for any pending invoice items. It is analogous to calling \[Create an invoice\](/api/invoices/create) for the given subscription at the specified interval.
    #[must_use]
    pub fn pending_invoice_item_interval(mut self, value: serde_json::Value) -> Self {
        self.params.insert("pending_invoice_item_interval".to_owned(), value);
        self
    }

    /// Determines how to handle \[prorations\](https://docs.stripe.com/billing/subscriptions/prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quan…
    #[must_use]
    pub fn proration_behavior(mut self, value: impl Into<String>) -> Self {
        self.params.insert("proration_behavior".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// If set, prorations will be calculated as though the subscription was updated at the given time. This can be used to apply exactly the same prorations that were previewed with the \[create preview\](https://stripe.com/docs/api/invoices/crea…
    #[must_use]
    pub fn proration_date(mut self, value: i64) -> Self {
        self.params.insert("proration_date".to_owned(), serde_json::Value::from(value));
        self
    }

    /// If specified, the funds from the subscription's invoices will be transferred to the destination and the ID of the resulting transfers will be found on the resulting charges. This will be unset if you POST an empty value.
    #[must_use]
    pub fn transfer_data(mut self, value: serde_json::Value) -> Self {
        self.params.insert("transfer_data".to_owned(), value);
        self
    }

    /// Unix timestamp representing the end of the trial period the customer will get before being charged for the first time. This will always overwrite any trials that might apply via a subscribed plan. If set, trial_end will override the defaul…
    #[must_use]
    pub fn trial_end(mut self, value: serde_json::Value) -> Self {
        self.params.insert("trial_end".to_owned(), value);
        self
    }

    /// Indicates if a plan's `trial_period_days` should be applied to the subscription. Setting `trial_end` per subscription is preferred, and this defaults to `false`. Setting this flag to `true` together with `trial_end` is not allowed. See \[U…
    #[must_use]
    pub fn trial_from_plan(mut self, value: bool) -> Self {
        self.params.insert("trial_from_plan".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Settings related to subscription trials.
    #[must_use]
    pub fn trial_settings(mut self, value: serde_json::Value) -> Self {
        self.params.insert("trial_settings".to_owned(), value);
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Subscription, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `DELETE /v1/customers/{customer}/subscriptions/{subscription_exposed_id}`
///
/// Cancels a customer’s subscription. If you set the at_period_end parameter to true, the subscription will remain active until the end of the period, at which point it will be canceled and not renewed. Otherwise, with the default false value, the subscription is terminated immediately. In either case, the customer will not be charged again for the subscription. Note, however, that any pending invoice items that you’ve created will still be charged for at the end of the period, unless manually del…
#[derive(Debug, Clone)]
pub struct DeleteCustomersCustomerSubscriptionsSubscriptionExposedId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteCustomersCustomerSubscriptionsSubscriptionExposedId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, subscription_exposed_id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let subscription_exposed_id = subscription_exposed_id.into();
        Self { path: format!("/v1/customers/{customer}/subscriptions/{subscription_exposed_id}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Can be set to `true` if `at_period_end` is not set to `true`. Will generate a final invoice that invoices for any un-invoiced metered usage and new/pending proration invoice items.
    #[must_use]
    pub fn invoice_now(mut self, value: bool) -> Self {
        self.params.insert("invoice_now".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Can be set to `true` if `at_period_end` is not set to `true`. Will generate a proration invoice item that credits remaining unused time until the subscription period end.
    #[must_use]
    pub fn prorate(mut self, value: bool) -> Self {
        self.params.insert("prorate".to_owned(), serde_json::Value::Bool(value));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Subscription, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount`
#[derive(Debug, Clone)]
pub struct GetCustomersCustomerSubscriptionsSubscriptionExposedIdDiscount {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomerSubscriptionsSubscriptionExposedIdDiscount {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, subscription_exposed_id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let subscription_exposed_id = subscription_exposed_id.into();
        Self { path: format!("/v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::Discount, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `DELETE /v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount`
///
/// Removes the currently applied discount on a customer.
#[derive(Debug, Clone)]
pub struct DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscount {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscount {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, subscription_exposed_id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let subscription_exposed_id = subscription_exposed_id.into();
        Self { path: format!("/v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount"), params }
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::DeletedDiscount, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}/tax_ids`
///
/// Returns a list of tax IDs for a customer.
#[derive(Debug, Clone)]
pub struct GetCustomersCustomerTaxIds {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomerTaxIds {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        Self { path: format!("/v1/customers/{customer}/tax_ids"), params }
    }

    /// A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before…
    #[must_use]
    pub fn ending_before(mut self, value: impl Into<String>) -> Self {
        self.params.insert("ending_before".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
    #[must_use]
    pub fn limit(mut self, value: i64) -> Self {
        self.params.insert("limit".to_owned(), serde_json::Value::from(value));
        self
    }

    /// A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after…
    #[must_use]
    pub fn starting_after(mut self, value: impl Into<String>) -> Self {
        self.params.insert("starting_after".to_owned(), serde_json::Value::String(value.into()));
        self
    }

    /// Turn this list request into a cursor [`crate::Paginator`]:
    /// `starting_after` is threaded automatically between pages.
    #[must_use]
    pub fn paginate(self) -> crate::Paginator<crate::models::TaxId> {
        crate::Paginator::new(self.path, self.params)
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<payrs_stripe_types::List<crate::models::TaxId>, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `POST /v1/customers/{customer}/tax_ids`
///
/// Creates a new tax_id object for a customer.
#[derive(Debug, Clone)]
pub struct PostCustomersCustomerTaxIds {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostCustomersCustomerTaxIds {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, type_: impl Into<String>, value: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        let customer = customer.into();
        params.insert("type".to_owned(), serde_json::Value::String(type_.into()));
        params.insert("value".to_owned(), serde_json::Value::String(value.into()));
        Self { path: format!("/v1/customers/{customer}/tax_ids"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TaxId, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}

/// `GET /v1/customers/{customer}/tax_ids/{id}`
///
/// Retrieves the tax_id object with the given identifier.
#[derive(Debug, Clone)]
pub struct GetCustomersCustomerTaxIdsId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl GetCustomersCustomerTaxIdsId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let id = id.into();
        Self { path: format!("/v1/customers/{customer}/tax_ids/{id}"), params }
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert("expand".to_owned(), serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()));
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::TaxId, Error> {
        let mut spec = RequestSpec::new(Method::Get, self.path);
        spec.raw_query = crate::encode_params(&self.params)?;
        client.execute(spec).await
    }
}

/// `DELETE /v1/customers/{customer}/tax_ids/{id}`
///
/// Deletes an existing tax_id object.
#[derive(Debug, Clone)]
pub struct DeleteCustomersCustomerTaxIdsId {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl DeleteCustomersCustomerTaxIdsId {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(customer: impl Into<String>, id: impl Into<String>) -> Self {
        let params = serde_json::Map::new();
        let customer = customer.into();
        let id = id.into();
        Self { path: format!("/v1/customers/{customer}/tax_ids/{id}"), params }
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::DeletedTaxId, Error> {
        let mut spec = RequestSpec::new(Method::Delete, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
