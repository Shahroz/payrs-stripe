//! Generated operations for the `account_links` section of the Stripe v1 API.
//!
//! Generated by `codegen/generate.py`. **Do not edit by hand.**
#![allow(
    clippy::doc_markdown,
    clippy::too_many_arguments,
    clippy::new_without_default,
    rustdoc::bare_urls
)]

use payrs_stripe_client::{Body, Client, Error, Method, RequestSpec};

/// `POST /v1/account_links`
///
/// Creates an AccountLink object that includes a single-use Stripe URL that the platform can redirect their user to in order to take them through the Connect Onboarding flow.
#[derive(Debug, Clone)]
pub struct PostAccountLinks {
    path: String,
    params: serde_json::Map<String, serde_json::Value>,
}

impl PostAccountLinks {
    /// Start building this request. Required parameters are arguments;
    /// everything optional is a chainable setter.
    #[must_use]
    pub fn new(account: impl Into<String>, type_: impl Into<String>) -> Self {
        let mut params = serde_json::Map::new();
        params.insert(
            "account".to_owned(),
            serde_json::Value::String(account.into()),
        );
        params.insert("type".to_owned(), serde_json::Value::String(type_.into()));
        Self {
            path: "/v1/account_links".to_owned(),
            params,
        }
    }

    /// The collect parameter is deprecated. Use `collection_options` instead.
    #[must_use]
    pub fn collect(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "collect".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Specifies the requirements that Stripe collects from connected accounts in the Connect Onboarding flow.
    #[must_use]
    pub fn collection_options(mut self, value: serde_json::Value) -> Self {
        self.params.insert("collection_options".to_owned(), value);
        self
    }

    /// Specifies which fields in the response should be expanded.
    #[must_use]
    pub fn expand(mut self, value: Vec<String>) -> Self {
        self.params.insert(
            "expand".to_owned(),
            serde_json::Value::Array(value.into_iter().map(serde_json::Value::String).collect()),
        );
        self
    }

    /// The URL the user will be redirected to if the account link is expired, has been previously-visited, or is otherwise invalid. The URL you specify should attempt to generate a new account link with the same parameters used to create the orig…
    #[must_use]
    pub fn refresh_url(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "refresh_url".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// The URL that the user will be redirected to upon leaving or completing the linked flow.
    #[must_use]
    pub fn return_url(mut self, value: impl Into<String>) -> Self {
        self.params.insert(
            "return_url".to_owned(),
            serde_json::Value::String(value.into()),
        );
        self
    }

    /// Set any parameter by name — the escape hatch for params newer
    /// than this generated code or deeply nested shapes.
    #[must_use]
    pub fn param(mut self, key: impl Into<String>, value: serde_json::Value) -> Self {
        self.params.insert(key.into(), value);
        self
    }

    /// Send the request.
    ///
    /// # Errors
    /// See [`payrs_stripe_client::Error`]: API errors, transport errors
    /// after retries, or response deserialization failures.
    pub async fn send(self, client: &Client) -> Result<crate::models::AccountLink, Error> {
        let mut spec = RequestSpec::new(Method::Post, self.path);
        if let Some(body) = crate::encode_params(&self.params)? {
            spec.body = Some(Body::Form(body));
        }
        client.execute(spec).await
    }
}
